apiVersion: v2
name: longhouse
description: Coordination system for organizations and neighborhoods
type: application

# Chart version — updated automatically by release workflow
version: 0.1.0

# Application version — matches the project release version
appVersion: "0.1.0"
