// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-client

import type { BoolResponse, BugReportRequest, Comment, CommentID, CommentListRequest, CompleteRequest, DevLoginRequest, DevUsersResponse, EffectiveSettings, EmptyRequest, EmptyResponse, Event, EventID, Group, GroupID, GroupMemberRef, GroupSkillRef, House, HouseID, HouseListRequest, HouseScopedListRequest, LoginRequest, LoginResponse, MeResponse, Member, MemberAudit, MemberID, MemberRoleRef, MemberScopedListRequest, MemberSkillRef, Milestone, MilestoneID, Project, ProjectID, ProjectMemberRef, ProjectOwnerRef, ProjectScopedListRequest, ProjectTaskOrderRequest, ProjectTaskRef, ResourceRef, Role, RoleID, Share, ShareAccessRequest, ShareID, Skill, SkillID, Task, TaskID, TrustedDomain, TrustedDomainID, UpdateSettingsRequest } from "./types.gen";

export interface ServiceTransport {
  call<TReq, TRes>(
    service: string,
    method: string,
    req: TReq,
    opts?: { signal?: AbortSignal },
  ): Promise<TRes>;
}

export class AuthClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  login(req: LoginRequest, opts?: { signal?: AbortSignal }): Promise<LoginResponse> {
    return this.t.call<LoginRequest, LoginResponse>("auth", "Login", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  complete(req: CompleteRequest, opts?: { signal?: AbortSignal }): Promise<LoginResponse> {
    return this.t.call<CompleteRequest, LoginResponse>("auth", "Complete", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  refresh(req: EmptyRequest, opts?: { signal?: AbortSignal }): Promise<LoginResponse> {
    return this.t.call<EmptyRequest, LoginResponse>("auth", "Refresh", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  me(req: EmptyRequest, opts?: { signal?: AbortSignal }): Promise<MeResponse> {
    return this.t.call<EmptyRequest, MeResponse>("auth", "Me", req, opts);
  }
}

export class BugClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  reportBug(req: BugReportRequest, opts?: { signal?: AbortSignal }): Promise<Task> {
    return this.t.call<BugReportRequest, Task>("bug", "ReportBug", req, opts);
  }
}

export class CommentClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createComment(req: Comment, opts?: { signal?: AbortSignal }): Promise<Comment> {
    return this.t.call<Comment, Comment>("comment", "CreateComment", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getComment(req: CommentID, opts?: { signal?: AbortSignal }): Promise<Comment> {
    return this.t.call<CommentID, Comment>("comment", "GetComment", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateComment(req: Comment, opts?: { signal?: AbortSignal }): Promise<Comment> {
    return this.t.call<Comment, Comment>("comment", "UpdateComment", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteComment(req: CommentID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<CommentID, EmptyResponse>("comment", "DeleteComment", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listComments(req: CommentListRequest, opts?: { signal?: AbortSignal }): Promise<Comment[]> {
    return this.t.call<CommentListRequest, Comment[]>("comment", "ListComments", req, opts);
  }
}

export class DevAuthClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listDevUsers(req: EmptyRequest, opts?: { signal?: AbortSignal }): Promise<DevUsersResponse> {
    return this.t.call<EmptyRequest, DevUsersResponse>("devauth", "ListDevUsers", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  devLogin(req: DevLoginRequest, opts?: { signal?: AbortSignal }): Promise<LoginResponse> {
    return this.t.call<DevLoginRequest, LoginResponse>("devauth", "DevLogin", req, opts);
  }
}

export class EventClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createEvent(req: Event, opts?: { signal?: AbortSignal }): Promise<Event> {
    return this.t.call<Event, Event>("event", "CreateEvent", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getEvent(req: EventID, opts?: { signal?: AbortSignal }): Promise<Event> {
    return this.t.call<EventID, Event>("event", "GetEvent", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateEvent(req: Event, opts?: { signal?: AbortSignal }): Promise<Event> {
    return this.t.call<Event, Event>("event", "UpdateEvent", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteEvent(req: EventID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<EventID, EmptyResponse>("event", "DeleteEvent", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteEventAndFuture(req: EventID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<EventID, EmptyResponse>("event", "DeleteEventAndFuture", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listEvents(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Event[]> {
    return this.t.call<HouseScopedListRequest, Event[]>("event", "ListEvents", req, opts);
  }
}

export class GroupClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createGroup(req: Group, opts?: { signal?: AbortSignal }): Promise<Group> {
    return this.t.call<Group, Group>("group", "CreateGroup", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateGroup(req: Group, opts?: { signal?: AbortSignal }): Promise<Group> {
    return this.t.call<Group, Group>("group", "UpdateGroup", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteGroup(req: GroupID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<GroupID, EmptyResponse>("group", "DeleteGroup", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listGroups(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Group[]> {
    return this.t.call<HouseScopedListRequest, Group[]>("group", "ListGroups", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addGroupMember(req: GroupMemberRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<GroupMemberRef, EmptyResponse>("group", "AddGroupMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeGroupMember(req: GroupMemberRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<GroupMemberRef, EmptyResponse>("group", "RemoveGroupMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listGroupMembers(req: MemberScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Member[]> {
    return this.t.call<MemberScopedListRequest, Member[]>("group", "ListGroupMembers", req, opts);
  }
}

export class HouseClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createHouse(req: House, opts?: { signal?: AbortSignal }): Promise<House> {
    return this.t.call<House, House>("house", "CreateHouse", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getHouse(req: HouseID, opts?: { signal?: AbortSignal }): Promise<House> {
    return this.t.call<HouseID, House>("house", "GetHouse", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateHouse(req: House, opts?: { signal?: AbortSignal }): Promise<House> {
    return this.t.call<House, House>("house", "UpdateHouse", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteHouse(req: HouseID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<HouseID, EmptyResponse>("house", "DeleteHouse", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listHouses(req: HouseListRequest, opts?: { signal?: AbortSignal }): Promise<House[]> {
    return this.t.call<HouseListRequest, House[]>("house", "ListHouses", req, opts);
  }
}

export class MemberAuditClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listAuditsForMember(req: MemberScopedListRequest, opts?: { signal?: AbortSignal }): Promise<MemberAudit[]> {
    return this.t.call<MemberScopedListRequest, MemberAudit[]>("memberaudit", "ListAuditsForMember", req, opts);
  }
}

export class MemberClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createMember(req: Member, opts?: { signal?: AbortSignal }): Promise<Member> {
    return this.t.call<Member, Member>("member", "CreateMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getMember(req: MemberID, opts?: { signal?: AbortSignal }): Promise<Member> {
    return this.t.call<MemberID, Member>("member", "GetMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getMemberByIdentity(req: Member, opts?: { signal?: AbortSignal }): Promise<Member> {
    return this.t.call<Member, Member>("member", "GetMemberByIdentity", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateMember(req: Member, opts?: { signal?: AbortSignal }): Promise<Member> {
    return this.t.call<Member, Member>("member", "UpdateMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteMember(req: MemberID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MemberID, EmptyResponse>("member", "DeleteMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listMembers(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Member[]> {
    return this.t.call<HouseScopedListRequest, Member[]>("member", "ListMembers", req, opts);
  }
}

export class ProjectClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createProject(req: Project, opts?: { signal?: AbortSignal }): Promise<Project> {
    return this.t.call<Project, Project>("project", "CreateProject", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getProject(req: ProjectID, opts?: { signal?: AbortSignal }): Promise<Project> {
    return this.t.call<ProjectID, Project>("project", "GetProject", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateProject(req: Project, opts?: { signal?: AbortSignal }): Promise<Project> {
    return this.t.call<Project, Project>("project", "UpdateProject", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteProject(req: ProjectID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectID, EmptyResponse>("project", "DeleteProject", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listProjects(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Project[]> {
    return this.t.call<HouseScopedListRequest, Project[]>("project", "ListProjects", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listProjectTasks(req: ProjectScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Task[]> {
    return this.t.call<ProjectScopedListRequest, Task[]>("project", "ListProjectTasks", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addProjectTask(req: ProjectTaskOrderRequest, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectTaskOrderRequest, EmptyResponse>("project", "AddProjectTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeProjectTask(req: ProjectTaskRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectTaskRef, EmptyResponse>("project", "RemoveProjectTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  setProjectTaskPosition(req: ProjectTaskOrderRequest, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectTaskOrderRequest, EmptyResponse>("project", "SetProjectTaskPosition", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listProjectMembers(req: ProjectID, opts?: { signal?: AbortSignal }): Promise<Member[]> {
    return this.t.call<ProjectID, Member[]>("project", "ListProjectMembers", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addProjectMember(req: ProjectMemberRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectMemberRef, EmptyResponse>("project", "AddProjectMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeProjectMember(req: ProjectMemberRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectMemberRef, EmptyResponse>("project", "RemoveProjectMember", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listProjectOwners(req: ProjectID, opts?: { signal?: AbortSignal }): Promise<Member[]> {
    return this.t.call<ProjectID, Member[]>("project", "ListProjectOwners", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addProjectOwner(req: ProjectOwnerRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectOwnerRef, EmptyResponse>("project", "AddProjectOwner", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeProjectOwner(req: ProjectOwnerRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ProjectOwnerRef, EmptyResponse>("project", "RemoveProjectOwner", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listMilestones(req: ProjectID, opts?: { signal?: AbortSignal }): Promise<Milestone[]> {
    return this.t.call<ProjectID, Milestone[]>("project", "ListMilestones", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createMilestone(req: Milestone, opts?: { signal?: AbortSignal }): Promise<Milestone> {
    return this.t.call<Milestone, Milestone>("project", "CreateMilestone", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateMilestone(req: Milestone, opts?: { signal?: AbortSignal }): Promise<Milestone> {
    return this.t.call<Milestone, Milestone>("project", "UpdateMilestone", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteMilestone(req: MilestoneID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MilestoneID, EmptyResponse>("project", "DeleteMilestone", req, opts);
  }
}

export class RoleClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createRole(req: Role, opts?: { signal?: AbortSignal }): Promise<Role> {
    return this.t.call<Role, Role>("role", "CreateRole", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateRole(req: Role, opts?: { signal?: AbortSignal }): Promise<Role> {
    return this.t.call<Role, Role>("role", "UpdateRole", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteRole(req: RoleID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<RoleID, EmptyResponse>("role", "DeleteRole", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listRoles(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Role[]> {
    return this.t.call<HouseScopedListRequest, Role[]>("role", "ListRoles", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  grantRole(req: MemberRoleRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MemberRoleRef, EmptyResponse>("role", "GrantRole", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  revokeRole(req: MemberRoleRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MemberRoleRef, EmptyResponse>("role", "RevokeRole", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listMemberRoles(req: MemberScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Role[]> {
    return this.t.call<MemberScopedListRequest, Role[]>("role", "ListMemberRoles", req, opts);
  }
}

export class SettingsClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getSettings(req: HouseID, opts?: { signal?: AbortSignal }): Promise<EffectiveSettings> {
    return this.t.call<HouseID, EffectiveSettings>("settings", "GetSettings", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateSettings(req: UpdateSettingsRequest, opts?: { signal?: AbortSignal }): Promise<EffectiveSettings> {
    return this.t.call<UpdateSettingsRequest, EffectiveSettings>("settings", "UpdateSettings", req, opts);
  }
}

export class ShareClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createShare(req: Share, opts?: { signal?: AbortSignal }): Promise<Share> {
    return this.t.call<Share, Share>("share", "CreateShare", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteShare(req: ShareID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<ShareID, EmptyResponse>("share", "DeleteShare", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listSharesByResource(req: ResourceRef, opts?: { signal?: AbortSignal }): Promise<Share[]> {
    return this.t.call<ResourceRef, Share[]>("share", "ListSharesByResource", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  checkAccess(req: ShareAccessRequest, opts?: { signal?: AbortSignal }): Promise<Share> {
    return this.t.call<ShareAccessRequest, Share>("share", "CheckAccess", req, opts);
  }
}

export class SkillClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createSkill(req: Skill, opts?: { signal?: AbortSignal }): Promise<Skill> {
    return this.t.call<Skill, Skill>("skill", "CreateSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateSkill(req: Skill, opts?: { signal?: AbortSignal }): Promise<Skill> {
    return this.t.call<Skill, Skill>("skill", "UpdateSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteSkill(req: SkillID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<SkillID, EmptyResponse>("skill", "DeleteSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listSkills(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Skill[]> {
    return this.t.call<HouseScopedListRequest, Skill[]>("skill", "ListSkills", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addMemberSkill(req: MemberSkillRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MemberSkillRef, EmptyResponse>("skill", "AddMemberSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeMemberSkill(req: MemberSkillRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<MemberSkillRef, EmptyResponse>("skill", "RemoveMemberSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listMemberSkills(req: MemberScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Skill[]> {
    return this.t.call<MemberScopedListRequest, Skill[]>("skill", "ListMemberSkills", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addGroupSkill(req: GroupSkillRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<GroupSkillRef, EmptyResponse>("skill", "AddGroupSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeGroupSkill(req: GroupSkillRef, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<GroupSkillRef, EmptyResponse>("skill", "RemoveGroupSkill", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listGroupSkills(req: GroupID, opts?: { signal?: AbortSignal }): Promise<Skill[]> {
    return this.t.call<GroupID, Skill[]>("skill", "ListGroupSkills", req, opts);
  }
}

export class TaskClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  createTask(req: Task, opts?: { signal?: AbortSignal }): Promise<Task> {
    return this.t.call<Task, Task>("task", "CreateTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  getTask(req: TaskID, opts?: { signal?: AbortSignal }): Promise<Task> {
    return this.t.call<TaskID, Task>("task", "GetTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  updateTask(req: Task, opts?: { signal?: AbortSignal }): Promise<Task> {
    return this.t.call<Task, Task>("task", "UpdateTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  deleteTask(req: TaskID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<TaskID, EmptyResponse>("task", "DeleteTask", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listTasks(req: HouseScopedListRequest, opts?: { signal?: AbortSignal }): Promise<Task[]> {
    return this.t.call<HouseScopedListRequest, Task[]>("task", "ListTasks", req, opts);
  }
}

export class TrustedDomainClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  addTrustedDomain(req: TrustedDomain, opts?: { signal?: AbortSignal }): Promise<TrustedDomain> {
    return this.t.call<TrustedDomain, TrustedDomain>("trusteddomain", "AddTrustedDomain", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  removeTrustedDomain(req: TrustedDomainID, opts?: { signal?: AbortSignal }): Promise<EmptyResponse> {
    return this.t.call<TrustedDomainID, EmptyResponse>("trusteddomain", "RemoveTrustedDomain", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  listTrustedDomains(req: HouseID, opts?: { signal?: AbortSignal }): Promise<TrustedDomain[]> {
    return this.t.call<HouseID, TrustedDomain[]>("trusteddomain", "ListTrustedDomains", req, opts);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) defined by the transport
   */
  isDomainTrusted(req: TrustedDomain, opts?: { signal?: AbortSignal }): Promise<BoolResponse> {
    return this.t.call<TrustedDomain, BoolResponse>("trusteddomain", "IsDomainTrusted", req, opts);
  }
}

export class ApiClient {
  readonly auth: AuthClient;
  readonly bug: BugClient;
  readonly comment: CommentClient;
  readonly devAuth: DevAuthClient;
  readonly event: EventClient;
  readonly group: GroupClient;
  readonly house: HouseClient;
  readonly memberAudit: MemberAuditClient;
  readonly member: MemberClient;
  readonly project: ProjectClient;
  readonly role: RoleClient;
  readonly settings: SettingsClient;
  readonly share: ShareClient;
  readonly skill: SkillClient;
  readonly task: TaskClient;
  readonly trustedDomain: TrustedDomainClient;
  constructor(t: ServiceTransport) {
    this.auth = new AuthClient(t);
    this.bug = new BugClient(t);
    this.comment = new CommentClient(t);
    this.devAuth = new DevAuthClient(t);
    this.event = new EventClient(t);
    this.group = new GroupClient(t);
    this.house = new HouseClient(t);
    this.memberAudit = new MemberAuditClient(t);
    this.member = new MemberClient(t);
    this.project = new ProjectClient(t);
    this.role = new RoleClient(t);
    this.settings = new SettingsClient(t);
    this.share = new ShareClient(t);
    this.skill = new SkillClient(t);
    this.task = new TaskClient(t);
    this.trustedDomain = new TrustedDomainClient(t);
  }
}
