name: longhouse-web-build-and-deploy
description: "Build web UI Docker image and push to registries"
triggers:
  events:
    - push
  branches:
    - main
paths:
  include: ["version/VERSION.txt"]
job:
  image: containers.catalystsquad.com/public/reactorcide/runnerbase:dev
  command: "bash /job/src/.reactorcide/jobs/scripts/web-build-and-deploy.sh"
  timeout: 1200
  capabilities:
    - docker
environment:
  REGISTRY_INTERNAL: "containers.catalystsquad.com"
  REGISTRY_INTERNAL_PATH: "public/catalystcommunity/longhouse-web"
  REGISTRY_EXTERNAL: "containers.catalystsquad.com"
  REGISTRY_EXTERNAL_PATH: "public/catalystcommunity/longhouse-web"
  REGISTRY_USER: "${secret:catalystcommunity/registry:user}"
  REGISTRY_PASSWORD: "${secret:catalystcommunity/registry:password}"
