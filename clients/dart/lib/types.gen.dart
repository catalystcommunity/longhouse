// Generated CSIL types.
//
// Code generated by csilgen; DO NOT EDIT.

import 'dart:typed_data';
import 'csil_cbor.gen.dart';

typedef HouseId = String;

typedef MemberId = String;

typedef RoleId = String;

typedef SkillId = String;

typedef GroupId = String;

typedef ProjectId = String;

typedef EventId = String;

typedef TaskId = String;

typedef CommentId = String;

typedef ShareId = String;

typedef TrustedDomainId = String;

typedef MemberAuditId = String;

typedef MilestoneId = String;

typedef NotificationId = String;

typedef NotificationEventId = String;

typedef Timestamp = String;

typedef TaskStatus = String;

typedef TargetType = String;

typedef AccessLevel = String;

typedef GranteeType = String;

typedef ResourceType = String;

typedef ProjectStatus = String;

typedef DependencyNodeType = String;

typedef RecurrenceFreq = String;

typedef MilestoneState = String;

final class House {
  final HouseId houseId;
  final String name;
  final String? description;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const House({
    required this.houseId,
    required this.name,
    this.description,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['name'] = name;
    if (description != null) map['description'] = description;
    return map;
  }

  factory House.fromMap(Map<String, Object?> map) {
    return House(
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      description: map['description'] as String?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (name.isEmpty) {
      throw ArgumentError('\'name\' must have length >= 1');
    }
    if (name.length > 256) {
      throw ArgumentError('\'name\' must have length <= 256');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! House) return false;
    return houseId == other.houseId &&
        name == other.name &&
        description == other.description &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([houseId, name, description, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['name'] = name;
    if (description != null) map['description'] = description!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory House.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return House(
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory House.fromCbor(List<int> bytes) =>
      House.fromCborValue(CsilCbor.decode(bytes));
}

final class Member {
  final MemberId memberId;
  final HouseId houseId;
  final String linkkeysDomain;
  final String linkkeysUserId;
  final String? displayName;
  final String? email;
  final String? avatarUrl;
  final Uint8List? cachedPublicKey;
  final Timestamp createdAt;
  final Timestamp updatedAt;
  final Timestamp? lastSeenAt;
  final Timestamp? deactivatedAt;

  const Member({
    required this.memberId,
    required this.houseId,
    required this.linkkeysDomain,
    required this.linkkeysUserId,
    this.displayName,
    this.email,
    this.avatarUrl,
    this.cachedPublicKey,
    required this.createdAt,
    required this.updatedAt,
    this.lastSeenAt,
    this.deactivatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    if (displayName != null) map['display_name'] = displayName;
    if (avatarUrl != null) map['avatar_url'] = avatarUrl;
    if (cachedPublicKey != null) map['cached_public_key'] = cachedPublicKey;
    return map;
  }

  factory Member.fromMap(Map<String, Object?> map) {
    return Member(
      memberId: map['member_id'] as MemberId,
      houseId: map['house_id'] as HouseId,
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      displayName: map['display_name'] as String?,
      email: map['email'] as String?,
      avatarUrl: map['avatar_url'] as String?,
      cachedPublicKey: map['cached_public_key'] as Uint8List?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
      lastSeenAt: map['last_seen_at'] as Timestamp?,
      deactivatedAt: map['deactivated_at'] as Timestamp?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! Member) return false;
    return memberId == other.memberId &&
        houseId == other.houseId &&
        linkkeysDomain == other.linkkeysDomain &&
        linkkeysUserId == other.linkkeysUserId &&
        displayName == other.displayName &&
        email == other.email &&
        avatarUrl == other.avatarUrl &&
        _bytesEqual(cachedPublicKey, other.cachedPublicKey) &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt &&
        lastSeenAt == other.lastSeenAt &&
        deactivatedAt == other.deactivatedAt;
  }

  @override
  int get hashCode => Object.hashAll([memberId, houseId, linkkeysDomain, linkkeysUserId, displayName, email, avatarUrl, cachedPublicKey == null ? null : Object.hashAll(cachedPublicKey!), createdAt, updatedAt, lastSeenAt, deactivatedAt]);

  static bool _bytesEqual(Uint8List? a, Uint8List? b) {
    if (a == null || b == null) return a == b;
    if (a.length != b.length) return false;
    for (var i = 0; i < a.length; i++) {
      if (a[i] != b[i]) return false;
    }
    return true;
  }

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    if (displayName != null) map['display_name'] = displayName!;
    if (avatarUrl != null) map['avatar_url'] = avatarUrl!;
    if (cachedPublicKey != null) map['cached_public_key'] = cachedPublicKey!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Member.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Member(
      memberId: map['member_id'] as String,
      houseId: map['house_id'] as String,
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      displayName: map['display_name'] == null ? null : map['display_name'] as String,
      email: map['email'] == null ? null : map['email'] as String,
      avatarUrl: map['avatar_url'] == null ? null : map['avatar_url'] as String,
      cachedPublicKey: map['cached_public_key'] == null ? null : map['cached_public_key'] as Uint8List,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
      lastSeenAt: map['last_seen_at'] == null ? null : map['last_seen_at'] as String,
      deactivatedAt: map['deactivated_at'] == null ? null : map['deactivated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Member.fromCbor(List<int> bytes) =>
      Member.fromCborValue(CsilCbor.decode(bytes));
}

final class TrustedDomain {
  final TrustedDomainId trustedDomainId;
  final HouseId houseId;
  final String domain;
  final Timestamp createdAt;

  const TrustedDomain({
    required this.trustedDomainId,
    required this.houseId,
    required this.domain,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['domain'] = domain;
    return map;
  }

  factory TrustedDomain.fromMap(Map<String, Object?> map) {
    return TrustedDomain(
      trustedDomainId: map['trusted_domain_id'] as TrustedDomainId,
      houseId: map['house_id'] as HouseId,
      domain: map['domain'] as String,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! TrustedDomain) return false;
    return trustedDomainId == other.trustedDomainId &&
        houseId == other.houseId &&
        domain == other.domain &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([trustedDomainId, houseId, domain, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['domain'] = domain;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory TrustedDomain.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return TrustedDomain(
      trustedDomainId: map['trusted_domain_id'] as String,
      houseId: map['house_id'] as String,
      domain: map['domain'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory TrustedDomain.fromCbor(List<int> bytes) =>
      TrustedDomain.fromCborValue(CsilCbor.decode(bytes));
}

final class Role {
  final RoleId roleId;
  final HouseId houseId;
  final String name;
  final String? description;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Role({
    required this.roleId,
    required this.houseId,
    required this.name,
    this.description,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description;
    return map;
  }

  factory Role.fromMap(Map<String, Object?> map) {
    return Role(
      roleId: map['role_id'] as RoleId,
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      description: map['description'] as String?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (name.isEmpty) {
      throw ArgumentError('\'name\' must have length >= 1');
    }
    if (name.length > 128) {
      throw ArgumentError('\'name\' must have length <= 128');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Role) return false;
    return roleId == other.roleId &&
        houseId == other.houseId &&
        name == other.name &&
        description == other.description &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([roleId, houseId, name, description, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Role.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Role(
      roleId: map['role_id'] as String,
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Role.fromCbor(List<int> bytes) =>
      Role.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberRole {
  final MemberId memberId;
  final RoleId roleId;
  final Timestamp createdAt;

  const MemberRole({
    required this.memberId,
    required this.roleId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['role_id'] = roleId;
    return map;
  }

  factory MemberRole.fromMap(Map<String, Object?> map) {
    return MemberRole(
      memberId: map['member_id'] as MemberId,
      roleId: map['role_id'] as RoleId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberRole) return false;
    return memberId == other.memberId &&
        roleId == other.roleId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([memberId, roleId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['role_id'] = roleId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberRole.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberRole(
      memberId: map['member_id'] as String,
      roleId: map['role_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberRole.fromCbor(List<int> bytes) =>
      MemberRole.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberAudit {
  final MemberAuditId auditId;
  final HouseId houseId;
  final MemberId subjectMemberId;
  final MemberId? actorMemberId;
  final String action;
  final String? targetType;
  final String? targetId;
  final String? detail;
  final Timestamp createdAt;

  const MemberAudit({
    required this.auditId,
    required this.houseId,
    required this.subjectMemberId,
    this.actorMemberId,
    required this.action,
    this.targetType,
    this.targetId,
    this.detail,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['subject_member_id'] = subjectMemberId;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId;
    map['action'] = action;
    if (targetType != null) map['target_type'] = targetType;
    if (targetId != null) map['target_id'] = targetId;
    if (detail != null) map['detail'] = detail;
    return map;
  }

  factory MemberAudit.fromMap(Map<String, Object?> map) {
    return MemberAudit(
      auditId: map['audit_id'] as MemberAuditId,
      houseId: map['house_id'] as HouseId,
      subjectMemberId: map['subject_member_id'] as MemberId,
      actorMemberId: map['actor_member_id'] as MemberId?,
      action: map['action'] as String,
      targetType: map['target_type'] as String?,
      targetId: map['target_id'] as String?,
      detail: map['detail'] as String?,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberAudit) return false;
    return auditId == other.auditId &&
        houseId == other.houseId &&
        subjectMemberId == other.subjectMemberId &&
        actorMemberId == other.actorMemberId &&
        action == other.action &&
        targetType == other.targetType &&
        targetId == other.targetId &&
        detail == other.detail &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([auditId, houseId, subjectMemberId, actorMemberId, action, targetType, targetId, detail, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['subject_member_id'] = subjectMemberId;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId!;
    map['action'] = action;
    if (targetType != null) map['target_type'] = targetType!;
    if (targetId != null) map['target_id'] = targetId!;
    if (detail != null) map['detail'] = detail!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberAudit.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberAudit(
      auditId: map['audit_id'] as String,
      houseId: map['house_id'] as String,
      subjectMemberId: map['subject_member_id'] as String,
      actorMemberId: map['actor_member_id'] == null ? null : map['actor_member_id'] as String,
      action: map['action'] as String,
      targetType: map['target_type'] == null ? null : map['target_type'] as String,
      targetId: map['target_id'] == null ? null : map['target_id'] as String,
      detail: map['detail'] == null ? null : map['detail'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberAudit.fromCbor(List<int> bytes) =>
      MemberAudit.fromCborValue(CsilCbor.decode(bytes));
}

final class Skill {
  final SkillId skillId;
  final HouseId houseId;
  final String name;
  final String? description;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Skill({
    required this.skillId,
    required this.houseId,
    required this.name,
    this.description,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description;
    return map;
  }

  factory Skill.fromMap(Map<String, Object?> map) {
    return Skill(
      skillId: map['skill_id'] as SkillId,
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      description: map['description'] as String?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (name.isEmpty) {
      throw ArgumentError('\'name\' must have length >= 1');
    }
    if (name.length > 256) {
      throw ArgumentError('\'name\' must have length <= 256');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Skill) return false;
    return skillId == other.skillId &&
        houseId == other.houseId &&
        name == other.name &&
        description == other.description &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([skillId, houseId, name, description, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Skill.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Skill(
      skillId: map['skill_id'] as String,
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Skill.fromCbor(List<int> bytes) =>
      Skill.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberSkill {
  final MemberId memberId;
  final SkillId skillId;
  final Timestamp createdAt;

  const MemberSkill({
    required this.memberId,
    required this.skillId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['skill_id'] = skillId;
    return map;
  }

  factory MemberSkill.fromMap(Map<String, Object?> map) {
    return MemberSkill(
      memberId: map['member_id'] as MemberId,
      skillId: map['skill_id'] as SkillId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberSkill) return false;
    return memberId == other.memberId &&
        skillId == other.skillId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([memberId, skillId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['skill_id'] = skillId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberSkill.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberSkill(
      memberId: map['member_id'] as String,
      skillId: map['skill_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberSkill.fromCbor(List<int> bytes) =>
      MemberSkill.fromCborValue(CsilCbor.decode(bytes));
}

final class GroupSkill {
  final GroupId groupId;
  final SkillId skillId;
  final Timestamp createdAt;

  const GroupSkill({
    required this.groupId,
    required this.skillId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['skill_id'] = skillId;
    return map;
  }

  factory GroupSkill.fromMap(Map<String, Object?> map) {
    return GroupSkill(
      groupId: map['group_id'] as GroupId,
      skillId: map['skill_id'] as SkillId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! GroupSkill) return false;
    return groupId == other.groupId &&
        skillId == other.skillId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([groupId, skillId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['skill_id'] = skillId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory GroupSkill.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return GroupSkill(
      groupId: map['group_id'] as String,
      skillId: map['skill_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory GroupSkill.fromCbor(List<int> bytes) =>
      GroupSkill.fromCborValue(CsilCbor.decode(bytes));
}

final class Group {
  final GroupId groupId;
  final HouseId houseId;
  final String name;
  final String? description;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Group({
    required this.groupId,
    required this.houseId,
    required this.name,
    this.description,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description;
    return map;
  }

  factory Group.fromMap(Map<String, Object?> map) {
    return Group(
      groupId: map['group_id'] as GroupId,
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      description: map['description'] as String?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (name.isEmpty) {
      throw ArgumentError('\'name\' must have length >= 1');
    }
    if (name.length > 256) {
      throw ArgumentError('\'name\' must have length <= 256');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Group) return false;
    return groupId == other.groupId &&
        houseId == other.houseId &&
        name == other.name &&
        description == other.description &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([groupId, houseId, name, description, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Group.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Group(
      groupId: map['group_id'] as String,
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Group.fromCbor(List<int> bytes) =>
      Group.fromCborValue(CsilCbor.decode(bytes));
}

final class GroupMember {
  final GroupId groupId;
  final MemberId memberId;
  final Timestamp createdAt;

  const GroupMember({
    required this.groupId,
    required this.memberId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['member_id'] = memberId;
    return map;
  }

  factory GroupMember.fromMap(Map<String, Object?> map) {
    return GroupMember(
      groupId: map['group_id'] as GroupId,
      memberId: map['member_id'] as MemberId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! GroupMember) return false;
    return groupId == other.groupId &&
        memberId == other.memberId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([groupId, memberId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory GroupMember.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return GroupMember(
      groupId: map['group_id'] as String,
      memberId: map['member_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory GroupMember.fromCbor(List<int> bytes) =>
      GroupMember.fromCborValue(CsilCbor.decode(bytes));
}

final class Project {
  final ProjectId projectId;
  final HouseId houseId;
  final String name;
  final String? description;
  final String? category;
  final ProjectStatus? status;
  final AccessLevel? visibility;
  final MemberId? createdByMemberId;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Project({
    required this.projectId,
    required this.houseId,
    required this.name,
    this.description,
    this.category,
    this.status,
    this.visibility,
    this.createdByMemberId,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description;
    if (category != null) map['category'] = category;
    if (status != null) map['status'] = status;
    if (visibility != null) map['visibility'] = visibility;
    return map;
  }

  factory Project.fromMap(Map<String, Object?> map) {
    return Project(
      projectId: map['project_id'] as ProjectId,
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      description: map['description'] as String?,
      category: map['category'] as String?,
      status: map['status'] as ProjectStatus?,
      visibility: map['visibility'] as AccessLevel?,
      createdByMemberId: map['created_by_member_id'] as MemberId?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (name.isEmpty) {
      throw ArgumentError('\'name\' must have length >= 1');
    }
    if (name.length > 256) {
      throw ArgumentError('\'name\' must have length <= 256');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Project) return false;
    return projectId == other.projectId &&
        houseId == other.houseId &&
        name == other.name &&
        description == other.description &&
        category == other.category &&
        status == other.status &&
        visibility == other.visibility &&
        createdByMemberId == other.createdByMemberId &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([projectId, houseId, name, description, category, status, visibility, createdByMemberId, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    if (description != null) map['description'] = description!;
    if (category != null) map['category'] = category!;
    if (status != null) map['status'] = status!;
    if (visibility != null) map['visibility'] = visibility!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Project.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Project(
      projectId: map['project_id'] as String,
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      category: map['category'] == null ? null : map['category'] as String,
      status: map['status'] == null ? null : map['status'],
      visibility: map['visibility'] == null ? null : map['visibility'],
      createdByMemberId: map['created_by_member_id'] == null ? null : map['created_by_member_id'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Project.fromCbor(List<int> bytes) =>
      Project.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectTask {
  final ProjectId projectId;
  final TaskId taskId;
  final int position;
  final Timestamp createdAt;

  const ProjectTask({
    required this.projectId,
    required this.taskId,
    required this.position,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    map['position'] = position;
    return map;
  }

  factory ProjectTask.fromMap(Map<String, Object?> map) {
    return ProjectTask(
      projectId: map['project_id'] as ProjectId,
      taskId: map['task_id'] as TaskId,
      position: map['position'] as int,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectTask) return false;
    return projectId == other.projectId &&
        taskId == other.taskId &&
        position == other.position &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([projectId, taskId, position, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    map['position'] = position;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectTask.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectTask(
      projectId: map['project_id'] as String,
      taskId: map['task_id'] as String,
      position: map['position'] as int,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectTask.fromCbor(List<int> bytes) =>
      ProjectTask.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectMember {
  final ProjectId projectId;
  final MemberId memberId;
  final Timestamp createdAt;

  const ProjectMember({
    required this.projectId,
    required this.memberId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  factory ProjectMember.fromMap(Map<String, Object?> map) {
    return ProjectMember(
      projectId: map['project_id'] as ProjectId,
      memberId: map['member_id'] as MemberId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectMember) return false;
    return projectId == other.projectId &&
        memberId == other.memberId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([projectId, memberId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectMember.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectMember(
      projectId: map['project_id'] as String,
      memberId: map['member_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectMember.fromCbor(List<int> bytes) =>
      ProjectMember.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectOwner {
  final ProjectId projectId;
  final MemberId memberId;
  final Timestamp createdAt;

  const ProjectOwner({
    required this.projectId,
    required this.memberId,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  factory ProjectOwner.fromMap(Map<String, Object?> map) {
    return ProjectOwner(
      projectId: map['project_id'] as ProjectId,
      memberId: map['member_id'] as MemberId,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectOwner) return false;
    return projectId == other.projectId &&
        memberId == other.memberId &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([projectId, memberId, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectOwner.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectOwner(
      projectId: map['project_id'] as String,
      memberId: map['member_id'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectOwner.fromCbor(List<int> bytes) =>
      ProjectOwner.fromCborValue(CsilCbor.decode(bytes));
}

final class Milestone {
  final MilestoneId milestoneId;
  final ProjectId projectId;
  final String label;
  final String whenLabel;
  final MilestoneState state;
  final int position;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Milestone({
    required this.milestoneId,
    required this.projectId,
    required this.label,
    required this.whenLabel,
    required this.state,
    required this.position,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['label'] = label;
    map['when_label'] = whenLabel;
    map['state'] = state;
    map['position'] = position;
    return map;
  }

  factory Milestone.fromMap(Map<String, Object?> map) {
    return Milestone(
      milestoneId: map['milestone_id'] as MilestoneId,
      projectId: map['project_id'] as ProjectId,
      label: map['label'] as String,
      whenLabel: map['when_label'] as String,
      state: map['state'] as MilestoneState,
      position: map['position'] as int,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (label.isEmpty) {
      throw ArgumentError('\'label\' must have length >= 1');
    }
    if (label.length > 256) {
      throw ArgumentError('\'label\' must have length <= 256');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Milestone) return false;
    return milestoneId == other.milestoneId &&
        projectId == other.projectId &&
        label == other.label &&
        whenLabel == other.whenLabel &&
        state == other.state &&
        position == other.position &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([milestoneId, projectId, label, whenLabel, state, position, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['label'] = label;
    map['when_label'] = whenLabel;
    map['state'] = state;
    map['position'] = position;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Milestone.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Milestone(
      milestoneId: map['milestone_id'] as String,
      projectId: map['project_id'] as String,
      label: map['label'] as String,
      whenLabel: map['when_label'] as String,
      state: map['state'],
      position: map['position'] as int,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Milestone.fromCbor(List<int> bytes) =>
      Milestone.fromCborValue(CsilCbor.decode(bytes));
}

final class Event {
  final EventId eventId;
  final HouseId houseId;
  final MemberId ownerMemberId;
  final String title;
  final String? description;
  final String? location;
  final Timestamp? startsAt;
  final Timestamp? endsAt;
  final bool? allDay;
  final RecurrenceFreq? recurrenceFreq;
  final int? recurrenceInterval;
  final List<int>? recurrenceByWeekday;
  final int? recurrenceBySetpos;
  final Timestamp? nextRecurrenceAt;
  final EventId? recurrenceRootEventId;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Event({
    required this.eventId,
    required this.houseId,
    required this.ownerMemberId,
    required this.title,
    this.description,
    this.location,
    this.startsAt,
    this.endsAt,
    this.allDay,
    this.recurrenceFreq,
    this.recurrenceInterval,
    this.recurrenceByWeekday,
    this.recurrenceBySetpos,
    this.nextRecurrenceAt,
    this.recurrenceRootEventId,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['owner_member_id'] = ownerMemberId;
    map['title'] = title;
    if (description != null) map['description'] = description;
    if (location != null) map['location'] = location;
    if (startsAt != null) map['starts_at'] = startsAt;
    if (endsAt != null) map['ends_at'] = endsAt;
    if (allDay != null) map['all_day'] = allDay;
    if (recurrenceFreq != null) map['recurrence_freq'] = recurrenceFreq;
    if (recurrenceInterval != null) map['recurrence_interval'] = recurrenceInterval;
    if (recurrenceByWeekday != null) map['recurrence_by_weekday'] = recurrenceByWeekday;
    if (recurrenceBySetpos != null) map['recurrence_by_setpos'] = recurrenceBySetpos;
    if (nextRecurrenceAt != null) map['next_recurrence_at'] = nextRecurrenceAt;
    if (recurrenceRootEventId != null) map['recurrence_root_event_id'] = recurrenceRootEventId;
    return map;
  }

  factory Event.fromMap(Map<String, Object?> map) {
    return Event(
      eventId: map['event_id'] as EventId,
      houseId: map['house_id'] as HouseId,
      ownerMemberId: map['owner_member_id'] as MemberId,
      title: map['title'] as String,
      description: map['description'] as String?,
      location: map['location'] as String?,
      startsAt: map['starts_at'] as Timestamp?,
      endsAt: map['ends_at'] as Timestamp?,
      allDay: map['all_day'] as bool?,
      recurrenceFreq: map['recurrence_freq'] as RecurrenceFreq?,
      recurrenceInterval: map['recurrence_interval'] as int?,
      recurrenceByWeekday: map['recurrence_by_weekday'] as List<int>?,
      recurrenceBySetpos: map['recurrence_by_setpos'] as int?,
      nextRecurrenceAt: map['next_recurrence_at'] as Timestamp?,
      recurrenceRootEventId: map['recurrence_root_event_id'] as EventId?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (title.isEmpty) {
      throw ArgumentError('\'title\' must have length >= 1');
    }
    if (title.length > 512) {
      throw ArgumentError('\'title\' must have length <= 512');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Event) return false;
    return eventId == other.eventId &&
        houseId == other.houseId &&
        ownerMemberId == other.ownerMemberId &&
        title == other.title &&
        description == other.description &&
        location == other.location &&
        startsAt == other.startsAt &&
        endsAt == other.endsAt &&
        allDay == other.allDay &&
        recurrenceFreq == other.recurrenceFreq &&
        recurrenceInterval == other.recurrenceInterval &&
        recurrenceByWeekday == other.recurrenceByWeekday &&
        recurrenceBySetpos == other.recurrenceBySetpos &&
        nextRecurrenceAt == other.nextRecurrenceAt &&
        recurrenceRootEventId == other.recurrenceRootEventId &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([eventId, houseId, ownerMemberId, title, description, location, startsAt, endsAt, allDay, recurrenceFreq, recurrenceInterval, recurrenceByWeekday, recurrenceBySetpos, nextRecurrenceAt, recurrenceRootEventId, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['owner_member_id'] = ownerMemberId;
    map['title'] = title;
    if (description != null) map['description'] = description!;
    if (location != null) map['location'] = location!;
    if (startsAt != null) map['starts_at'] = startsAt!;
    if (endsAt != null) map['ends_at'] = endsAt!;
    if (allDay != null) map['all_day'] = allDay!;
    if (recurrenceFreq != null) map['recurrence_freq'] = recurrenceFreq!;
    if (recurrenceInterval != null) map['recurrence_interval'] = recurrenceInterval!;
    if (recurrenceByWeekday != null) map['recurrence_by_weekday'] = recurrenceByWeekday!;
    if (recurrenceBySetpos != null) map['recurrence_by_setpos'] = recurrenceBySetpos!;
    if (nextRecurrenceAt != null) map['next_recurrence_at'] = nextRecurrenceAt!;
    if (recurrenceRootEventId != null) map['recurrence_root_event_id'] = recurrenceRootEventId!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Event.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Event(
      eventId: map['event_id'] as String,
      houseId: map['house_id'] as String,
      ownerMemberId: map['owner_member_id'] as String,
      title: map['title'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      location: map['location'] == null ? null : map['location'] as String,
      startsAt: map['starts_at'] == null ? null : map['starts_at'] as String,
      endsAt: map['ends_at'] == null ? null : map['ends_at'] as String,
      allDay: map['all_day'] == null ? null : map['all_day'] as bool,
      recurrenceFreq: map['recurrence_freq'] == null ? null : map['recurrence_freq'],
      recurrenceInterval: map['recurrence_interval'] == null ? null : map['recurrence_interval'] as int,
      recurrenceByWeekday: map['recurrence_by_weekday'] == null ? null : (map['recurrence_by_weekday'] as List).map((csilE) => csilE as int).cast<int>().toList(),
      recurrenceBySetpos: map['recurrence_by_setpos'] == null ? null : map['recurrence_by_setpos'] as int,
      nextRecurrenceAt: map['next_recurrence_at'] == null ? null : map['next_recurrence_at'] as String,
      recurrenceRootEventId: map['recurrence_root_event_id'] == null ? null : map['recurrence_root_event_id'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Event.fromCbor(List<int> bytes) =>
      Event.fromCborValue(CsilCbor.decode(bytes));
}

final class Task {
  final TaskId taskId;
  final HouseId houseId;
  final MemberId ownerMemberId;
  final List<MemberId>? assignees;
  final SkillId? assignedToSkillId;
  final TaskId? parentTaskId;
  final AccessLevel? visibility;
  final String title;
  final String? description;
  final TaskStatus? status;
  final Timestamp? dueAt;
  final String? tag;
  final int? estimateMinutes;
  final RecurrenceFreq? recurrenceFreq;
  final int? recurrenceInterval;
  final List<int>? recurrenceByWeekday;
  final int? recurrenceBySetpos;
  final Timestamp? nextRecurrenceAt;
  final TaskId? recurrenceRootTaskId;
  final Timestamp? deletedAt;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Task({
    required this.taskId,
    required this.houseId,
    required this.ownerMemberId,
    this.assignees,
    this.assignedToSkillId,
    this.parentTaskId,
    this.visibility,
    required this.title,
    this.description,
    this.status,
    this.dueAt,
    this.tag,
    this.estimateMinutes,
    this.recurrenceFreq,
    this.recurrenceInterval,
    this.recurrenceByWeekday,
    this.recurrenceBySetpos,
    this.nextRecurrenceAt,
    this.recurrenceRootTaskId,
    this.deletedAt,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['owner_member_id'] = ownerMemberId;
    if (assignees != null) map['assignees'] = assignees;
    if (assignedToSkillId != null) map['assigned_to_skill_id'] = assignedToSkillId;
    if (parentTaskId != null) map['parent_task_id'] = parentTaskId;
    if (visibility != null) map['visibility'] = visibility;
    map['title'] = title;
    if (description != null) map['description'] = description;
    if (status != null) map['status'] = status;
    if (dueAt != null) map['due_at'] = dueAt;
    if (tag != null) map['tag'] = tag;
    if (estimateMinutes != null) map['estimate_minutes'] = estimateMinutes;
    if (recurrenceFreq != null) map['recurrence_freq'] = recurrenceFreq;
    if (recurrenceInterval != null) map['recurrence_interval'] = recurrenceInterval;
    if (recurrenceByWeekday != null) map['recurrence_by_weekday'] = recurrenceByWeekday;
    if (recurrenceBySetpos != null) map['recurrence_by_setpos'] = recurrenceBySetpos;
    if (nextRecurrenceAt != null) map['next_recurrence_at'] = nextRecurrenceAt;
    if (recurrenceRootTaskId != null) map['recurrence_root_task_id'] = recurrenceRootTaskId;
    if (deletedAt != null) map['deleted_at'] = deletedAt;
    return map;
  }

  factory Task.fromMap(Map<String, Object?> map) {
    return Task(
      taskId: map['task_id'] as TaskId,
      houseId: map['house_id'] as HouseId,
      ownerMemberId: map['owner_member_id'] as MemberId,
      assignees: map['assignees'] as List<MemberId>?,
      assignedToSkillId: map['assigned_to_skill_id'] as SkillId?,
      parentTaskId: map['parent_task_id'] as TaskId?,
      visibility: map['visibility'] as AccessLevel?,
      title: map['title'] as String,
      description: map['description'] as String?,
      status: map['status'] as TaskStatus?,
      dueAt: map['due_at'] as Timestamp?,
      tag: map['tag'] as String?,
      estimateMinutes: map['estimate_minutes'] as int?,
      recurrenceFreq: map['recurrence_freq'] as RecurrenceFreq?,
      recurrenceInterval: map['recurrence_interval'] as int?,
      recurrenceByWeekday: map['recurrence_by_weekday'] as List<int>?,
      recurrenceBySetpos: map['recurrence_by_setpos'] as int?,
      nextRecurrenceAt: map['next_recurrence_at'] as Timestamp?,
      recurrenceRootTaskId: map['recurrence_root_task_id'] as TaskId?,
      deletedAt: map['deleted_at'] as Timestamp?,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (title.isEmpty) {
      throw ArgumentError('\'title\' must have length >= 1');
    }
    if (title.length > 512) {
      throw ArgumentError('\'title\' must have length <= 512');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Task) return false;
    return taskId == other.taskId &&
        houseId == other.houseId &&
        ownerMemberId == other.ownerMemberId &&
        assignees == other.assignees &&
        assignedToSkillId == other.assignedToSkillId &&
        parentTaskId == other.parentTaskId &&
        visibility == other.visibility &&
        title == other.title &&
        description == other.description &&
        status == other.status &&
        dueAt == other.dueAt &&
        tag == other.tag &&
        estimateMinutes == other.estimateMinutes &&
        recurrenceFreq == other.recurrenceFreq &&
        recurrenceInterval == other.recurrenceInterval &&
        recurrenceByWeekday == other.recurrenceByWeekday &&
        recurrenceBySetpos == other.recurrenceBySetpos &&
        nextRecurrenceAt == other.nextRecurrenceAt &&
        recurrenceRootTaskId == other.recurrenceRootTaskId &&
        deletedAt == other.deletedAt &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([taskId, houseId, ownerMemberId, assignees, assignedToSkillId, parentTaskId, visibility, title, description, status, dueAt, tag, estimateMinutes, recurrenceFreq, recurrenceInterval, recurrenceByWeekday, recurrenceBySetpos, nextRecurrenceAt, recurrenceRootTaskId, deletedAt, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['owner_member_id'] = ownerMemberId;
    if (assignees != null) map['assignees'] = assignees!;
    if (assignedToSkillId != null) map['assigned_to_skill_id'] = assignedToSkillId!;
    if (parentTaskId != null) map['parent_task_id'] = parentTaskId!;
    if (visibility != null) map['visibility'] = visibility!;
    map['title'] = title;
    if (description != null) map['description'] = description!;
    if (status != null) map['status'] = status!;
    if (dueAt != null) map['due_at'] = dueAt!;
    if (tag != null) map['tag'] = tag!;
    if (estimateMinutes != null) map['estimate_minutes'] = estimateMinutes!;
    if (recurrenceFreq != null) map['recurrence_freq'] = recurrenceFreq!;
    if (recurrenceInterval != null) map['recurrence_interval'] = recurrenceInterval!;
    if (recurrenceByWeekday != null) map['recurrence_by_weekday'] = recurrenceByWeekday!;
    if (recurrenceBySetpos != null) map['recurrence_by_setpos'] = recurrenceBySetpos!;
    if (nextRecurrenceAt != null) map['next_recurrence_at'] = nextRecurrenceAt!;
    if (recurrenceRootTaskId != null) map['recurrence_root_task_id'] = recurrenceRootTaskId!;
    if (deletedAt != null) map['deleted_at'] = deletedAt!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Task.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Task(
      taskId: map['task_id'] as String,
      houseId: map['house_id'] as String,
      ownerMemberId: map['owner_member_id'] as String,
      assignees: map['assignees'] == null ? null : (map['assignees'] as List).map((csilE) => csilE as String).cast<MemberId>().toList(),
      assignedToSkillId: map['assigned_to_skill_id'] == null ? null : map['assigned_to_skill_id'] as String,
      parentTaskId: map['parent_task_id'] == null ? null : map['parent_task_id'] as String,
      visibility: map['visibility'] == null ? null : map['visibility'],
      title: map['title'] as String,
      description: map['description'] == null ? null : map['description'] as String,
      status: map['status'] == null ? null : map['status'],
      dueAt: map['due_at'] == null ? null : map['due_at'] as String,
      tag: map['tag'] == null ? null : map['tag'] as String,
      estimateMinutes: map['estimate_minutes'] == null ? null : map['estimate_minutes'] as int,
      recurrenceFreq: map['recurrence_freq'] == null ? null : map['recurrence_freq'],
      recurrenceInterval: map['recurrence_interval'] == null ? null : map['recurrence_interval'] as int,
      recurrenceByWeekday: map['recurrence_by_weekday'] == null ? null : (map['recurrence_by_weekday'] as List).map((csilE) => csilE as int).cast<int>().toList(),
      recurrenceBySetpos: map['recurrence_by_setpos'] == null ? null : map['recurrence_by_setpos'] as int,
      nextRecurrenceAt: map['next_recurrence_at'] == null ? null : map['next_recurrence_at'] as String,
      recurrenceRootTaskId: map['recurrence_root_task_id'] == null ? null : map['recurrence_root_task_id'] as String,
      deletedAt: map['deleted_at'] == null ? null : map['deleted_at'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Task.fromCbor(List<int> bytes) =>
      Task.fromCborValue(CsilCbor.decode(bytes));
}

final class Comment {
  final CommentId commentId;
  final HouseId houseId;
  final MemberId memberId;
  final TargetType targetType;
  final String targetId;
  final String body;
  final Timestamp createdAt;
  final Timestamp updatedAt;

  const Comment({
    required this.commentId,
    required this.houseId,
    required this.memberId,
    required this.targetType,
    required this.targetId,
    required this.body,
    required this.createdAt,
    required this.updatedAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['target_type'] = targetType;
    map['target_id'] = targetId;
    map['body'] = body;
    return map;
  }

  factory Comment.fromMap(Map<String, Object?> map) {
    return Comment(
      commentId: map['comment_id'] as CommentId,
      houseId: map['house_id'] as HouseId,
      memberId: map['member_id'] as MemberId,
      targetType: map['target_type'] as TargetType,
      targetId: map['target_id'] as String,
      body: map['body'] as String,
      createdAt: map['created_at'] as Timestamp,
      updatedAt: map['updated_at'] as Timestamp,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (body.isEmpty) {
      throw ArgumentError('\'body\' must have length >= 1');
    }
    if (body.length > 10000) {
      throw ArgumentError('\'body\' must have length <= 10000');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! Comment) return false;
    return commentId == other.commentId &&
        houseId == other.houseId &&
        memberId == other.memberId &&
        targetType == other.targetType &&
        targetId == other.targetId &&
        body == other.body &&
        createdAt == other.createdAt &&
        updatedAt == other.updatedAt;
  }

  @override
  int get hashCode => Object.hashAll([commentId, houseId, memberId, targetType, targetId, body, createdAt, updatedAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['target_type'] = targetType;
    map['target_id'] = targetId;
    map['body'] = body;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Comment.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Comment(
      commentId: map['comment_id'] as String,
      houseId: map['house_id'] as String,
      memberId: map['member_id'] as String,
      targetType: map['target_type'],
      targetId: map['target_id'] as String,
      body: map['body'] as String,
      createdAt: map['created_at'] as String,
      updatedAt: map['updated_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Comment.fromCbor(List<int> bytes) =>
      Comment.fromCborValue(CsilCbor.decode(bytes));
}

final class Share {
  final ShareId shareId;
  final HouseId houseId;
  final MemberId sharedBy;
  final String linkkeysDomain;
  final String linkkeysUserId;
  final ResourceType resourceType;
  final String resourceId;
  final AccessLevel? accessLevel;
  final Timestamp createdAt;
  final Timestamp? expiresAt;

  const Share({
    required this.shareId,
    required this.houseId,
    required this.sharedBy,
    required this.linkkeysDomain,
    required this.linkkeysUserId,
    required this.resourceType,
    required this.resourceId,
    this.accessLevel,
    required this.createdAt,
    this.expiresAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    if (accessLevel != null) map['access_level'] = accessLevel;
    if (expiresAt != null) map['expires_at'] = expiresAt;
    return map;
  }

  factory Share.fromMap(Map<String, Object?> map) {
    return Share(
      shareId: map['share_id'] as ShareId,
      houseId: map['house_id'] as HouseId,
      sharedBy: map['shared_by'] as MemberId,
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      resourceType: map['resource_type'] as ResourceType,
      resourceId: map['resource_id'] as String,
      accessLevel: map['access_level'] as AccessLevel?,
      createdAt: map['created_at'] as Timestamp,
      expiresAt: map['expires_at'] as Timestamp?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! Share) return false;
    return shareId == other.shareId &&
        houseId == other.houseId &&
        sharedBy == other.sharedBy &&
        linkkeysDomain == other.linkkeysDomain &&
        linkkeysUserId == other.linkkeysUserId &&
        resourceType == other.resourceType &&
        resourceId == other.resourceId &&
        accessLevel == other.accessLevel &&
        createdAt == other.createdAt &&
        expiresAt == other.expiresAt;
  }

  @override
  int get hashCode => Object.hashAll([shareId, houseId, sharedBy, linkkeysDomain, linkkeysUserId, resourceType, resourceId, accessLevel, createdAt, expiresAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    if (accessLevel != null) map['access_level'] = accessLevel!;
    if (expiresAt != null) map['expires_at'] = expiresAt!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Share.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Share(
      shareId: map['share_id'] as String,
      houseId: map['house_id'] as String,
      sharedBy: map['shared_by'] as String,
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      resourceType: map['resource_type'],
      resourceId: map['resource_id'] as String,
      accessLevel: map['access_level'] == null ? null : map['access_level'],
      createdAt: map['created_at'] as String,
      expiresAt: map['expires_at'] == null ? null : map['expires_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Share.fromCbor(List<int> bytes) =>
      Share.fromCborValue(CsilCbor.decode(bytes));
}

final class HouseSummary {
  final HouseId houseId;
  final String name;
  final MemberId memberId;
  final List<String> roles;

  const HouseSummary({
    required this.houseId,
    required this.name,
    required this.memberId,
    required this.roles,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    map['member_id'] = memberId;
    map['roles'] = roles;
    return map;
  }

  factory HouseSummary.fromMap(Map<String, Object?> map) {
    return HouseSummary(
      houseId: map['house_id'] as HouseId,
      name: map['name'] as String,
      memberId: map['member_id'] as MemberId,
      roles: map['roles'] as List<String>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! HouseSummary) return false;
    return houseId == other.houseId &&
        name == other.name &&
        memberId == other.memberId &&
        roles == other.roles;
  }

  @override
  int get hashCode => Object.hashAll([houseId, name, memberId, roles]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['name'] = name;
    map['member_id'] = memberId;
    map['roles'] = roles;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory HouseSummary.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return HouseSummary(
      houseId: map['house_id'] as String,
      name: map['name'] as String,
      memberId: map['member_id'] as String,
      roles: (map['roles'] as List).map((csilE) => csilE as String).cast<String>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory HouseSummary.fromCbor(List<int> bytes) =>
      HouseSummary.fromCborValue(CsilCbor.decode(bytes));
}

final class HouseRoles {
  final HouseId house;
  final MemberId member;
  final List<String> roles;

  const HouseRoles({
    required this.house,
    required this.member,
    required this.roles,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house'] = house;
    map['member'] = member;
    map['roles'] = roles;
    return map;
  }

  factory HouseRoles.fromMap(Map<String, Object?> map) {
    return HouseRoles(
      house: map['house'] as HouseId,
      member: map['member'] as MemberId,
      roles: map['roles'] as List<String>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! HouseRoles) return false;
    return house == other.house &&
        member == other.member &&
        roles == other.roles;
  }

  @override
  int get hashCode => Object.hashAll([house, member, roles]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house'] = house;
    map['member'] = member;
    map['roles'] = roles;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory HouseRoles.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return HouseRoles(
      house: map['house'] as String,
      member: map['member'] as String,
      roles: (map['roles'] as List).map((csilE) => csilE as String).cast<String>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory HouseRoles.fromCbor(List<int> bytes) =>
      HouseRoles.fromCborValue(CsilCbor.decode(bytes));
}

final class Identity {
  final String domain;
  final String userId;
  final String? displayName;
  final List<HouseRoles> houses;
  final int iat;
  final int exp;

  const Identity({
    required this.domain,
    required this.userId,
    this.displayName,
    required this.houses,
    required this.iat,
    required this.exp,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName;
    map['houses'] = houses;
    map['iat'] = iat;
    map['exp'] = exp;
    return map;
  }

  factory Identity.fromMap(Map<String, Object?> map) {
    return Identity(
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] as String?,
      houses: map['houses'] as List<HouseRoles>,
      iat: map['iat'] as int,
      exp: map['exp'] as int,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! Identity) return false;
    return domain == other.domain &&
        userId == other.userId &&
        displayName == other.displayName &&
        houses == other.houses &&
        iat == other.iat &&
        exp == other.exp;
  }

  @override
  int get hashCode => Object.hashAll([domain, userId, displayName, houses, iat, exp]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName!;
    map['houses'] = houses.map((csilE) => csilE.toCborValue()).toList();
    map['iat'] = iat;
    map['exp'] = exp;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Identity.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Identity(
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] == null ? null : map['display_name'] as String,
      houses: (map['houses'] as List).map((csilE) => HouseRoles.fromCborValue(csilE)).cast<HouseRoles>().toList(),
      iat: map['iat'] as int,
      exp: map['exp'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Identity.fromCbor(List<int> bytes) =>
      Identity.fromCborValue(CsilCbor.decode(bytes));
}

final class LoginRequest {
  final String signedAssertion;

  const LoginRequest({
    required this.signedAssertion,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['signed_assertion'] = signedAssertion;
    return map;
  }

  factory LoginRequest.fromMap(Map<String, Object?> map) {
    return LoginRequest(
      signedAssertion: map['signed_assertion'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! LoginRequest) return false;
    return signedAssertion == other.signedAssertion;
  }

  @override
  int get hashCode => Object.hashAll([signedAssertion]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['signed_assertion'] = signedAssertion;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory LoginRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return LoginRequest(
      signedAssertion: map['signed_assertion'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory LoginRequest.fromCbor(List<int> bytes) =>
      LoginRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class CompleteRequest {
  final String encryptedToken;

  const CompleteRequest({
    required this.encryptedToken,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['encrypted_token'] = encryptedToken;
    return map;
  }

  factory CompleteRequest.fromMap(Map<String, Object?> map) {
    return CompleteRequest(
      encryptedToken: map['encrypted_token'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! CompleteRequest) return false;
    return encryptedToken == other.encryptedToken;
  }

  @override
  int get hashCode => Object.hashAll([encryptedToken]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['encrypted_token'] = encryptedToken;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory CompleteRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return CompleteRequest(
      encryptedToken: map['encrypted_token'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory CompleteRequest.fromCbor(List<int> bytes) =>
      CompleteRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class LoginResponse {
  final String token;
  final String domain;
  final String userId;
  final String? displayName;
  final Timestamp expiresAt;

  const LoginResponse({
    required this.token,
    required this.domain,
    required this.userId,
    this.displayName,
    required this.expiresAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['token'] = token;
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName;
    map['expires_at'] = expiresAt;
    return map;
  }

  factory LoginResponse.fromMap(Map<String, Object?> map) {
    return LoginResponse(
      token: map['token'] as String,
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] as String?,
      expiresAt: map['expires_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! LoginResponse) return false;
    return token == other.token &&
        domain == other.domain &&
        userId == other.userId &&
        displayName == other.displayName &&
        expiresAt == other.expiresAt;
  }

  @override
  int get hashCode => Object.hashAll([token, domain, userId, displayName, expiresAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['token'] = token;
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName!;
    map['expires_at'] = expiresAt;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory LoginResponse.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return LoginResponse(
      token: map['token'] as String,
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] == null ? null : map['display_name'] as String,
      expiresAt: map['expires_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory LoginResponse.fromCbor(List<int> bytes) =>
      LoginResponse.fromCborValue(CsilCbor.decode(bytes));
}

final class DevUserEntry {
  final MemberId memberId;
  final HouseId houseId;
  final String houseName;
  final String? displayName;
  final String? linkkeysDomain;
  final String? linkkeysUserId;
  final List<String> roles;

  const DevUserEntry({
    required this.memberId,
    required this.houseId,
    required this.houseName,
    this.displayName,
    this.linkkeysDomain,
    this.linkkeysUserId,
    required this.roles,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['house_id'] = houseId;
    map['house_name'] = houseName;
    if (displayName != null) map['display_name'] = displayName;
    if (linkkeysDomain != null) map['linkkeys_domain'] = linkkeysDomain;
    if (linkkeysUserId != null) map['linkkeys_user_id'] = linkkeysUserId;
    map['roles'] = roles;
    return map;
  }

  factory DevUserEntry.fromMap(Map<String, Object?> map) {
    return DevUserEntry(
      memberId: map['member_id'] as MemberId,
      houseId: map['house_id'] as HouseId,
      houseName: map['house_name'] as String,
      displayName: map['display_name'] as String?,
      linkkeysDomain: map['linkkeys_domain'] as String?,
      linkkeysUserId: map['linkkeys_user_id'] as String?,
      roles: map['roles'] as List<String>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DevUserEntry) return false;
    return memberId == other.memberId &&
        houseId == other.houseId &&
        houseName == other.houseName &&
        displayName == other.displayName &&
        linkkeysDomain == other.linkkeysDomain &&
        linkkeysUserId == other.linkkeysUserId &&
        roles == other.roles;
  }

  @override
  int get hashCode => Object.hashAll([memberId, houseId, houseName, displayName, linkkeysDomain, linkkeysUserId, roles]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['house_id'] = houseId;
    map['house_name'] = houseName;
    if (displayName != null) map['display_name'] = displayName!;
    if (linkkeysDomain != null) map['linkkeys_domain'] = linkkeysDomain!;
    if (linkkeysUserId != null) map['linkkeys_user_id'] = linkkeysUserId!;
    map['roles'] = roles;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DevUserEntry.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DevUserEntry(
      memberId: map['member_id'] as String,
      houseId: map['house_id'] as String,
      houseName: map['house_name'] as String,
      displayName: map['display_name'] == null ? null : map['display_name'] as String,
      linkkeysDomain: map['linkkeys_domain'] == null ? null : map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] == null ? null : map['linkkeys_user_id'] as String,
      roles: (map['roles'] as List).map((csilE) => csilE as String).cast<String>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DevUserEntry.fromCbor(List<int> bytes) =>
      DevUserEntry.fromCborValue(CsilCbor.decode(bytes));
}

final class DevUsersResponse {
  final List<DevUserEntry> users;

  const DevUsersResponse({
    required this.users,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['users'] = users;
    return map;
  }

  factory DevUsersResponse.fromMap(Map<String, Object?> map) {
    return DevUsersResponse(
      users: map['users'] as List<DevUserEntry>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DevUsersResponse) return false;
    return users == other.users;
  }

  @override
  int get hashCode => Object.hashAll([users]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['users'] = users.map((csilE) => csilE.toCborValue()).toList();
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DevUsersResponse.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DevUsersResponse(
      users: (map['users'] as List).map((csilE) => DevUserEntry.fromCborValue(csilE)).cast<DevUserEntry>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DevUsersResponse.fromCbor(List<int> bytes) =>
      DevUsersResponse.fromCborValue(CsilCbor.decode(bytes));
}

final class DevLoginRequest {
  final MemberId memberId;

  const DevLoginRequest({
    required this.memberId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    return map;
  }

  factory DevLoginRequest.fromMap(Map<String, Object?> map) {
    return DevLoginRequest(
      memberId: map['member_id'] as MemberId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DevLoginRequest) return false;
    return memberId == other.memberId;
  }

  @override
  int get hashCode => Object.hashAll([memberId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DevLoginRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DevLoginRequest(
      memberId: map['member_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DevLoginRequest.fromCbor(List<int> bytes) =>
      DevLoginRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class MeResponse {
  final String domain;
  final String userId;
  final String? displayName;
  final Timestamp expiresAt;
  final List<HouseSummary> houses;

  const MeResponse({
    required this.domain,
    required this.userId,
    this.displayName,
    required this.expiresAt,
    required this.houses,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName;
    map['expires_at'] = expiresAt;
    map['houses'] = houses;
    return map;
  }

  factory MeResponse.fromMap(Map<String, Object?> map) {
    return MeResponse(
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] as String?,
      expiresAt: map['expires_at'] as Timestamp,
      houses: map['houses'] as List<HouseSummary>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MeResponse) return false;
    return domain == other.domain &&
        userId == other.userId &&
        displayName == other.displayName &&
        expiresAt == other.expiresAt &&
        houses == other.houses;
  }

  @override
  int get hashCode => Object.hashAll([domain, userId, displayName, expiresAt, houses]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['domain'] = domain;
    map['user_id'] = userId;
    if (displayName != null) map['display_name'] = displayName!;
    map['expires_at'] = expiresAt;
    map['houses'] = houses.map((csilE) => csilE.toCborValue()).toList();
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MeResponse.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MeResponse(
      domain: map['domain'] as String,
      userId: map['user_id'] as String,
      displayName: map['display_name'] == null ? null : map['display_name'] as String,
      expiresAt: map['expires_at'] as String,
      houses: (map['houses'] as List).map((csilE) => HouseSummary.fromCborValue(csilE)).cast<HouseSummary>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MeResponse.fromCbor(List<int> bytes) =>
      MeResponse.fromCborValue(CsilCbor.decode(bytes));
}

final class EmptyRequest {
  // (no named fields)

  const EmptyRequest();

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    return map;
  }

  factory EmptyRequest.fromMap(Map<String, Object?> map) {
    return EmptyRequest(
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! EmptyRequest) return false;
    return true;
  }

  @override
  int get hashCode => Object.hashAll([]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory EmptyRequest.fromCborValue(Object? cbor) {
    return EmptyRequest(
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory EmptyRequest.fromCbor(List<int> bytes) =>
      EmptyRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class EmptyResponse {
  // (no named fields)

  const EmptyResponse();

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    return map;
  }

  factory EmptyResponse.fromMap(Map<String, Object?> map) {
    return EmptyResponse(
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! EmptyResponse) return false;
    return true;
  }

  @override
  int get hashCode => Object.hashAll([]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory EmptyResponse.fromCborValue(Object? cbor) {
    return EmptyResponse(
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory EmptyResponse.fromCbor(List<int> bytes) =>
      EmptyResponse.fromCborValue(CsilCbor.decode(bytes));
}

final class BoolResponse {
  final bool value;

  const BoolResponse({
    required this.value,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['value'] = value;
    return map;
  }

  factory BoolResponse.fromMap(Map<String, Object?> map) {
    return BoolResponse(
      value: map['value'] as bool,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! BoolResponse) return false;
    return value == other.value;
  }

  @override
  int get hashCode => Object.hashAll([value]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['value'] = value;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory BoolResponse.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return BoolResponse(
      value: map['value'] as bool,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory BoolResponse.fromCbor(List<int> bytes) =>
      BoolResponse.fromCborValue(CsilCbor.decode(bytes));
}

final class HouseListRequest {
  final int? limit;
  final int? offset;

  const HouseListRequest({
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory HouseListRequest.fromMap(Map<String, Object?> map) {
    return HouseListRequest(
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! HouseListRequest) return false;
    return limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory HouseListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return HouseListRequest(
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory HouseListRequest.fromCbor(List<int> bytes) =>
      HouseListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class HouseScopedListRequest {
  final HouseId houseId;
  final int? limit;
  final int? offset;

  const HouseScopedListRequest({
    required this.houseId,
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory HouseScopedListRequest.fromMap(Map<String, Object?> map) {
    return HouseScopedListRequest(
      houseId: map['house_id'] as HouseId,
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! HouseScopedListRequest) return false;
    return houseId == other.houseId &&
        limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([houseId, limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory HouseScopedListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return HouseScopedListRequest(
      houseId: map['house_id'] as String,
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory HouseScopedListRequest.fromCbor(List<int> bytes) =>
      HouseScopedListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class TaskList {
  final List<Task> tasks;
  final int hiddenCount;

  const TaskList({
    required this.tasks,
    required this.hiddenCount,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['tasks'] = tasks;
    map['hidden_count'] = hiddenCount;
    return map;
  }

  factory TaskList.fromMap(Map<String, Object?> map) {
    return TaskList(
      tasks: map['tasks'] as List<Task>,
      hiddenCount: map['hidden_count'] as int,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! TaskList) return false;
    return tasks == other.tasks &&
        hiddenCount == other.hiddenCount;
  }

  @override
  int get hashCode => Object.hashAll([tasks, hiddenCount]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['tasks'] = tasks.map((csilE) => csilE.toCborValue()).toList();
    map['hidden_count'] = hiddenCount;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory TaskList.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return TaskList(
      tasks: (map['tasks'] as List).map((csilE) => Task.fromCborValue(csilE)).cast<Task>().toList(),
      hiddenCount: map['hidden_count'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory TaskList.fromCbor(List<int> bytes) =>
      TaskList.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectList {
  final List<Project> projects;
  final int hiddenCount;

  const ProjectList({
    required this.projects,
    required this.hiddenCount,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['projects'] = projects;
    map['hidden_count'] = hiddenCount;
    return map;
  }

  factory ProjectList.fromMap(Map<String, Object?> map) {
    return ProjectList(
      projects: map['projects'] as List<Project>,
      hiddenCount: map['hidden_count'] as int,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectList) return false;
    return projects == other.projects &&
        hiddenCount == other.hiddenCount;
  }

  @override
  int get hashCode => Object.hashAll([projects, hiddenCount]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['projects'] = projects.map((csilE) => csilE.toCborValue()).toList();
    map['hidden_count'] = hiddenCount;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectList.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectList(
      projects: (map['projects'] as List).map((csilE) => Project.fromCborValue(csilE)).cast<Project>().toList(),
      hiddenCount: map['hidden_count'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectList.fromCbor(List<int> bytes) =>
      ProjectList.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberScopedListRequest {
  final HouseId houseId;
  final MemberId memberId;
  final int? limit;
  final int? offset;

  const MemberScopedListRequest({
    required this.houseId,
    required this.memberId,
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['member_id'] = memberId;
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory MemberScopedListRequest.fromMap(Map<String, Object?> map) {
    return MemberScopedListRequest(
      houseId: map['house_id'] as HouseId,
      memberId: map['member_id'] as MemberId,
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberScopedListRequest) return false;
    return houseId == other.houseId &&
        memberId == other.memberId &&
        limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([houseId, memberId, limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['member_id'] = memberId;
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberScopedListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberScopedListRequest(
      houseId: map['house_id'] as String,
      memberId: map['member_id'] as String,
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberScopedListRequest.fromCbor(List<int> bytes) =>
      MemberScopedListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectScopedListRequest {
  final HouseId houseId;
  final ProjectId projectId;
  final int? limit;
  final int? offset;

  const ProjectScopedListRequest({
    required this.houseId,
    required this.projectId,
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['project_id'] = projectId;
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory ProjectScopedListRequest.fromMap(Map<String, Object?> map) {
    return ProjectScopedListRequest(
      houseId: map['house_id'] as HouseId,
      projectId: map['project_id'] as ProjectId,
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectScopedListRequest) return false;
    return houseId == other.houseId &&
        projectId == other.projectId &&
        limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([houseId, projectId, limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['project_id'] = projectId;
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectScopedListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectScopedListRequest(
      houseId: map['house_id'] as String,
      projectId: map['project_id'] as String,
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectScopedListRequest.fromCbor(List<int> bytes) =>
      ProjectScopedListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class CommentListRequest {
  final TargetType targetType;
  final String targetId;
  final int? limit;
  final int? offset;

  const CommentListRequest({
    required this.targetType,
    required this.targetId,
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['target_type'] = targetType;
    map['target_id'] = targetId;
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory CommentListRequest.fromMap(Map<String, Object?> map) {
    return CommentListRequest(
      targetType: map['target_type'] as TargetType,
      targetId: map['target_id'] as String,
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! CommentListRequest) return false;
    return targetType == other.targetType &&
        targetId == other.targetId &&
        limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([targetType, targetId, limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['target_type'] = targetType;
    map['target_id'] = targetId;
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory CommentListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return CommentListRequest(
      targetType: map['target_type'],
      targetId: map['target_id'] as String,
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory CommentListRequest.fromCbor(List<int> bytes) =>
      CommentListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class Notification {
  final NotificationId notificationId;
  final HouseId houseId;
  final MemberId memberId;
  final String kind;
  final MemberId? actorMemberId;
  final String actorName;
  final String? targetType;
  final String? targetId;
  final String targetTitle;
  final String body;
  final bool read;
  final Timestamp? readAt;
  final Timestamp createdAt;

  const Notification({
    required this.notificationId,
    required this.houseId,
    required this.memberId,
    required this.kind,
    this.actorMemberId,
    required this.actorName,
    this.targetType,
    this.targetId,
    required this.targetTitle,
    required this.body,
    required this.read,
    this.readAt,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['notification_id'] = notificationId;
    map['house_id'] = houseId;
    map['member_id'] = memberId;
    map['kind'] = kind;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId;
    map['actor_name'] = actorName;
    if (targetType != null) map['target_type'] = targetType;
    if (targetId != null) map['target_id'] = targetId;
    map['target_title'] = targetTitle;
    map['body'] = body;
    map['read'] = read;
    if (readAt != null) map['read_at'] = readAt;
    map['created_at'] = createdAt;
    return map;
  }

  factory Notification.fromMap(Map<String, Object?> map) {
    return Notification(
      notificationId: map['notification_id'] as NotificationId,
      houseId: map['house_id'] as HouseId,
      memberId: map['member_id'] as MemberId,
      kind: map['kind'] as String,
      actorMemberId: map['actor_member_id'] as MemberId?,
      actorName: map['actor_name'] as String,
      targetType: map['target_type'] as String?,
      targetId: map['target_id'] as String?,
      targetTitle: map['target_title'] as String,
      body: map['body'] as String,
      read: map['read'] as bool,
      readAt: map['read_at'] as Timestamp?,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! Notification) return false;
    return notificationId == other.notificationId &&
        houseId == other.houseId &&
        memberId == other.memberId &&
        kind == other.kind &&
        actorMemberId == other.actorMemberId &&
        actorName == other.actorName &&
        targetType == other.targetType &&
        targetId == other.targetId &&
        targetTitle == other.targetTitle &&
        body == other.body &&
        read == other.read &&
        readAt == other.readAt &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([notificationId, houseId, memberId, kind, actorMemberId, actorName, targetType, targetId, targetTitle, body, read, readAt, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['notification_id'] = notificationId;
    map['house_id'] = houseId;
    map['member_id'] = memberId;
    map['kind'] = kind;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId!;
    map['actor_name'] = actorName;
    if (targetType != null) map['target_type'] = targetType!;
    if (targetId != null) map['target_id'] = targetId!;
    map['target_title'] = targetTitle;
    map['body'] = body;
    map['read'] = read;
    if (readAt != null) map['read_at'] = readAt!;
    map['created_at'] = createdAt;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Notification.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Notification(
      notificationId: map['notification_id'] as String,
      houseId: map['house_id'] as String,
      memberId: map['member_id'] as String,
      kind: map['kind'] as String,
      actorMemberId: map['actor_member_id'] == null ? null : map['actor_member_id'] as String,
      actorName: map['actor_name'] as String,
      targetType: map['target_type'] == null ? null : map['target_type'] as String,
      targetId: map['target_id'] == null ? null : map['target_id'] as String,
      targetTitle: map['target_title'] as String,
      body: map['body'] as String,
      read: map['read'] as bool,
      readAt: map['read_at'] == null ? null : map['read_at'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Notification.fromCbor(List<int> bytes) =>
      Notification.fromCborValue(CsilCbor.decode(bytes));
}

final class NotificationListRequest {
  final HouseId houseId;
  final bool? unreadOnly;
  final int? limit;
  final int? offset;

  const NotificationListRequest({
    required this.houseId,
    this.unreadOnly,
    this.limit,
    this.offset,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (unreadOnly != null) map['unread_only'] = unreadOnly;
    if (limit != null) map['limit'] = limit;
    if (offset != null) map['offset'] = offset;
    return map;
  }

  factory NotificationListRequest.fromMap(Map<String, Object?> map) {
    return NotificationListRequest(
      houseId: map['house_id'] as HouseId,
      unreadOnly: map['unread_only'] as bool?,
      limit: map['limit'] as int?,
      offset: map['offset'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! NotificationListRequest) return false;
    return houseId == other.houseId &&
        unreadOnly == other.unreadOnly &&
        limit == other.limit &&
        offset == other.offset;
  }

  @override
  int get hashCode => Object.hashAll([houseId, unreadOnly, limit, offset]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (unreadOnly != null) map['unread_only'] = unreadOnly!;
    if (limit != null) map['limit'] = limit!;
    if (offset != null) map['offset'] = offset!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory NotificationListRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return NotificationListRequest(
      houseId: map['house_id'] as String,
      unreadOnly: map['unread_only'] == null ? null : map['unread_only'] as bool,
      limit: map['limit'] == null ? null : map['limit'] as int,
      offset: map['offset'] == null ? null : map['offset'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory NotificationListRequest.fromCbor(List<int> bytes) =>
      NotificationListRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class NotificationUnreadCount {
  final int count;

  const NotificationUnreadCount({
    required this.count,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['count'] = count;
    return map;
  }

  factory NotificationUnreadCount.fromMap(Map<String, Object?> map) {
    return NotificationUnreadCount(
      count: map['count'] as int,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! NotificationUnreadCount) return false;
    return count == other.count;
  }

  @override
  int get hashCode => Object.hashAll([count]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['count'] = count;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory NotificationUnreadCount.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return NotificationUnreadCount(
      count: map['count'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory NotificationUnreadCount.fromCbor(List<int> bytes) =>
      NotificationUnreadCount.fromCborValue(CsilCbor.decode(bytes));
}

final class ShareAccessRequest {
  final String linkkeysDomain;
  final String linkkeysUserId;
  final ResourceType resourceType;
  final String resourceId;

  const ShareAccessRequest({
    required this.linkkeysDomain,
    required this.linkkeysUserId,
    required this.resourceType,
    required this.resourceId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  factory ShareAccessRequest.fromMap(Map<String, Object?> map) {
    return ShareAccessRequest(
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      resourceType: map['resource_type'] as ResourceType,
      resourceId: map['resource_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ShareAccessRequest) return false;
    return linkkeysDomain == other.linkkeysDomain &&
        linkkeysUserId == other.linkkeysUserId &&
        resourceType == other.resourceType &&
        resourceId == other.resourceId;
  }

  @override
  int get hashCode => Object.hashAll([linkkeysDomain, linkkeysUserId, resourceType, resourceId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['linkkeys_domain'] = linkkeysDomain;
    map['linkkeys_user_id'] = linkkeysUserId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ShareAccessRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ShareAccessRequest(
      linkkeysDomain: map['linkkeys_domain'] as String,
      linkkeysUserId: map['linkkeys_user_id'] as String,
      resourceType: map['resource_type'],
      resourceId: map['resource_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ShareAccessRequest.fromCbor(List<int> bytes) =>
      ShareAccessRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class ResourceRef {
  final ResourceType resourceType;
  final String resourceId;

  const ResourceRef({
    required this.resourceType,
    required this.resourceId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  factory ResourceRef.fromMap(Map<String, Object?> map) {
    return ResourceRef(
      resourceType: map['resource_type'] as ResourceType,
      resourceId: map['resource_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ResourceRef) return false;
    return resourceType == other.resourceType &&
        resourceId == other.resourceId;
  }

  @override
  int get hashCode => Object.hashAll([resourceType, resourceId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ResourceRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ResourceRef(
      resourceType: map['resource_type'],
      resourceId: map['resource_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ResourceRef.fromCbor(List<int> bytes) =>
      ResourceRef.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberRoleRef {
  final MemberId memberId;
  final RoleId roleId;

  const MemberRoleRef({
    required this.memberId,
    required this.roleId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['role_id'] = roleId;
    return map;
  }

  factory MemberRoleRef.fromMap(Map<String, Object?> map) {
    return MemberRoleRef(
      memberId: map['member_id'] as MemberId,
      roleId: map['role_id'] as RoleId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberRoleRef) return false;
    return memberId == other.memberId &&
        roleId == other.roleId;
  }

  @override
  int get hashCode => Object.hashAll([memberId, roleId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['role_id'] = roleId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberRoleRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberRoleRef(
      memberId: map['member_id'] as String,
      roleId: map['role_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberRoleRef.fromCbor(List<int> bytes) =>
      MemberRoleRef.fromCborValue(CsilCbor.decode(bytes));
}

final class MemberSkillRef {
  final MemberId memberId;
  final SkillId skillId;

  const MemberSkillRef({
    required this.memberId,
    required this.skillId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['skill_id'] = skillId;
    return map;
  }

  factory MemberSkillRef.fromMap(Map<String, Object?> map) {
    return MemberSkillRef(
      memberId: map['member_id'] as MemberId,
      skillId: map['skill_id'] as SkillId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! MemberSkillRef) return false;
    return memberId == other.memberId &&
        skillId == other.skillId;
  }

  @override
  int get hashCode => Object.hashAll([memberId, skillId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['member_id'] = memberId;
    map['skill_id'] = skillId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory MemberSkillRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return MemberSkillRef(
      memberId: map['member_id'] as String,
      skillId: map['skill_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory MemberSkillRef.fromCbor(List<int> bytes) =>
      MemberSkillRef.fromCborValue(CsilCbor.decode(bytes));
}

final class GroupSkillRef {
  final GroupId groupId;
  final SkillId skillId;

  const GroupSkillRef({
    required this.groupId,
    required this.skillId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['skill_id'] = skillId;
    return map;
  }

  factory GroupSkillRef.fromMap(Map<String, Object?> map) {
    return GroupSkillRef(
      groupId: map['group_id'] as GroupId,
      skillId: map['skill_id'] as SkillId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! GroupSkillRef) return false;
    return groupId == other.groupId &&
        skillId == other.skillId;
  }

  @override
  int get hashCode => Object.hashAll([groupId, skillId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['skill_id'] = skillId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory GroupSkillRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return GroupSkillRef(
      groupId: map['group_id'] as String,
      skillId: map['skill_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory GroupSkillRef.fromCbor(List<int> bytes) =>
      GroupSkillRef.fromCborValue(CsilCbor.decode(bytes));
}

final class GroupMemberRef {
  final GroupId groupId;
  final MemberId memberId;

  const GroupMemberRef({
    required this.groupId,
    required this.memberId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['member_id'] = memberId;
    return map;
  }

  factory GroupMemberRef.fromMap(Map<String, Object?> map) {
    return GroupMemberRef(
      groupId: map['group_id'] as GroupId,
      memberId: map['member_id'] as MemberId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! GroupMemberRef) return false;
    return groupId == other.groupId &&
        memberId == other.memberId;
  }

  @override
  int get hashCode => Object.hashAll([groupId, memberId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['group_id'] = groupId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory GroupMemberRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return GroupMemberRef(
      groupId: map['group_id'] as String,
      memberId: map['member_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory GroupMemberRef.fromCbor(List<int> bytes) =>
      GroupMemberRef.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectTaskRef {
  final ProjectId projectId;
  final TaskId taskId;

  const ProjectTaskRef({
    required this.projectId,
    required this.taskId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    return map;
  }

  factory ProjectTaskRef.fromMap(Map<String, Object?> map) {
    return ProjectTaskRef(
      projectId: map['project_id'] as ProjectId,
      taskId: map['task_id'] as TaskId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectTaskRef) return false;
    return projectId == other.projectId &&
        taskId == other.taskId;
  }

  @override
  int get hashCode => Object.hashAll([projectId, taskId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectTaskRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectTaskRef(
      projectId: map['project_id'] as String,
      taskId: map['task_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectTaskRef.fromCbor(List<int> bytes) =>
      ProjectTaskRef.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectTaskOrderRequest {
  final ProjectId projectId;
  final TaskId taskId;
  final int position;

  const ProjectTaskOrderRequest({
    required this.projectId,
    required this.taskId,
    required this.position,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    map['position'] = position;
    return map;
  }

  factory ProjectTaskOrderRequest.fromMap(Map<String, Object?> map) {
    return ProjectTaskOrderRequest(
      projectId: map['project_id'] as ProjectId,
      taskId: map['task_id'] as TaskId,
      position: map['position'] as int,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectTaskOrderRequest) return false;
    return projectId == other.projectId &&
        taskId == other.taskId &&
        position == other.position;
  }

  @override
  int get hashCode => Object.hashAll([projectId, taskId, position]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['task_id'] = taskId;
    map['position'] = position;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectTaskOrderRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectTaskOrderRequest(
      projectId: map['project_id'] as String,
      taskId: map['task_id'] as String,
      position: map['position'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectTaskOrderRequest.fromCbor(List<int> bytes) =>
      ProjectTaskOrderRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectMemberRef {
  final ProjectId projectId;
  final MemberId memberId;

  const ProjectMemberRef({
    required this.projectId,
    required this.memberId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  factory ProjectMemberRef.fromMap(Map<String, Object?> map) {
    return ProjectMemberRef(
      projectId: map['project_id'] as ProjectId,
      memberId: map['member_id'] as MemberId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectMemberRef) return false;
    return projectId == other.projectId &&
        memberId == other.memberId;
  }

  @override
  int get hashCode => Object.hashAll([projectId, memberId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectMemberRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectMemberRef(
      projectId: map['project_id'] as String,
      memberId: map['member_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectMemberRef.fromCbor(List<int> bytes) =>
      ProjectMemberRef.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectOwnerRef {
  final ProjectId projectId;
  final MemberId memberId;

  const ProjectOwnerRef({
    required this.projectId,
    required this.memberId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  factory ProjectOwnerRef.fromMap(Map<String, Object?> map) {
    return ProjectOwnerRef(
      projectId: map['project_id'] as ProjectId,
      memberId: map['member_id'] as MemberId,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectOwnerRef) return false;
    return projectId == other.projectId &&
        memberId == other.memberId;
  }

  @override
  int get hashCode => Object.hashAll([projectId, memberId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['member_id'] = memberId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectOwnerRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectOwnerRef(
      projectId: map['project_id'] as String,
      memberId: map['member_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectOwnerRef.fromCbor(List<int> bytes) =>
      ProjectOwnerRef.fromCborValue(CsilCbor.decode(bytes));
}

final class DependencyRef {
  final DependencyNodeType dependentType;
  final String dependentId;
  final DependencyNodeType dependencyType;
  final String dependencyId;

  const DependencyRef({
    required this.dependentType,
    required this.dependentId,
    required this.dependencyType,
    required this.dependencyId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['dependent_type'] = dependentType;
    map['dependent_id'] = dependentId;
    map['dependency_type'] = dependencyType;
    map['dependency_id'] = dependencyId;
    return map;
  }

  factory DependencyRef.fromMap(Map<String, Object?> map) {
    return DependencyRef(
      dependentType: map['dependent_type'] as DependencyNodeType,
      dependentId: map['dependent_id'] as String,
      dependencyType: map['dependency_type'] as DependencyNodeType,
      dependencyId: map['dependency_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DependencyRef) return false;
    return dependentType == other.dependentType &&
        dependentId == other.dependentId &&
        dependencyType == other.dependencyType &&
        dependencyId == other.dependencyId;
  }

  @override
  int get hashCode => Object.hashAll([dependentType, dependentId, dependencyType, dependencyId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['dependent_type'] = dependentType;
    map['dependent_id'] = dependentId;
    map['dependency_type'] = dependencyType;
    map['dependency_id'] = dependencyId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DependencyRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DependencyRef(
      dependentType: map['dependent_type'],
      dependentId: map['dependent_id'] as String,
      dependencyType: map['dependency_type'],
      dependencyId: map['dependency_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DependencyRef.fromCbor(List<int> bytes) =>
      DependencyRef.fromCborValue(CsilCbor.decode(bytes));
}

final class DependencyTarget {
  final DependencyNodeType type;
  final String id;

  const DependencyTarget({
    required this.type,
    required this.id,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['type'] = type;
    map['id'] = id;
    return map;
  }

  factory DependencyTarget.fromMap(Map<String, Object?> map) {
    return DependencyTarget(
      type: map['type'] as DependencyNodeType,
      id: map['id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DependencyTarget) return false;
    return type == other.type &&
        id == other.id;
  }

  @override
  int get hashCode => Object.hashAll([type, id]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['type'] = type;
    map['id'] = id;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DependencyTarget.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DependencyTarget(
      type: map['type'],
      id: map['id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DependencyTarget.fromCbor(List<int> bytes) =>
      DependencyTarget.fromCborValue(CsilCbor.decode(bytes));
}

final class DependencyNode {
  final DependencyNodeType type;
  final String id;
  final String title;
  final String? status;

  const DependencyNode({
    required this.type,
    required this.id,
    required this.title,
    this.status,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['type'] = type;
    map['id'] = id;
    return map;
  }

  factory DependencyNode.fromMap(Map<String, Object?> map) {
    return DependencyNode(
      type: map['type'] as DependencyNodeType,
      id: map['id'] as String,
      title: map['title'] as String,
      status: map['status'] as String?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DependencyNode) return false;
    return type == other.type &&
        id == other.id &&
        title == other.title &&
        status == other.status;
  }

  @override
  int get hashCode => Object.hashAll([type, id, title, status]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['type'] = type;
    map['id'] = id;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DependencyNode.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DependencyNode(
      type: map['type'],
      id: map['id'] as String,
      title: map['title'] as String,
      status: map['status'] == null ? null : map['status'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DependencyNode.fromCbor(List<int> bytes) =>
      DependencyNode.fromCborValue(CsilCbor.decode(bytes));
}

final class DependencyGraph {
  final List<DependencyNode> dependencies;
  final List<DependencyNode> dependents;

  const DependencyGraph({
    required this.dependencies,
    required this.dependents,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['dependencies'] = dependencies;
    map['dependents'] = dependents;
    return map;
  }

  factory DependencyGraph.fromMap(Map<String, Object?> map) {
    return DependencyGraph(
      dependencies: map['dependencies'] as List<DependencyNode>,
      dependents: map['dependents'] as List<DependencyNode>,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! DependencyGraph) return false;
    return dependencies == other.dependencies &&
        dependents == other.dependents;
  }

  @override
  int get hashCode => Object.hashAll([dependencies, dependents]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['dependencies'] = dependencies.map((csilE) => csilE.toCborValue()).toList();
    map['dependents'] = dependents.map((csilE) => csilE.toCborValue()).toList();
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory DependencyGraph.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return DependencyGraph(
      dependencies: (map['dependencies'] as List).map((csilE) => DependencyNode.fromCborValue(csilE)).cast<DependencyNode>().toList(),
      dependents: (map['dependents'] as List).map((csilE) => DependencyNode.fromCborValue(csilE)).cast<DependencyNode>().toList(),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory DependencyGraph.fromCbor(List<int> bytes) =>
      DependencyGraph.fromCborValue(CsilCbor.decode(bytes));
}

final class Grant {
  final GranteeType granteeType;
  final String granteeId;
  final AccessLevel accessLevel;

  const Grant({
    required this.granteeType,
    required this.granteeId,
    required this.accessLevel,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  factory Grant.fromMap(Map<String, Object?> map) {
    return Grant(
      granteeType: map['grantee_type'] as GranteeType,
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'] as AccessLevel,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! Grant) return false;
    return granteeType == other.granteeType &&
        granteeId == other.granteeId &&
        accessLevel == other.accessLevel;
  }

  @override
  int get hashCode => Object.hashAll([granteeType, granteeId, accessLevel]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory Grant.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return Grant(
      granteeType: map['grantee_type'],
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory Grant.fromCbor(List<int> bytes) =>
      Grant.fromCborValue(CsilCbor.decode(bytes));
}

final class TaskGrantRef {
  final TaskId taskId;
  final GranteeType granteeType;
  final String granteeId;

  const TaskGrantRef({
    required this.taskId,
    required this.granteeType,
    required this.granteeId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    return map;
  }

  factory TaskGrantRef.fromMap(Map<String, Object?> map) {
    return TaskGrantRef(
      taskId: map['task_id'] as TaskId,
      granteeType: map['grantee_type'] as GranteeType,
      granteeId: map['grantee_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! TaskGrantRef) return false;
    return taskId == other.taskId &&
        granteeType == other.granteeType &&
        granteeId == other.granteeId;
  }

  @override
  int get hashCode => Object.hashAll([taskId, granteeType, granteeId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory TaskGrantRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return TaskGrantRef(
      taskId: map['task_id'] as String,
      granteeType: map['grantee_type'],
      granteeId: map['grantee_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory TaskGrantRef.fromCbor(List<int> bytes) =>
      TaskGrantRef.fromCborValue(CsilCbor.decode(bytes));
}

final class PutTaskGrantRequest {
  final TaskId taskId;
  final GranteeType granteeType;
  final String granteeId;
  final AccessLevel accessLevel;

  const PutTaskGrantRequest({
    required this.taskId,
    required this.granteeType,
    required this.granteeId,
    required this.accessLevel,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  factory PutTaskGrantRequest.fromMap(Map<String, Object?> map) {
    return PutTaskGrantRequest(
      taskId: map['task_id'] as TaskId,
      granteeType: map['grantee_type'] as GranteeType,
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'] as AccessLevel,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! PutTaskGrantRequest) return false;
    return taskId == other.taskId &&
        granteeType == other.granteeType &&
        granteeId == other.granteeId &&
        accessLevel == other.accessLevel;
  }

  @override
  int get hashCode => Object.hashAll([taskId, granteeType, granteeId, accessLevel]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory PutTaskGrantRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return PutTaskGrantRequest(
      taskId: map['task_id'] as String,
      granteeType: map['grantee_type'],
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory PutTaskGrantRequest.fromCbor(List<int> bytes) =>
      PutTaskGrantRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class SetTaskVisibilityRequest {
  final TaskId taskId;
  final AccessLevel visibility;

  const SetTaskVisibilityRequest({
    required this.taskId,
    required this.visibility,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['visibility'] = visibility;
    return map;
  }

  factory SetTaskVisibilityRequest.fromMap(Map<String, Object?> map) {
    return SetTaskVisibilityRequest(
      taskId: map['task_id'] as TaskId,
      visibility: map['visibility'] as AccessLevel,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! SetTaskVisibilityRequest) return false;
    return taskId == other.taskId &&
        visibility == other.visibility;
  }

  @override
  int get hashCode => Object.hashAll([taskId, visibility]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['task_id'] = taskId;
    map['visibility'] = visibility;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory SetTaskVisibilityRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return SetTaskVisibilityRequest(
      taskId: map['task_id'] as String,
      visibility: map['visibility'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory SetTaskVisibilityRequest.fromCbor(List<int> bytes) =>
      SetTaskVisibilityRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class ProjectGrantRef {
  final ProjectId projectId;
  final GranteeType granteeType;
  final String granteeId;

  const ProjectGrantRef({
    required this.projectId,
    required this.granteeType,
    required this.granteeId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    return map;
  }

  factory ProjectGrantRef.fromMap(Map<String, Object?> map) {
    return ProjectGrantRef(
      projectId: map['project_id'] as ProjectId,
      granteeType: map['grantee_type'] as GranteeType,
      granteeId: map['grantee_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ProjectGrantRef) return false;
    return projectId == other.projectId &&
        granteeType == other.granteeType &&
        granteeId == other.granteeId;
  }

  @override
  int get hashCode => Object.hashAll([projectId, granteeType, granteeId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ProjectGrantRef.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ProjectGrantRef(
      projectId: map['project_id'] as String,
      granteeType: map['grantee_type'],
      granteeId: map['grantee_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ProjectGrantRef.fromCbor(List<int> bytes) =>
      ProjectGrantRef.fromCborValue(CsilCbor.decode(bytes));
}

final class PutProjectGrantRequest {
  final ProjectId projectId;
  final GranteeType granteeType;
  final String granteeId;
  final AccessLevel accessLevel;

  const PutProjectGrantRequest({
    required this.projectId,
    required this.granteeType,
    required this.granteeId,
    required this.accessLevel,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  factory PutProjectGrantRequest.fromMap(Map<String, Object?> map) {
    return PutProjectGrantRequest(
      projectId: map['project_id'] as ProjectId,
      granteeType: map['grantee_type'] as GranteeType,
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'] as AccessLevel,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! PutProjectGrantRequest) return false;
    return projectId == other.projectId &&
        granteeType == other.granteeType &&
        granteeId == other.granteeId &&
        accessLevel == other.accessLevel;
  }

  @override
  int get hashCode => Object.hashAll([projectId, granteeType, granteeId, accessLevel]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['grantee_type'] = granteeType;
    map['grantee_id'] = granteeId;
    map['access_level'] = accessLevel;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory PutProjectGrantRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return PutProjectGrantRequest(
      projectId: map['project_id'] as String,
      granteeType: map['grantee_type'],
      granteeId: map['grantee_id'] as String,
      accessLevel: map['access_level'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory PutProjectGrantRequest.fromCbor(List<int> bytes) =>
      PutProjectGrantRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class SetProjectVisibilityRequest {
  final ProjectId projectId;
  final AccessLevel visibility;

  const SetProjectVisibilityRequest({
    required this.projectId,
    required this.visibility,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['visibility'] = visibility;
    return map;
  }

  factory SetProjectVisibilityRequest.fromMap(Map<String, Object?> map) {
    return SetProjectVisibilityRequest(
      projectId: map['project_id'] as ProjectId,
      visibility: map['visibility'] as AccessLevel,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! SetProjectVisibilityRequest) return false;
    return projectId == other.projectId &&
        visibility == other.visibility;
  }

  @override
  int get hashCode => Object.hashAll([projectId, visibility]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['project_id'] = projectId;
    map['visibility'] = visibility;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory SetProjectVisibilityRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return SetProjectVisibilityRequest(
      projectId: map['project_id'] as String,
      visibility: map['visibility'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory SetProjectVisibilityRequest.fromCbor(List<int> bytes) =>
      SetProjectVisibilityRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class EffectiveSettings {
  final bool? bugReportsEnabled;
  final ProjectId? bugReportsProjectId;
  final AccessLevel? defaultProjectVisibility;

  const EffectiveSettings({
    this.bugReportsEnabled,
    this.bugReportsProjectId,
    this.defaultProjectVisibility,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    if (bugReportsEnabled != null) map['bug_reports_enabled'] = bugReportsEnabled;
    if (bugReportsProjectId != null) map['bug_reports_project_id'] = bugReportsProjectId;
    if (defaultProjectVisibility != null) map['default_project_visibility'] = defaultProjectVisibility;
    return map;
  }

  factory EffectiveSettings.fromMap(Map<String, Object?> map) {
    return EffectiveSettings(
      bugReportsEnabled: map['bug_reports_enabled'] as bool?,
      bugReportsProjectId: map['bug_reports_project_id'] as ProjectId?,
      defaultProjectVisibility: map['default_project_visibility'] as AccessLevel?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! EffectiveSettings) return false;
    return bugReportsEnabled == other.bugReportsEnabled &&
        bugReportsProjectId == other.bugReportsProjectId &&
        defaultProjectVisibility == other.defaultProjectVisibility;
  }

  @override
  int get hashCode => Object.hashAll([bugReportsEnabled, bugReportsProjectId, defaultProjectVisibility]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    if (bugReportsEnabled != null) map['bug_reports_enabled'] = bugReportsEnabled!;
    if (bugReportsProjectId != null) map['bug_reports_project_id'] = bugReportsProjectId!;
    if (defaultProjectVisibility != null) map['default_project_visibility'] = defaultProjectVisibility!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory EffectiveSettings.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return EffectiveSettings(
      bugReportsEnabled: map['bug_reports_enabled'] == null ? null : map['bug_reports_enabled'] as bool,
      bugReportsProjectId: map['bug_reports_project_id'] == null ? null : map['bug_reports_project_id'] as String,
      defaultProjectVisibility: map['default_project_visibility'] == null ? null : map['default_project_visibility'],
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory EffectiveSettings.fromCbor(List<int> bytes) =>
      EffectiveSettings.fromCborValue(CsilCbor.decode(bytes));
}

final class UpdateSettingsRequest {
  final HouseId houseId;
  final EffectiveSettings settings;

  const UpdateSettingsRequest({
    required this.houseId,
    required this.settings,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['settings'] = settings;
    return map;
  }

  factory UpdateSettingsRequest.fromMap(Map<String, Object?> map) {
    return UpdateSettingsRequest(
      houseId: map['house_id'] as HouseId,
      settings: map['settings'] as EffectiveSettings,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! UpdateSettingsRequest) return false;
    return houseId == other.houseId &&
        settings == other.settings;
  }

  @override
  int get hashCode => Object.hashAll([houseId, settings]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['settings'] = settings.toCborValue();
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory UpdateSettingsRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return UpdateSettingsRequest(
      houseId: map['house_id'] as String,
      settings: EffectiveSettings.fromCborValue(map['settings']),
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory UpdateSettingsRequest.fromCbor(List<int> bytes) =>
      UpdateSettingsRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class BugReportRequest {
  final HouseId houseId;
  final String title;
  final String? description;

  const BugReportRequest({
    required this.houseId,
    required this.title,
    this.description,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['title'] = title;
    if (description != null) map['description'] = description;
    return map;
  }

  factory BugReportRequest.fromMap(Map<String, Object?> map) {
    return BugReportRequest(
      houseId: map['house_id'] as HouseId,
      title: map['title'] as String,
      description: map['description'] as String?,
    );
  }

  /// Throws [ArgumentError] when a field constraint is violated.
  void validate() {
    if (title.isEmpty) {
      throw ArgumentError('\'title\' must have length >= 1');
    }
    if (title.length > 512) {
      throw ArgumentError('\'title\' must have length <= 512');
    }
  }

  @override
  bool operator ==(Object other) {
    if (other is! BugReportRequest) return false;
    return houseId == other.houseId &&
        title == other.title &&
        description == other.description;
  }

  @override
  int get hashCode => Object.hashAll([houseId, title, description]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['title'] = title;
    if (description != null) map['description'] = description!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory BugReportRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return BugReportRequest(
      houseId: map['house_id'] as String,
      title: map['title'] as String,
      description: map['description'] == null ? null : map['description'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory BugReportRequest.fromCbor(List<int> bytes) =>
      BugReportRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class ServiceError {
  final int code;
  final String message;

  const ServiceError({
    required this.code,
    required this.message,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['code'] = code;
    map['message'] = message;
    return map;
  }

  factory ServiceError.fromMap(Map<String, Object?> map) {
    return ServiceError(
      code: map['code'] as int,
      message: map['message'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! ServiceError) return false;
    return code == other.code &&
        message == other.message;
  }

  @override
  int get hashCode => Object.hashAll([code, message]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['code'] = code;
    map['message'] = message;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory ServiceError.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return ServiceError(
      code: map['code'] as int,
      message: map['message'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory ServiceError.fromCbor(List<int> bytes) =>
      ServiceError.fromCborValue(CsilCbor.decode(bytes));
}

typedef AuditId = String;

final class AuditEntry {
  final AuditId auditId;
  final HouseId? houseId;
  final MemberId? actorMemberId;
  final String actorDomain;
  final String actorUserId;
  final String serviceName;
  final String method;
  final String action;
  final String? resourceType;
  final String? resourceId;
  final String outcome;
  final String? before;
  final String? after;
  final String? detail;
  final Timestamp createdAt;

  const AuditEntry({
    required this.auditId,
    this.houseId,
    this.actorMemberId,
    required this.actorDomain,
    required this.actorUserId,
    required this.serviceName,
    required this.method,
    required this.action,
    this.resourceType,
    this.resourceId,
    required this.outcome,
    this.before,
    this.after,
    this.detail,
    required this.createdAt,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    if (houseId != null) map['house_id'] = houseId;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId;
    map['actor_domain'] = actorDomain;
    map['actor_user_id'] = actorUserId;
    map['service_name'] = serviceName;
    map['method'] = method;
    map['action'] = action;
    if (resourceType != null) map['resource_type'] = resourceType;
    if (resourceId != null) map['resource_id'] = resourceId;
    map['outcome'] = outcome;
    if (before != null) map['before'] = before;
    if (after != null) map['after'] = after;
    if (detail != null) map['detail'] = detail;
    return map;
  }

  factory AuditEntry.fromMap(Map<String, Object?> map) {
    return AuditEntry(
      auditId: map['audit_id'] as AuditId,
      houseId: map['house_id'] as HouseId?,
      actorMemberId: map['actor_member_id'] as MemberId?,
      actorDomain: map['actor_domain'] as String,
      actorUserId: map['actor_user_id'] as String,
      serviceName: map['service_name'] as String,
      method: map['method'] as String,
      action: map['action'] as String,
      resourceType: map['resource_type'] as String?,
      resourceId: map['resource_id'] as String?,
      outcome: map['outcome'] as String,
      before: map['before'] as String?,
      after: map['after'] as String?,
      detail: map['detail'] as String?,
      createdAt: map['created_at'] as Timestamp,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! AuditEntry) return false;
    return auditId == other.auditId &&
        houseId == other.houseId &&
        actorMemberId == other.actorMemberId &&
        actorDomain == other.actorDomain &&
        actorUserId == other.actorUserId &&
        serviceName == other.serviceName &&
        method == other.method &&
        action == other.action &&
        resourceType == other.resourceType &&
        resourceId == other.resourceId &&
        outcome == other.outcome &&
        before == other.before &&
        after == other.after &&
        detail == other.detail &&
        createdAt == other.createdAt;
  }

  @override
  int get hashCode => Object.hashAll([auditId, houseId, actorMemberId, actorDomain, actorUserId, serviceName, method, action, resourceType, resourceId, outcome, before, after, detail, createdAt]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    if (houseId != null) map['house_id'] = houseId!;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId!;
    map['actor_domain'] = actorDomain;
    map['actor_user_id'] = actorUserId;
    map['service_name'] = serviceName;
    map['method'] = method;
    map['action'] = action;
    if (resourceType != null) map['resource_type'] = resourceType!;
    if (resourceId != null) map['resource_id'] = resourceId!;
    map['outcome'] = outcome;
    if (before != null) map['before'] = before!;
    if (after != null) map['after'] = after!;
    if (detail != null) map['detail'] = detail!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory AuditEntry.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return AuditEntry(
      auditId: map['audit_id'] as String,
      houseId: map['house_id'] == null ? null : map['house_id'] as String,
      actorMemberId: map['actor_member_id'] == null ? null : map['actor_member_id'] as String,
      actorDomain: map['actor_domain'] as String,
      actorUserId: map['actor_user_id'] as String,
      serviceName: map['service_name'] as String,
      method: map['method'] as String,
      action: map['action'] as String,
      resourceType: map['resource_type'] == null ? null : map['resource_type'] as String,
      resourceId: map['resource_id'] == null ? null : map['resource_id'] as String,
      outcome: map['outcome'] as String,
      before: map['before'] == null ? null : map['before'] as String,
      after: map['after'] == null ? null : map['after'] as String,
      detail: map['detail'] == null ? null : map['detail'] as String,
      createdAt: map['created_at'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory AuditEntry.fromCbor(List<int> bytes) =>
      AuditEntry.fromCborValue(CsilCbor.decode(bytes));
}

final class AuditQuery {
  final HouseId houseId;
  final MemberId? actorMemberId;
  final String? resourceType;
  final String? action;
  final Timestamp? since;
  final Timestamp? until;
  final String? cursor;
  final int? limit;

  const AuditQuery({
    required this.houseId,
    this.actorMemberId,
    this.resourceType,
    this.action,
    this.since,
    this.until,
    this.cursor,
    this.limit,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId;
    if (resourceType != null) map['resource_type'] = resourceType;
    if (action != null) map['action'] = action;
    if (since != null) map['since'] = since;
    if (until != null) map['until'] = until;
    if (cursor != null) map['cursor'] = cursor;
    if (limit != null) map['limit'] = limit;
    return map;
  }

  factory AuditQuery.fromMap(Map<String, Object?> map) {
    return AuditQuery(
      houseId: map['house_id'] as HouseId,
      actorMemberId: map['actor_member_id'] as MemberId?,
      resourceType: map['resource_type'] as String?,
      action: map['action'] as String?,
      since: map['since'] as Timestamp?,
      until: map['until'] as Timestamp?,
      cursor: map['cursor'] as String?,
      limit: map['limit'] as int?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! AuditQuery) return false;
    return houseId == other.houseId &&
        actorMemberId == other.actorMemberId &&
        resourceType == other.resourceType &&
        action == other.action &&
        since == other.since &&
        until == other.until &&
        cursor == other.cursor &&
        limit == other.limit;
  }

  @override
  int get hashCode => Object.hashAll([houseId, actorMemberId, resourceType, action, since, until, cursor, limit]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (actorMemberId != null) map['actor_member_id'] = actorMemberId!;
    if (resourceType != null) map['resource_type'] = resourceType!;
    if (action != null) map['action'] = action!;
    if (since != null) map['since'] = since!;
    if (until != null) map['until'] = until!;
    if (cursor != null) map['cursor'] = cursor!;
    if (limit != null) map['limit'] = limit!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory AuditQuery.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return AuditQuery(
      houseId: map['house_id'] as String,
      actorMemberId: map['actor_member_id'] == null ? null : map['actor_member_id'] as String,
      resourceType: map['resource_type'] == null ? null : map['resource_type'] as String,
      action: map['action'] == null ? null : map['action'] as String,
      since: map['since'] == null ? null : map['since'] as String,
      until: map['until'] == null ? null : map['until'] as String,
      cursor: map['cursor'] == null ? null : map['cursor'] as String,
      limit: map['limit'] == null ? null : map['limit'] as int,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory AuditQuery.fromCbor(List<int> bytes) =>
      AuditQuery.fromCborValue(CsilCbor.decode(bytes));
}

final class AuditPage {
  final List<AuditEntry> entries;
  final String? nextCursor;

  const AuditPage({
    required this.entries,
    this.nextCursor,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['entries'] = entries;
    if (nextCursor != null) map['next_cursor'] = nextCursor;
    return map;
  }

  factory AuditPage.fromMap(Map<String, Object?> map) {
    return AuditPage(
      entries: map['entries'] as List<AuditEntry>,
      nextCursor: map['next_cursor'] as String?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! AuditPage) return false;
    return entries == other.entries &&
        nextCursor == other.nextCursor;
  }

  @override
  int get hashCode => Object.hashAll([entries, nextCursor]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['entries'] = entries.map((csilE) => csilE.toCborValue()).toList();
    if (nextCursor != null) map['next_cursor'] = nextCursor!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory AuditPage.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return AuditPage(
      entries: (map['entries'] as List).map((csilE) => AuditEntry.fromCborValue(csilE)).cast<AuditEntry>().toList(),
      nextCursor: map['next_cursor'] == null ? null : map['next_cursor'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory AuditPage.fromCbor(List<int> bytes) =>
      AuditPage.fromCborValue(CsilCbor.decode(bytes));
}

final class TrashItem {
  final String resourceType;
  final String resourceId;
  final HouseId houseId;
  final String? title;
  final Timestamp deletedAt;
  final MemberId? deletedByMemberId;
  final String deletedOpId;

  const TrashItem({
    required this.resourceType,
    required this.resourceId,
    required this.houseId,
    this.title,
    required this.deletedAt,
    this.deletedByMemberId,
    required this.deletedOpId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    map['house_id'] = houseId;
    if (title != null) map['title'] = title;
    map['deleted_at'] = deletedAt;
    if (deletedByMemberId != null) map['deleted_by_member_id'] = deletedByMemberId;
    map['deleted_op_id'] = deletedOpId;
    return map;
  }

  factory TrashItem.fromMap(Map<String, Object?> map) {
    return TrashItem(
      resourceType: map['resource_type'] as String,
      resourceId: map['resource_id'] as String,
      houseId: map['house_id'] as HouseId,
      title: map['title'] as String?,
      deletedAt: map['deleted_at'] as Timestamp,
      deletedByMemberId: map['deleted_by_member_id'] as MemberId?,
      deletedOpId: map['deleted_op_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! TrashItem) return false;
    return resourceType == other.resourceType &&
        resourceId == other.resourceId &&
        houseId == other.houseId &&
        title == other.title &&
        deletedAt == other.deletedAt &&
        deletedByMemberId == other.deletedByMemberId &&
        deletedOpId == other.deletedOpId;
  }

  @override
  int get hashCode => Object.hashAll([resourceType, resourceId, houseId, title, deletedAt, deletedByMemberId, deletedOpId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    map['house_id'] = houseId;
    if (title != null) map['title'] = title!;
    map['deleted_at'] = deletedAt;
    if (deletedByMemberId != null) map['deleted_by_member_id'] = deletedByMemberId!;
    map['deleted_op_id'] = deletedOpId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory TrashItem.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return TrashItem(
      resourceType: map['resource_type'] as String,
      resourceId: map['resource_id'] as String,
      houseId: map['house_id'] as String,
      title: map['title'] == null ? null : map['title'] as String,
      deletedAt: map['deleted_at'] as String,
      deletedByMemberId: map['deleted_by_member_id'] == null ? null : map['deleted_by_member_id'] as String,
      deletedOpId: map['deleted_op_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory TrashItem.fromCbor(List<int> bytes) =>
      TrashItem.fromCborValue(CsilCbor.decode(bytes));
}

final class TrashPage {
  final List<TrashItem> items;
  final String? nextCursor;

  const TrashPage({
    required this.items,
    this.nextCursor,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['items'] = items;
    if (nextCursor != null) map['next_cursor'] = nextCursor;
    return map;
  }

  factory TrashPage.fromMap(Map<String, Object?> map) {
    return TrashPage(
      items: map['items'] as List<TrashItem>,
      nextCursor: map['next_cursor'] as String?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! TrashPage) return false;
    return items == other.items &&
        nextCursor == other.nextCursor;
  }

  @override
  int get hashCode => Object.hashAll([items, nextCursor]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['items'] = items.map((csilE) => csilE.toCborValue()).toList();
    if (nextCursor != null) map['next_cursor'] = nextCursor!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory TrashPage.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return TrashPage(
      items: (map['items'] as List).map((csilE) => TrashItem.fromCborValue(csilE)).cast<TrashItem>().toList(),
      nextCursor: map['next_cursor'] == null ? null : map['next_cursor'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory TrashPage.fromCbor(List<int> bytes) =>
      TrashPage.fromCborValue(CsilCbor.decode(bytes));
}

final class RestoreRequest {
  final HouseId houseId;
  final String? deletedOpId;
  final String? resourceType;
  final String? resourceId;

  const RestoreRequest({
    required this.houseId,
    this.deletedOpId,
    this.resourceType,
    this.resourceId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (deletedOpId != null) map['deleted_op_id'] = deletedOpId;
    if (resourceType != null) map['resource_type'] = resourceType;
    if (resourceId != null) map['resource_id'] = resourceId;
    return map;
  }

  factory RestoreRequest.fromMap(Map<String, Object?> map) {
    return RestoreRequest(
      houseId: map['house_id'] as HouseId,
      deletedOpId: map['deleted_op_id'] as String?,
      resourceType: map['resource_type'] as String?,
      resourceId: map['resource_id'] as String?,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! RestoreRequest) return false;
    return houseId == other.houseId &&
        deletedOpId == other.deletedOpId &&
        resourceType == other.resourceType &&
        resourceId == other.resourceId;
  }

  @override
  int get hashCode => Object.hashAll([houseId, deletedOpId, resourceType, resourceId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    if (deletedOpId != null) map['deleted_op_id'] = deletedOpId!;
    if (resourceType != null) map['resource_type'] = resourceType!;
    if (resourceId != null) map['resource_id'] = resourceId!;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory RestoreRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return RestoreRequest(
      houseId: map['house_id'] as String,
      deletedOpId: map['deleted_op_id'] == null ? null : map['deleted_op_id'] as String,
      resourceType: map['resource_type'] == null ? null : map['resource_type'] as String,
      resourceId: map['resource_id'] == null ? null : map['resource_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory RestoreRequest.fromCbor(List<int> bytes) =>
      RestoreRequest.fromCborValue(CsilCbor.decode(bytes));
}

final class PurgeRequest {
  final HouseId houseId;
  final String resourceType;
  final String resourceId;

  const PurgeRequest({
    required this.houseId,
    required this.resourceType,
    required this.resourceId,
  });

  Map<String, Object?> toMap() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  factory PurgeRequest.fromMap(Map<String, Object?> map) {
    return PurgeRequest(
      houseId: map['house_id'] as HouseId,
      resourceType: map['resource_type'] as String,
      resourceId: map['resource_id'] as String,
    );
  }

  @override
  bool operator ==(Object other) {
    if (other is! PurgeRequest) return false;
    return houseId == other.houseId &&
        resourceType == other.resourceType &&
        resourceId == other.resourceId;
  }

  @override
  int get hashCode => Object.hashAll([houseId, resourceType, resourceId]);

  /// The CBOR-encodable dynamic tree for this record (deep).
  Map<String, Object?> toCborValue() {
    final map = <String, Object?>{};
    map['house_id'] = houseId;
    map['resource_type'] = resourceType;
    map['resource_id'] = resourceId;
    return map;
  }

  /// Reconstruct this record from a decoded CBOR dynamic tree.
  factory PurgeRequest.fromCborValue(Object? cbor) {
    final map = cbor as Map;
    return PurgeRequest(
      houseId: map['house_id'] as String,
      resourceType: map['resource_type'] as String,
      resourceId: map['resource_id'] as String,
    );
  }

  /// Encode this record to canonical CSIL CBOR bytes.
  Uint8List toCbor() => CsilCbor.encodeValue(toCborValue());

  /// Decode a CSIL CBOR byte payload into this record.
  factory PurgeRequest.fromCbor(List<int> bytes) =>
      PurgeRequest.fromCborValue(CsilCbor.decode(bytes));
}

