defmodule LonghouseClient.MixProject do
  use Mix.Project

  def project do
    [
      app: :longhouse_client,
      version: "0.13.0",
      elixir: "~> 1.14",
      deps: deps()
    ]
  end

  defp deps do
    []
  end
end
