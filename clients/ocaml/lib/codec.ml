(* Generated CBOR (de)serializers for the CSIL value types. *)
(* Code generated by csilgen; DO NOT EDIT. *)

[@@@warning "-30"]

open Types

module Cbor = Csil_cbor

let rec encode_task_status (v : task_status) : Cbor.t =
  match v with Open -> Cbor.Text "open" | In_progress -> Cbor.Text "in_progress" | Done -> Cbor.Text "done" | Cancelled -> Cbor.Text "cancelled"

and encode_target_type (v : target_type) : Cbor.t =
  match v with Event -> Cbor.Text "event" | Task -> Cbor.Text "task" | Project -> Cbor.Text "project"

and encode_access_level (v : access_level) : Cbor.t =
  match v with None -> Cbor.Text "none" | Read -> Cbor.Text "read" | Edit -> Cbor.Text "edit" | Full -> Cbor.Text "full"

and encode_grantee_type (v : grantee_type) : Cbor.t =
  match v with Member -> Cbor.Text "member" | Group -> Cbor.Text "group"

and encode_resource_type (v : resource_type) : Cbor.t =
  match v with Event -> Cbor.Text "event" | Task -> Cbor.Text "task" | House -> Cbor.Text "house"

and encode_project_status (v : project_status) : Cbor.t =
  match v with Active -> Cbor.Text "active" | Archived -> Cbor.Text "archived"

and encode_dependency_node_type (v : dependency_node_type) : Cbor.t =
  match v with Task -> Cbor.Text "task" | Project -> Cbor.Text "project"

and encode_recurrence_freq (v : recurrence_freq) : Cbor.t =
  match v with Hourly -> Cbor.Text "hourly" | Daily -> Cbor.Text "daily" | Weekly -> Cbor.Text "weekly" | Monthly -> Cbor.Text "monthly" | Quarterly -> Cbor.Text "quarterly" | Yearly -> Cbor.Text "yearly"

and encode_milestone_state (v : milestone_state) : Cbor.t =
  match v with Done -> Cbor.Text "done" | Current -> Cbor.Text "current" | Future -> Cbor.Text "future"

and encode_house (v : house) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_member (v : member) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.email with Some csil_x -> Some (Cbor.Text "email", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         (match v.avatar_url with Some csil_x -> Some (Cbor.Text "avatar_url", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.display_name with Some csil_x -> Some (Cbor.Text "display_name", (Cbor.Text csil_x)) | None -> None);
         (match v.last_seen_at with Some csil_x -> Some (Cbor.Text "last_seen_at", (Cbor.Text csil_x)) | None -> None);
         (match v.deactivated_at with Some csil_x -> Some (Cbor.Text "deactivated_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "linkkeys_domain", (Cbor.Text v.linkkeys_domain));
         Some (Cbor.Text "linkkeys_user_id", (Cbor.Text v.linkkeys_user_id));
         (match v.cached_public_key with Some csil_x -> Some (Cbor.Text "cached_public_key", (Cbor.Bytes csil_x)) | None -> None);
       ])

and encode_trusted_domain (v : trusted_domain) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "domain", (Cbor.Text v.domain));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "trusted_domain_id", (Cbor.Text v.trusted_domain_id));
       ])

and encode_role (v : role) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         Some (Cbor.Text "role_id", (Cbor.Text v.role_id));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_member_role (v : member_role) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "role_id", (Cbor.Text v.role_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
       ])

and encode_member_audit (v : member_audit) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "action", (Cbor.Text v.action));
         (match v.detail with Some csil_x -> Some (Cbor.Text "detail", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "audit_id", (Cbor.Text v.audit_id));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.target_id with Some csil_x -> Some (Cbor.Text "target_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         (match v.target_type with Some csil_x -> Some (Cbor.Text "target_type", (Cbor.Text csil_x)) | None -> None);
         (match v.actor_member_id with Some csil_x -> Some (Cbor.Text "actor_member_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "subject_member_id", (Cbor.Text v.subject_member_id));
       ])

and encode_skill (v : skill) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "skill_id", (Cbor.Text v.skill_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_member_skill (v : member_skill) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "skill_id", (Cbor.Text v.skill_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
       ])

and encode_group_skill (v : group_skill) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "group_id", (Cbor.Text v.group_id));
         Some (Cbor.Text "skill_id", (Cbor.Text v.skill_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
       ])

and encode_group (v : group) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         Some (Cbor.Text "group_id", (Cbor.Text v.group_id));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_group_member (v : group_member) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "group_id", (Cbor.Text v.group_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
       ])

and encode_project (v : project) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         (match v.status with Some csil_x -> Some (Cbor.Text "status", (encode_project_status csil_x)) | None -> None);
         (match v.category with Some csil_x -> Some (Cbor.Text "category", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.visibility with Some csil_x -> Some (Cbor.Text "visibility", (encode_access_level csil_x)) | None -> None);
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
         (match v.created_by_member_id with Some csil_x -> Some (Cbor.Text "created_by_member_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_project_task (v : project_task) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "position", (Cbor.int64 v.position));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_project_member (v : project_member) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_project_owner (v : project_owner) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_milestone (v : milestone) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "label", (Cbor.Text v.label));
         Some (Cbor.Text "state", (encode_milestone_state v.state));
         Some (Cbor.Text "position", (Cbor.int64 v.position));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         Some (Cbor.Text "when_label", (Cbor.Text v.when_label));
         Some (Cbor.Text "milestone_id", (Cbor.Text v.milestone_id));
       ])

and encode_event (v : event) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "title", (Cbor.Text v.title));
         (match v.all_day with Some csil_x -> Some (Cbor.Text "all_day", (Cbor.Bool csil_x)) | None -> None);
         (match v.ends_at with Some csil_x -> Some (Cbor.Text "ends_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "event_id", (Cbor.Text v.event_id));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.location with Some csil_x -> Some (Cbor.Text "location", (Cbor.Text csil_x)) | None -> None);
         (match v.starts_at with Some csil_x -> Some (Cbor.Text "starts_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "owner_member_id", (Cbor.Text v.owner_member_id));
         (match v.recurrence_freq with Some csil_x -> Some (Cbor.Text "recurrence_freq", (encode_recurrence_freq csil_x)) | None -> None);
         (match v.next_recurrence_at with Some csil_x -> Some (Cbor.Text "next_recurrence_at", (Cbor.Text csil_x)) | None -> None);
         (match v.recurrence_interval with Some csil_x -> Some (Cbor.Text "recurrence_interval", (Cbor.int64 csil_x)) | None -> None);
         (match v.recurrence_by_setpos with Some csil_x -> Some (Cbor.Text "recurrence_by_setpos", (Cbor.int64 csil_x)) | None -> None);
         (match v.recurrence_by_weekday with Some csil_x -> Some (Cbor.Text "recurrence_by_weekday", (Cbor.Array (List.map (fun csil_e -> (Cbor.int64 csil_e)) csil_x))) | None -> None);
         (match v.recurrence_root_event_id with Some csil_x -> Some (Cbor.Text "recurrence_root_event_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_task (v : task) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.tag with Some csil_x -> Some (Cbor.Text "tag", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "title", (Cbor.Text v.title));
         (match v.due_at with Some csil_x -> Some (Cbor.Text "due_at", (Cbor.Text csil_x)) | None -> None);
         (match v.status with Some csil_x -> Some (Cbor.Text "status", (encode_task_status csil_x)) | None -> None);
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.assignees with Some csil_x -> Some (Cbor.Text "assignees", (Cbor.Array (List.map (fun csil_e -> (Cbor.Text csil_e)) csil_x))) | None -> None);
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         (match v.deleted_at with Some csil_x -> Some (Cbor.Text "deleted_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         (match v.visibility with Some csil_x -> Some (Cbor.Text "visibility", (encode_access_level csil_x)) | None -> None);
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
         (match v.parent_task_id with Some csil_x -> Some (Cbor.Text "parent_task_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "owner_member_id", (Cbor.Text v.owner_member_id));
         (match v.recurrence_freq with Some csil_x -> Some (Cbor.Text "recurrence_freq", (encode_recurrence_freq csil_x)) | None -> None);
         (match v.estimate_minutes with Some csil_x -> Some (Cbor.Text "estimate_minutes", (Cbor.int64 csil_x)) | None -> None);
         (match v.next_recurrence_at with Some csil_x -> Some (Cbor.Text "next_recurrence_at", (Cbor.Text csil_x)) | None -> None);
         (match v.recurrence_interval with Some csil_x -> Some (Cbor.Text "recurrence_interval", (Cbor.int64 csil_x)) | None -> None);
         (match v.assigned_to_skill_id with Some csil_x -> Some (Cbor.Text "assigned_to_skill_id", (Cbor.Text csil_x)) | None -> None);
         (match v.recurrence_by_setpos with Some csil_x -> Some (Cbor.Text "recurrence_by_setpos", (Cbor.int64 csil_x)) | None -> None);
         (match v.recurrence_by_weekday with Some csil_x -> Some (Cbor.Text "recurrence_by_weekday", (Cbor.Array (List.map (fun csil_e -> (Cbor.int64 csil_e)) csil_x))) | None -> None);
         (match v.recurrence_root_task_id with Some csil_x -> Some (Cbor.Text "recurrence_root_task_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_comment (v : comment) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "body", (Cbor.Text v.body));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "target_id", (Cbor.Text v.target_id));
         Some (Cbor.Text "comment_id", (Cbor.Text v.comment_id));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         Some (Cbor.Text "updated_at", (Cbor.Text v.updated_at));
         Some (Cbor.Text "target_type", (encode_target_type v.target_type));
       ])

and encode_share (v : share) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "share_id", (Cbor.Text v.share_id));
         Some (Cbor.Text "shared_by", (Cbor.Text v.shared_by));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         (match v.expires_at with Some csil_x -> Some (Cbor.Text "expires_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "resource_id", (Cbor.Text v.resource_id));
         (match v.access_level with Some csil_x -> Some (Cbor.Text "access_level", (encode_access_level csil_x)) | None -> None);
         Some (Cbor.Text "resource_type", (encode_resource_type v.resource_type));
         Some (Cbor.Text "linkkeys_domain", (Cbor.Text v.linkkeys_domain));
         Some (Cbor.Text "linkkeys_user_id", (Cbor.Text v.linkkeys_user_id));
       ])

and encode_house_summary (v : house_summary) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "name", (Cbor.Text v.name));
         Some (Cbor.Text "roles", (Cbor.Array (List.map (fun csil_e -> (Cbor.Text csil_e)) v.roles)));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_house_roles (v : house_roles) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "house", (Cbor.Text v.house));
         Some (Cbor.Text "roles", (Cbor.Array (List.map (fun csil_e -> (Cbor.Text csil_e)) v.roles)));
         Some (Cbor.Text "member", (Cbor.Text v.member));
       ])

and encode_identity (v : identity) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "exp", (Cbor.int64 v.exp));
         Some (Cbor.Text "iat", (Cbor.int64 v.iat));
         Some (Cbor.Text "domain", (Cbor.Text v.domain));
         Some (Cbor.Text "houses", (Cbor.Array (List.map (fun csil_e -> (encode_house_roles csil_e)) v.houses)));
         Some (Cbor.Text "user_id", (Cbor.Text v.user_id));
         (match v.display_name with Some csil_x -> Some (Cbor.Text "display_name", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_login_request (v : login_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "signed_assertion", (Cbor.Text v.signed_assertion));
       ])

and encode_complete_request (v : complete_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "encrypted_token", (Cbor.Text v.encrypted_token));
       ])

and encode_login_response (v : login_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "token", (Cbor.Text v.token));
         Some (Cbor.Text "domain", (Cbor.Text v.domain));
         Some (Cbor.Text "user_id", (Cbor.Text v.user_id));
         Some (Cbor.Text "expires_at", (Cbor.Text v.expires_at));
         (match v.display_name with Some csil_x -> Some (Cbor.Text "display_name", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_dev_user_entry (v : dev_user_entry) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "roles", (Cbor.Array (List.map (fun csil_e -> (Cbor.Text csil_e)) v.roles)));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "house_name", (Cbor.Text v.house_name));
         (match v.display_name with Some csil_x -> Some (Cbor.Text "display_name", (Cbor.Text csil_x)) | None -> None);
         (match v.linkkeys_domain with Some csil_x -> Some (Cbor.Text "linkkeys_domain", (Cbor.Text csil_x)) | None -> None);
         (match v.linkkeys_user_id with Some csil_x -> Some (Cbor.Text "linkkeys_user_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_dev_users_response (v : dev_users_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "users", (Cbor.Array (List.map (fun csil_e -> (encode_dev_user_entry csil_e)) v.users)));
       ])

and encode_dev_login_request (v : dev_login_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_me_response (v : me_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "domain", (Cbor.Text v.domain));
         Some (Cbor.Text "houses", (Cbor.Array (List.map (fun csil_e -> (encode_house_summary csil_e)) v.houses)));
         Some (Cbor.Text "user_id", (Cbor.Text v.user_id));
         Some (Cbor.Text "expires_at", (Cbor.Text v.expires_at));
         (match v.display_name with Some csil_x -> Some (Cbor.Text "display_name", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_empty_request (_v : empty_request) : Cbor.t = Cbor.Map []

and encode_empty_response (_v : empty_response) : Cbor.t = Cbor.Map []

and encode_bool_response (v : bool_response) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "value", (Cbor.Bool v.value));
       ])

and encode_house_list_request (v : house_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
       ])

and encode_house_scoped_list_request (v : house_scoped_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
       ])

and encode_task_list (v : task_list) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "tasks", (Cbor.Array (List.map (fun csil_e -> (encode_task csil_e)) v.tasks)));
         Some (Cbor.Text "hidden_count", (Cbor.int64 v.hidden_count));
       ])

and encode_project_list (v : project_list) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "projects", (Cbor.Array (List.map (fun csil_e -> (encode_project csil_e)) v.projects)));
         Some (Cbor.Text "hidden_count", (Cbor.int64 v.hidden_count));
       ])

and encode_member_scoped_list_request (v : member_scoped_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_project_scoped_list_request (v : project_scoped_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_comment_list_request (v : comment_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
         Some (Cbor.Text "target_id", (Cbor.Text v.target_id));
         Some (Cbor.Text "target_type", (encode_target_type v.target_type));
       ])

and encode_notification (v : notification) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "body", (Cbor.Text v.body));
         Some (Cbor.Text "kind", (Cbor.Text v.kind));
         Some (Cbor.Text "read", (Cbor.Bool v.read));
         (match v.read_at with Some csil_x -> Some (Cbor.Text "read_at", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         (match v.target_id with Some csil_x -> Some (Cbor.Text "target_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "actor_name", (Cbor.Text v.actor_name));
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         (match v.target_type with Some csil_x -> Some (Cbor.Text "target_type", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "target_title", (Cbor.Text v.target_title));
         (match v.actor_member_id with Some csil_x -> Some (Cbor.Text "actor_member_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "notification_id", (Cbor.Text v.notification_id));
       ])

and encode_notification_list_request (v : notification_list_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.offset with Some csil_x -> Some (Cbor.Text "offset", (Cbor.int64 csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.unread_only with Some csil_x -> Some (Cbor.Text "unread_only", (Cbor.Bool csil_x)) | None -> None);
       ])

and encode_notification_unread_count (v : notification_unread_count) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "count", (Cbor.int64 v.count));
       ])

and encode_share_access_request (v : share_access_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "resource_id", (Cbor.Text v.resource_id));
         Some (Cbor.Text "resource_type", (encode_resource_type v.resource_type));
         Some (Cbor.Text "linkkeys_domain", (Cbor.Text v.linkkeys_domain));
         Some (Cbor.Text "linkkeys_user_id", (Cbor.Text v.linkkeys_user_id));
       ])

and encode_resource_ref (v : resource_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "resource_id", (Cbor.Text v.resource_id));
         Some (Cbor.Text "resource_type", (encode_resource_type v.resource_type));
       ])

and encode_member_role_ref (v : member_role_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "role_id", (Cbor.Text v.role_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_member_skill_ref (v : member_skill_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "skill_id", (Cbor.Text v.skill_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_group_skill_ref (v : group_skill_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "group_id", (Cbor.Text v.group_id));
         Some (Cbor.Text "skill_id", (Cbor.Text v.skill_id));
       ])

and encode_group_member_ref (v : group_member_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "group_id", (Cbor.Text v.group_id));
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
       ])

and encode_project_task_ref (v : project_task_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_project_task_order_request (v : project_task_order_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "position", (Cbor.int64 v.position));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_project_member_ref (v : project_member_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_project_owner_ref (v : project_owner_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "member_id", (Cbor.Text v.member_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
       ])

and encode_dependency_ref (v : dependency_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "dependent_id", (Cbor.Text v.dependent_id));
         Some (Cbor.Text "dependency_id", (Cbor.Text v.dependency_id));
         Some (Cbor.Text "dependent_type", (encode_dependency_node_type v.dependent_type));
         Some (Cbor.Text "dependency_type", (encode_dependency_node_type v.dependency_type));
       ])

and encode_dependency_target (v : dependency_target) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "id", (Cbor.Text v.id));
         Some (Cbor.Text "type", (encode_dependency_node_type v.type_));
       ])

and encode_dependency_node (v : dependency_node) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "id", (Cbor.Text v.id));
         Some (Cbor.Text "type", (encode_dependency_node_type v.type_));
         Some (Cbor.Text "title", (Cbor.Text v.title));
         (match v.status with Some csil_x -> Some (Cbor.Text "status", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_dependency_graph (v : dependency_graph) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "dependents", (Cbor.Array (List.map (fun csil_e -> (encode_dependency_node csil_e)) v.dependents)));
         Some (Cbor.Text "dependencies", (Cbor.Array (List.map (fun csil_e -> (encode_dependency_node csil_e)) v.dependencies)));
       ])

and encode_grant (v : grant) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "grantee_id", (Cbor.Text v.grantee_id));
         Some (Cbor.Text "access_level", (encode_access_level v.access_level));
         Some (Cbor.Text "grantee_type", (encode_grantee_type v.grantee_type));
       ])

and encode_task_grant_ref (v : task_grant_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "grantee_id", (Cbor.Text v.grantee_id));
         Some (Cbor.Text "grantee_type", (encode_grantee_type v.grantee_type));
       ])

and encode_put_task_grant_request (v : put_task_grant_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "grantee_id", (Cbor.Text v.grantee_id));
         Some (Cbor.Text "access_level", (encode_access_level v.access_level));
         Some (Cbor.Text "grantee_type", (encode_grantee_type v.grantee_type));
       ])

and encode_set_task_visibility_request (v : set_task_visibility_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "task_id", (Cbor.Text v.task_id));
         Some (Cbor.Text "visibility", (encode_access_level v.visibility));
       ])

and encode_project_grant_ref (v : project_grant_ref) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "grantee_id", (Cbor.Text v.grantee_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
         Some (Cbor.Text "grantee_type", (encode_grantee_type v.grantee_type));
       ])

and encode_put_project_grant_request (v : put_project_grant_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "grantee_id", (Cbor.Text v.grantee_id));
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
         Some (Cbor.Text "access_level", (encode_access_level v.access_level));
         Some (Cbor.Text "grantee_type", (encode_grantee_type v.grantee_type));
       ])

and encode_set_project_visibility_request (v : set_project_visibility_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "project_id", (Cbor.Text v.project_id));
         Some (Cbor.Text "visibility", (encode_access_level v.visibility));
       ])

and encode_effective_settings (v : effective_settings) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.bug_reports_enabled with Some csil_x -> Some (Cbor.Text "bug_reports_enabled", (Cbor.Bool csil_x)) | None -> None);
         (match v.bug_reports_project_id with Some csil_x -> Some (Cbor.Text "bug_reports_project_id", (Cbor.Text csil_x)) | None -> None);
         (match v.default_project_visibility with Some csil_x -> Some (Cbor.Text "default_project_visibility", (encode_access_level csil_x)) | None -> None);
       ])

and encode_update_settings_request (v : update_settings_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "settings", (encode_effective_settings v.settings));
       ])

and encode_bug_report_request (v : bug_report_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "title", (Cbor.Text v.title));
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.description with Some csil_x -> Some (Cbor.Text "description", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_service_error (v : service_error) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "code", (Cbor.int64 v.code));
         Some (Cbor.Text "message", (Cbor.Text v.message));
       ])

and encode_audit_entry (v : audit_entry) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.after with Some csil_x -> Some (Cbor.Text "after", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "action", (Cbor.Text v.action));
         (match v.before with Some csil_x -> Some (Cbor.Text "before", (Cbor.Text csil_x)) | None -> None);
         (match v.detail with Some csil_x -> Some (Cbor.Text "detail", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "method", (Cbor.Text v.method_));
         Some (Cbor.Text "outcome", (Cbor.Text v.outcome));
         Some (Cbor.Text "audit_id", (Cbor.Text v.audit_id));
         (match v.house_id with Some csil_x -> Some (Cbor.Text "house_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "created_at", (Cbor.Text v.created_at));
         (match v.resource_id with Some csil_x -> Some (Cbor.Text "resource_id", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "actor_domain", (Cbor.Text v.actor_domain));
         Some (Cbor.Text "service_name", (Cbor.Text v.service_name));
         Some (Cbor.Text "actor_user_id", (Cbor.Text v.actor_user_id));
         (match v.resource_type with Some csil_x -> Some (Cbor.Text "resource_type", (Cbor.Text csil_x)) | None -> None);
         (match v.actor_member_id with Some csil_x -> Some (Cbor.Text "actor_member_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_audit_query (v : audit_query) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.limit with Some csil_x -> Some (Cbor.Text "limit", (Cbor.int64 csil_x)) | None -> None);
         (match v.since with Some csil_x -> Some (Cbor.Text "since", (Cbor.Text csil_x)) | None -> None);
         (match v.until with Some csil_x -> Some (Cbor.Text "until", (Cbor.Text csil_x)) | None -> None);
         (match v.action with Some csil_x -> Some (Cbor.Text "action", (Cbor.Text csil_x)) | None -> None);
         (match v.cursor with Some csil_x -> Some (Cbor.Text "cursor", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.resource_type with Some csil_x -> Some (Cbor.Text "resource_type", (Cbor.Text csil_x)) | None -> None);
         (match v.actor_member_id with Some csil_x -> Some (Cbor.Text "actor_member_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_audit_page (v : audit_page) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "entries", (Cbor.Array (List.map (fun csil_e -> (encode_audit_entry csil_e)) v.entries)));
         (match v.next_cursor with Some csil_x -> Some (Cbor.Text "next_cursor", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_trash_item (v : trash_item) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         (match v.title with Some csil_x -> Some (Cbor.Text "title", (Cbor.Text csil_x)) | None -> None);
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "deleted_at", (Cbor.Text v.deleted_at));
         Some (Cbor.Text "resource_id", (Cbor.Text v.resource_id));
         Some (Cbor.Text "deleted_op_id", (Cbor.Text v.deleted_op_id));
         Some (Cbor.Text "resource_type", (Cbor.Text v.resource_type));
         (match v.deleted_by_member_id with Some csil_x -> Some (Cbor.Text "deleted_by_member_id", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_trash_page (v : trash_page) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "items", (Cbor.Array (List.map (fun csil_e -> (encode_trash_item csil_e)) v.items)));
         (match v.next_cursor with Some csil_x -> Some (Cbor.Text "next_cursor", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_restore_request (v : restore_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         (match v.resource_id with Some csil_x -> Some (Cbor.Text "resource_id", (Cbor.Text csil_x)) | None -> None);
         (match v.deleted_op_id with Some csil_x -> Some (Cbor.Text "deleted_op_id", (Cbor.Text csil_x)) | None -> None);
         (match v.resource_type with Some csil_x -> Some (Cbor.Text "resource_type", (Cbor.Text csil_x)) | None -> None);
       ])

and encode_purge_request (v : purge_request) : Cbor.t =
  Cbor.Map
    (List.filter_map
       (fun x -> x)
       [
         Some (Cbor.Text "house_id", (Cbor.Text v.house_id));
         Some (Cbor.Text "resource_id", (Cbor.Text v.resource_id));
         Some (Cbor.Text "resource_type", (Cbor.Text v.resource_type));
       ])

let rec decode_task_status (csil_c : Cbor.t) : task_status =
  match Cbor.to_text csil_c with "open" -> Open | "in_progress" -> In_progress | "done" -> Done | "cancelled" -> Cancelled | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_target_type (csil_c : Cbor.t) : target_type =
  match Cbor.to_text csil_c with "event" -> Event | "task" -> Task | "project" -> Project | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_access_level (csil_c : Cbor.t) : access_level =
  match Cbor.to_text csil_c with "none" -> None | "read" -> Read | "edit" -> Edit | "full" -> Full | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_grantee_type (csil_c : Cbor.t) : grantee_type =
  match Cbor.to_text csil_c with "member" -> Member | "group" -> Group | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_resource_type (csil_c : Cbor.t) : resource_type =
  match Cbor.to_text csil_c with "event" -> Event | "task" -> Task | "house" -> House | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_project_status (csil_c : Cbor.t) : project_status =
  match Cbor.to_text csil_c with "active" -> Active | "archived" -> Archived | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_dependency_node_type (csil_c : Cbor.t) : dependency_node_type =
  match Cbor.to_text csil_c with "task" -> Task | "project" -> Project | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_recurrence_freq (csil_c : Cbor.t) : recurrence_freq =
  match Cbor.to_text csil_c with "hourly" -> Hourly | "daily" -> Daily | "weekly" -> Weekly | "monthly" -> Monthly | "quarterly" -> Quarterly | "yearly" -> Yearly | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_milestone_state (csil_c : Cbor.t) : milestone_state =
  match Cbor.to_text csil_c with "done" -> Done | "current" -> Current | "future" -> Future | csil_s -> failwith ("csilgen: unknown enum literal " ^ csil_s)

and decode_house (csil_c : Cbor.t) : house =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for house"

and decode_member (csil_c : Cbor.t) : member =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        email = (match csil_field "email" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        avatar_url = (match csil_field "avatar_url" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        display_name = (match csil_field "display_name" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        last_seen_at = (match csil_field "last_seen_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        deactivated_at = (match csil_field "deactivated_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        linkkeys_domain = (Cbor.to_text (csil_req "linkkeys_domain"));
        linkkeys_user_id = (Cbor.to_text (csil_req "linkkeys_user_id"));
        cached_public_key = (match csil_field "cached_public_key" with Some csil_v -> Some (Cbor.to_bytes csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for member"

and decode_trusted_domain (csil_c : Cbor.t) : trusted_domain =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        domain = (Cbor.to_text (csil_req "domain"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        trusted_domain_id = (Cbor.to_text (csil_req "trusted_domain_id"));
      }
  | _ -> failwith "csilgen: expected map for trusted_domain"

and decode_role (csil_c : Cbor.t) : role =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        role_id = (Cbor.to_text (csil_req "role_id"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for role"

and decode_member_role (csil_c : Cbor.t) : member_role =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        role_id = (Cbor.to_text (csil_req "role_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
      }
  | _ -> failwith "csilgen: expected map for member_role"

and decode_member_audit (csil_c : Cbor.t) : member_audit =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        action = (Cbor.to_text (csil_req "action"));
        detail = (match csil_field "detail" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        audit_id = (Cbor.to_text (csil_req "audit_id"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        target_id = (match csil_field "target_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        created_at = (Cbor.to_text (csil_req "created_at"));
        target_type = (match csil_field "target_type" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        actor_member_id = (match csil_field "actor_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        subject_member_id = (Cbor.to_text (csil_req "subject_member_id"));
      }
  | _ -> failwith "csilgen: expected map for member_audit"

and decode_skill (csil_c : Cbor.t) : skill =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        skill_id = (Cbor.to_text (csil_req "skill_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for skill"

and decode_member_skill (csil_c : Cbor.t) : member_skill =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        skill_id = (Cbor.to_text (csil_req "skill_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
      }
  | _ -> failwith "csilgen: expected map for member_skill"

and decode_group_skill (csil_c : Cbor.t) : group_skill =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        group_id = (Cbor.to_text (csil_req "group_id"));
        skill_id = (Cbor.to_text (csil_req "skill_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
      }
  | _ -> failwith "csilgen: expected map for group_skill"

and decode_group (csil_c : Cbor.t) : group =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        group_id = (Cbor.to_text (csil_req "group_id"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for group"

and decode_group_member (csil_c : Cbor.t) : group_member =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        group_id = (Cbor.to_text (csil_req "group_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
      }
  | _ -> failwith "csilgen: expected map for group_member"

and decode_project (csil_c : Cbor.t) : project =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        status = (match csil_field "status" with Some csil_v -> Some (decode_project_status csil_v) | None -> None);
        category = (match csil_field "category" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        project_id = (Cbor.to_text (csil_req "project_id"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        visibility = (match csil_field "visibility" with Some csil_v -> Some (decode_access_level csil_v) | None -> None);
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        created_by_member_id = (match csil_field "created_by_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for project"

and decode_project_task (csil_c : Cbor.t) : project_task =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        position = (Cbor.to_i64 (csil_req "position"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_task"

and decode_project_member (csil_c : Cbor.t) : project_member =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        member_id = (Cbor.to_text (csil_req "member_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_member"

and decode_project_owner (csil_c : Cbor.t) : project_owner =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        member_id = (Cbor.to_text (csil_req "member_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_owner"

and decode_milestone (csil_c : Cbor.t) : milestone =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        label = (Cbor.to_text (csil_req "label"));
        state = (decode_milestone_state (csil_req "state"));
        position = (Cbor.to_i64 (csil_req "position"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        project_id = (Cbor.to_text (csil_req "project_id"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        when_label = (Cbor.to_text (csil_req "when_label"));
        milestone_id = (Cbor.to_text (csil_req "milestone_id"));
      }
  | _ -> failwith "csilgen: expected map for milestone"

and decode_event (csil_c : Cbor.t) : event =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        title = (Cbor.to_text (csil_req "title"));
        all_day = (match csil_field "all_day" with Some csil_v -> Some (Cbor.to_bool csil_v) | None -> None);
        ends_at = (match csil_field "ends_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        event_id = (Cbor.to_text (csil_req "event_id"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        location = (match csil_field "location" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        starts_at = (match csil_field "starts_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        owner_member_id = (Cbor.to_text (csil_req "owner_member_id"));
        recurrence_freq = (match csil_field "recurrence_freq" with Some csil_v -> Some (decode_recurrence_freq csil_v) | None -> None);
        next_recurrence_at = (match csil_field "next_recurrence_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        recurrence_interval = (match csil_field "recurrence_interval" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        recurrence_by_setpos = (match csil_field "recurrence_by_setpos" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        recurrence_by_weekday = (match csil_field "recurrence_by_weekday" with Some csil_v -> Some (match csil_v with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_i64 csil_e)) csil_xs | _ -> failwith "csilgen: expected array") | None -> None);
        recurrence_root_event_id = (match csil_field "recurrence_root_event_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for event"

and decode_task (csil_c : Cbor.t) : task =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        tag = (match csil_field "tag" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        title = (Cbor.to_text (csil_req "title"));
        due_at = (match csil_field "due_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        status = (match csil_field "status" with Some csil_v -> Some (decode_task_status csil_v) | None -> None);
        task_id = (Cbor.to_text (csil_req "task_id"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        assignees = (match csil_field "assignees" with Some csil_v -> Some (match csil_v with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_text csil_e)) csil_xs | _ -> failwith "csilgen: expected array") | None -> None);
        created_at = (Cbor.to_text (csil_req "created_at"));
        deleted_at = (match csil_field "deleted_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        visibility = (match csil_field "visibility" with Some csil_v -> Some (decode_access_level csil_v) | None -> None);
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        parent_task_id = (match csil_field "parent_task_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        owner_member_id = (Cbor.to_text (csil_req "owner_member_id"));
        recurrence_freq = (match csil_field "recurrence_freq" with Some csil_v -> Some (decode_recurrence_freq csil_v) | None -> None);
        estimate_minutes = (match csil_field "estimate_minutes" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        next_recurrence_at = (match csil_field "next_recurrence_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        recurrence_interval = (match csil_field "recurrence_interval" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        assigned_to_skill_id = (match csil_field "assigned_to_skill_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        recurrence_by_setpos = (match csil_field "recurrence_by_setpos" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        recurrence_by_weekday = (match csil_field "recurrence_by_weekday" with Some csil_v -> Some (match csil_v with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_i64 csil_e)) csil_xs | _ -> failwith "csilgen: expected array") | None -> None);
        recurrence_root_task_id = (match csil_field "recurrence_root_task_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for task"

and decode_comment (csil_c : Cbor.t) : comment =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        body = (Cbor.to_text (csil_req "body"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        target_id = (Cbor.to_text (csil_req "target_id"));
        comment_id = (Cbor.to_text (csil_req "comment_id"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        updated_at = (Cbor.to_text (csil_req "updated_at"));
        target_type = (decode_target_type (csil_req "target_type"));
      }
  | _ -> failwith "csilgen: expected map for comment"

and decode_share (csil_c : Cbor.t) : share =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        house_id = (Cbor.to_text (csil_req "house_id"));
        share_id = (Cbor.to_text (csil_req "share_id"));
        shared_by = (Cbor.to_text (csil_req "shared_by"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        expires_at = (match csil_field "expires_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        resource_id = (Cbor.to_text (csil_req "resource_id"));
        access_level = (match csil_field "access_level" with Some csil_v -> Some (decode_access_level csil_v) | None -> None);
        resource_type = (decode_resource_type (csil_req "resource_type"));
        linkkeys_domain = (Cbor.to_text (csil_req "linkkeys_domain"));
        linkkeys_user_id = (Cbor.to_text (csil_req "linkkeys_user_id"));
      }
  | _ -> failwith "csilgen: expected map for share"

and decode_house_summary (csil_c : Cbor.t) : house_summary =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        name = (Cbor.to_text (csil_req "name"));
        roles = (match (csil_req "roles") with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_text csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for house_summary"

and decode_house_roles (csil_c : Cbor.t) : house_roles =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        house = (Cbor.to_text (csil_req "house"));
        roles = (match (csil_req "roles") with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_text csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        member = (Cbor.to_text (csil_req "member"));
      }
  | _ -> failwith "csilgen: expected map for house_roles"

and decode_identity (csil_c : Cbor.t) : identity =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        exp = (Cbor.to_i64 (csil_req "exp"));
        iat = (Cbor.to_i64 (csil_req "iat"));
        domain = (Cbor.to_text (csil_req "domain"));
        houses = (match (csil_req "houses") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_house_roles csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        user_id = (Cbor.to_text (csil_req "user_id"));
        display_name = (match csil_field "display_name" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for identity"

and decode_login_request (csil_c : Cbor.t) : login_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        signed_assertion = (Cbor.to_text (csil_req "signed_assertion"));
      }
  | _ -> failwith "csilgen: expected map for login_request"

and decode_complete_request (csil_c : Cbor.t) : complete_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        encrypted_token = (Cbor.to_text (csil_req "encrypted_token"));
      }
  | _ -> failwith "csilgen: expected map for complete_request"

and decode_login_response (csil_c : Cbor.t) : login_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        token = (Cbor.to_text (csil_req "token"));
        domain = (Cbor.to_text (csil_req "domain"));
        user_id = (Cbor.to_text (csil_req "user_id"));
        expires_at = (Cbor.to_text (csil_req "expires_at"));
        display_name = (match csil_field "display_name" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for login_response"

and decode_dev_user_entry (csil_c : Cbor.t) : dev_user_entry =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        roles = (match (csil_req "roles") with Cbor.Array csil_xs -> List.map (fun csil_e -> (Cbor.to_text csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        house_name = (Cbor.to_text (csil_req "house_name"));
        display_name = (match csil_field "display_name" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        linkkeys_domain = (match csil_field "linkkeys_domain" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        linkkeys_user_id = (match csil_field "linkkeys_user_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for dev_user_entry"

and decode_dev_users_response (csil_c : Cbor.t) : dev_users_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        users = (match (csil_req "users") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_dev_user_entry csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
      }
  | _ -> failwith "csilgen: expected map for dev_users_response"

and decode_dev_login_request (csil_c : Cbor.t) : dev_login_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for dev_login_request"

and decode_me_response (csil_c : Cbor.t) : me_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        domain = (Cbor.to_text (csil_req "domain"));
        houses = (match (csil_req "houses") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_house_summary csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        user_id = (Cbor.to_text (csil_req "user_id"));
        expires_at = (Cbor.to_text (csil_req "expires_at"));
        display_name = (match csil_field "display_name" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for me_response"

and decode_empty_request (csil_c : Cbor.t) : empty_request =
  match csil_c with Cbor.Map _ -> () | _ -> failwith "csilgen: expected map for empty_request"

and decode_empty_response (csil_c : Cbor.t) : empty_response =
  match csil_c with Cbor.Map _ -> () | _ -> failwith "csilgen: expected map for empty_response"

and decode_bool_response (csil_c : Cbor.t) : bool_response =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        value = (Cbor.to_bool (csil_req "value"));
      }
  | _ -> failwith "csilgen: expected map for bool_response"

and decode_house_list_request (csil_c : Cbor.t) : house_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for house_list_request"

and decode_house_scoped_list_request (csil_c : Cbor.t) : house_scoped_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
      }
  | _ -> failwith "csilgen: expected map for house_scoped_list_request"

and decode_task_list (csil_c : Cbor.t) : task_list =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        tasks = (match (csil_req "tasks") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_task csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        hidden_count = (Cbor.to_i64 (csil_req "hidden_count"));
      }
  | _ -> failwith "csilgen: expected map for task_list"

and decode_project_list (csil_c : Cbor.t) : project_list =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        projects = (match (csil_req "projects") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_project csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        hidden_count = (Cbor.to_i64 (csil_req "hidden_count"));
      }
  | _ -> failwith "csilgen: expected map for project_list"

and decode_member_scoped_list_request (csil_c : Cbor.t) : member_scoped_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for member_scoped_list_request"

and decode_project_scoped_list_request (csil_c : Cbor.t) : project_scoped_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_scoped_list_request"

and decode_comment_list_request (csil_c : Cbor.t) : comment_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        target_id = (Cbor.to_text (csil_req "target_id"));
        target_type = (decode_target_type (csil_req "target_type"));
      }
  | _ -> failwith "csilgen: expected map for comment_list_request"

and decode_notification (csil_c : Cbor.t) : notification =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        body = (Cbor.to_text (csil_req "body"));
        kind = (Cbor.to_text (csil_req "kind"));
        read = (Cbor.to_bool (csil_req "read"));
        read_at = (match csil_field "read_at" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
        target_id = (match csil_field "target_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        actor_name = (Cbor.to_text (csil_req "actor_name"));
        created_at = (Cbor.to_text (csil_req "created_at"));
        target_type = (match csil_field "target_type" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        target_title = (Cbor.to_text (csil_req "target_title"));
        actor_member_id = (match csil_field "actor_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        notification_id = (Cbor.to_text (csil_req "notification_id"));
      }
  | _ -> failwith "csilgen: expected map for notification"

and decode_notification_list_request (csil_c : Cbor.t) : notification_list_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        offset = (match csil_field "offset" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        unread_only = (match csil_field "unread_only" with Some csil_v -> Some (Cbor.to_bool csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for notification_list_request"

and decode_notification_unread_count (csil_c : Cbor.t) : notification_unread_count =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        count = (Cbor.to_i64 (csil_req "count"));
      }
  | _ -> failwith "csilgen: expected map for notification_unread_count"

and decode_share_access_request (csil_c : Cbor.t) : share_access_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        resource_id = (Cbor.to_text (csil_req "resource_id"));
        resource_type = (decode_resource_type (csil_req "resource_type"));
        linkkeys_domain = (Cbor.to_text (csil_req "linkkeys_domain"));
        linkkeys_user_id = (Cbor.to_text (csil_req "linkkeys_user_id"));
      }
  | _ -> failwith "csilgen: expected map for share_access_request"

and decode_resource_ref (csil_c : Cbor.t) : resource_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        resource_id = (Cbor.to_text (csil_req "resource_id"));
        resource_type = (decode_resource_type (csil_req "resource_type"));
      }
  | _ -> failwith "csilgen: expected map for resource_ref"

and decode_member_role_ref (csil_c : Cbor.t) : member_role_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        role_id = (Cbor.to_text (csil_req "role_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for member_role_ref"

and decode_member_skill_ref (csil_c : Cbor.t) : member_skill_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        skill_id = (Cbor.to_text (csil_req "skill_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for member_skill_ref"

and decode_group_skill_ref (csil_c : Cbor.t) : group_skill_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        group_id = (Cbor.to_text (csil_req "group_id"));
        skill_id = (Cbor.to_text (csil_req "skill_id"));
      }
  | _ -> failwith "csilgen: expected map for group_skill_ref"

and decode_group_member_ref (csil_c : Cbor.t) : group_member_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        group_id = (Cbor.to_text (csil_req "group_id"));
        member_id = (Cbor.to_text (csil_req "member_id"));
      }
  | _ -> failwith "csilgen: expected map for group_member_ref"

and decode_project_task_ref (csil_c : Cbor.t) : project_task_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_task_ref"

and decode_project_task_order_request (csil_c : Cbor.t) : project_task_order_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        position = (Cbor.to_i64 (csil_req "position"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_task_order_request"

and decode_project_member_ref (csil_c : Cbor.t) : project_member_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        member_id = (Cbor.to_text (csil_req "member_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_member_ref"

and decode_project_owner_ref (csil_c : Cbor.t) : project_owner_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        member_id = (Cbor.to_text (csil_req "member_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
      }
  | _ -> failwith "csilgen: expected map for project_owner_ref"

and decode_dependency_ref (csil_c : Cbor.t) : dependency_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        dependent_id = (Cbor.to_text (csil_req "dependent_id"));
        dependency_id = (Cbor.to_text (csil_req "dependency_id"));
        dependent_type = (decode_dependency_node_type (csil_req "dependent_type"));
        dependency_type = (decode_dependency_node_type (csil_req "dependency_type"));
      }
  | _ -> failwith "csilgen: expected map for dependency_ref"

and decode_dependency_target (csil_c : Cbor.t) : dependency_target =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        id = (Cbor.to_text (csil_req "id"));
        type_ = (decode_dependency_node_type (csil_req "type"));
      }
  | _ -> failwith "csilgen: expected map for dependency_target"

and decode_dependency_node (csil_c : Cbor.t) : dependency_node =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        id = (Cbor.to_text (csil_req "id"));
        type_ = (decode_dependency_node_type (csil_req "type"));
        title = (Cbor.to_text (csil_req "title"));
        status = (match csil_field "status" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for dependency_node"

and decode_dependency_graph (csil_c : Cbor.t) : dependency_graph =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        dependents = (match (csil_req "dependents") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_dependency_node csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        dependencies = (match (csil_req "dependencies") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_dependency_node csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
      }
  | _ -> failwith "csilgen: expected map for dependency_graph"

and decode_grant (csil_c : Cbor.t) : grant =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        grantee_id = (Cbor.to_text (csil_req "grantee_id"));
        access_level = (decode_access_level (csil_req "access_level"));
        grantee_type = (decode_grantee_type (csil_req "grantee_type"));
      }
  | _ -> failwith "csilgen: expected map for grant"

and decode_task_grant_ref (csil_c : Cbor.t) : task_grant_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        grantee_id = (Cbor.to_text (csil_req "grantee_id"));
        grantee_type = (decode_grantee_type (csil_req "grantee_type"));
      }
  | _ -> failwith "csilgen: expected map for task_grant_ref"

and decode_put_task_grant_request (csil_c : Cbor.t) : put_task_grant_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        grantee_id = (Cbor.to_text (csil_req "grantee_id"));
        access_level = (decode_access_level (csil_req "access_level"));
        grantee_type = (decode_grantee_type (csil_req "grantee_type"));
      }
  | _ -> failwith "csilgen: expected map for put_task_grant_request"

and decode_set_task_visibility_request (csil_c : Cbor.t) : set_task_visibility_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        task_id = (Cbor.to_text (csil_req "task_id"));
        visibility = (decode_access_level (csil_req "visibility"));
      }
  | _ -> failwith "csilgen: expected map for set_task_visibility_request"

and decode_project_grant_ref (csil_c : Cbor.t) : project_grant_ref =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        grantee_id = (Cbor.to_text (csil_req "grantee_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
        grantee_type = (decode_grantee_type (csil_req "grantee_type"));
      }
  | _ -> failwith "csilgen: expected map for project_grant_ref"

and decode_put_project_grant_request (csil_c : Cbor.t) : put_project_grant_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        grantee_id = (Cbor.to_text (csil_req "grantee_id"));
        project_id = (Cbor.to_text (csil_req "project_id"));
        access_level = (decode_access_level (csil_req "access_level"));
        grantee_type = (decode_grantee_type (csil_req "grantee_type"));
      }
  | _ -> failwith "csilgen: expected map for put_project_grant_request"

and decode_set_project_visibility_request (csil_c : Cbor.t) : set_project_visibility_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        project_id = (Cbor.to_text (csil_req "project_id"));
        visibility = (decode_access_level (csil_req "visibility"));
      }
  | _ -> failwith "csilgen: expected map for set_project_visibility_request"

and decode_effective_settings (csil_c : Cbor.t) : effective_settings =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        bug_reports_enabled = (match csil_field "bug_reports_enabled" with Some csil_v -> Some (Cbor.to_bool csil_v) | None -> None);
        bug_reports_project_id = (match csil_field "bug_reports_project_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        default_project_visibility = (match csil_field "default_project_visibility" with Some csil_v -> Some (decode_access_level csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for effective_settings"

and decode_update_settings_request (csil_c : Cbor.t) : update_settings_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        house_id = (Cbor.to_text (csil_req "house_id"));
        settings = (decode_effective_settings (csil_req "settings"));
      }
  | _ -> failwith "csilgen: expected map for update_settings_request"

and decode_bug_report_request (csil_c : Cbor.t) : bug_report_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        title = (Cbor.to_text (csil_req "title"));
        house_id = (Cbor.to_text (csil_req "house_id"));
        description = (match csil_field "description" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for bug_report_request"

and decode_service_error (csil_c : Cbor.t) : service_error =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        code = (Cbor.to_i64 (csil_req "code"));
        message = (Cbor.to_text (csil_req "message"));
      }
  | _ -> failwith "csilgen: expected map for service_error"

and decode_audit_entry (csil_c : Cbor.t) : audit_entry =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        after = (match csil_field "after" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        action = (Cbor.to_text (csil_req "action"));
        before = (match csil_field "before" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        detail = (match csil_field "detail" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        method_ = (Cbor.to_text (csil_req "method"));
        outcome = (Cbor.to_text (csil_req "outcome"));
        audit_id = (Cbor.to_text (csil_req "audit_id"));
        house_id = (match csil_field "house_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        created_at = (Cbor.to_text (csil_req "created_at"));
        resource_id = (match csil_field "resource_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        actor_domain = (Cbor.to_text (csil_req "actor_domain"));
        service_name = (Cbor.to_text (csil_req "service_name"));
        actor_user_id = (Cbor.to_text (csil_req "actor_user_id"));
        resource_type = (match csil_field "resource_type" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        actor_member_id = (match csil_field "actor_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for audit_entry"

and decode_audit_query (csil_c : Cbor.t) : audit_query =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        limit = (match csil_field "limit" with Some csil_v -> Some (Cbor.to_i64 csil_v) | None -> None);
        since = (match csil_field "since" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        until = (match csil_field "until" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        action = (match csil_field "action" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        cursor = (match csil_field "cursor" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        resource_type = (match csil_field "resource_type" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        actor_member_id = (match csil_field "actor_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for audit_query"

and decode_audit_page (csil_c : Cbor.t) : audit_page =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        entries = (match (csil_req "entries") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_audit_entry csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        next_cursor = (match csil_field "next_cursor" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for audit_page"

and decode_trash_item (csil_c : Cbor.t) : trash_item =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        title = (match csil_field "title" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        house_id = (Cbor.to_text (csil_req "house_id"));
        deleted_at = (Cbor.to_text (csil_req "deleted_at"));
        resource_id = (Cbor.to_text (csil_req "resource_id"));
        deleted_op_id = (Cbor.to_text (csil_req "deleted_op_id"));
        resource_type = (Cbor.to_text (csil_req "resource_type"));
        deleted_by_member_id = (match csil_field "deleted_by_member_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for trash_item"

and decode_trash_page (csil_c : Cbor.t) : trash_page =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        items = (match (csil_req "items") with Cbor.Array csil_xs -> List.map (fun csil_e -> (decode_trash_item csil_e)) csil_xs | _ -> failwith "csilgen: expected array");
        next_cursor = (match csil_field "next_cursor" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for trash_page"

and decode_restore_request (csil_c : Cbor.t) : restore_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        house_id = (Cbor.to_text (csil_req "house_id"));
        resource_id = (match csil_field "resource_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        deleted_op_id = (match csil_field "deleted_op_id" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
        resource_type = (match csil_field "resource_type" with Some csil_v -> Some (Cbor.to_text csil_v) | None -> None);
      }
  | _ -> failwith "csilgen: expected map for restore_request"

and decode_purge_request (csil_c : Cbor.t) : purge_request =
  match csil_c with
  | Cbor.Map csil_kvs ->
      let csil_field k = List.assoc_opt (Cbor.Text k) csil_kvs in
      let csil_req k =
        match csil_field k with Some v -> v | None -> failwith ("csilgen: missing field " ^ k)
      in
      ignore csil_req;
      {
        house_id = (Cbor.to_text (csil_req "house_id"));
        resource_id = (Cbor.to_text (csil_req "resource_id"));
        resource_type = (Cbor.to_text (csil_req "resource_type"));
      }
  | _ -> failwith "csilgen: expected map for purge_request"

let encode_task_status_bytes (v : task_status) : bytes = Cbor.encode (encode_task_status v)
let decode_task_status_bytes (b : bytes) : task_status =
  match Cbor.decode b with Ok c -> decode_task_status c | Error e -> failwith e

let encode_target_type_bytes (v : target_type) : bytes = Cbor.encode (encode_target_type v)
let decode_target_type_bytes (b : bytes) : target_type =
  match Cbor.decode b with Ok c -> decode_target_type c | Error e -> failwith e

let encode_access_level_bytes (v : access_level) : bytes = Cbor.encode (encode_access_level v)
let decode_access_level_bytes (b : bytes) : access_level =
  match Cbor.decode b with Ok c -> decode_access_level c | Error e -> failwith e

let encode_grantee_type_bytes (v : grantee_type) : bytes = Cbor.encode (encode_grantee_type v)
let decode_grantee_type_bytes (b : bytes) : grantee_type =
  match Cbor.decode b with Ok c -> decode_grantee_type c | Error e -> failwith e

let encode_resource_type_bytes (v : resource_type) : bytes = Cbor.encode (encode_resource_type v)
let decode_resource_type_bytes (b : bytes) : resource_type =
  match Cbor.decode b with Ok c -> decode_resource_type c | Error e -> failwith e

let encode_project_status_bytes (v : project_status) : bytes = Cbor.encode (encode_project_status v)
let decode_project_status_bytes (b : bytes) : project_status =
  match Cbor.decode b with Ok c -> decode_project_status c | Error e -> failwith e

let encode_dependency_node_type_bytes (v : dependency_node_type) : bytes = Cbor.encode (encode_dependency_node_type v)
let decode_dependency_node_type_bytes (b : bytes) : dependency_node_type =
  match Cbor.decode b with Ok c -> decode_dependency_node_type c | Error e -> failwith e

let encode_recurrence_freq_bytes (v : recurrence_freq) : bytes = Cbor.encode (encode_recurrence_freq v)
let decode_recurrence_freq_bytes (b : bytes) : recurrence_freq =
  match Cbor.decode b with Ok c -> decode_recurrence_freq c | Error e -> failwith e

let encode_milestone_state_bytes (v : milestone_state) : bytes = Cbor.encode (encode_milestone_state v)
let decode_milestone_state_bytes (b : bytes) : milestone_state =
  match Cbor.decode b with Ok c -> decode_milestone_state c | Error e -> failwith e

let encode_house_bytes (v : house) : bytes = Cbor.encode (encode_house v)
let decode_house_bytes (b : bytes) : house =
  match Cbor.decode b with Ok c -> decode_house c | Error e -> failwith e

let encode_member_bytes (v : member) : bytes = Cbor.encode (encode_member v)
let decode_member_bytes (b : bytes) : member =
  match Cbor.decode b with Ok c -> decode_member c | Error e -> failwith e

let encode_trusted_domain_bytes (v : trusted_domain) : bytes = Cbor.encode (encode_trusted_domain v)
let decode_trusted_domain_bytes (b : bytes) : trusted_domain =
  match Cbor.decode b with Ok c -> decode_trusted_domain c | Error e -> failwith e

let encode_role_bytes (v : role) : bytes = Cbor.encode (encode_role v)
let decode_role_bytes (b : bytes) : role =
  match Cbor.decode b with Ok c -> decode_role c | Error e -> failwith e

let encode_member_role_bytes (v : member_role) : bytes = Cbor.encode (encode_member_role v)
let decode_member_role_bytes (b : bytes) : member_role =
  match Cbor.decode b with Ok c -> decode_member_role c | Error e -> failwith e

let encode_member_audit_bytes (v : member_audit) : bytes = Cbor.encode (encode_member_audit v)
let decode_member_audit_bytes (b : bytes) : member_audit =
  match Cbor.decode b with Ok c -> decode_member_audit c | Error e -> failwith e

let encode_skill_bytes (v : skill) : bytes = Cbor.encode (encode_skill v)
let decode_skill_bytes (b : bytes) : skill =
  match Cbor.decode b with Ok c -> decode_skill c | Error e -> failwith e

let encode_member_skill_bytes (v : member_skill) : bytes = Cbor.encode (encode_member_skill v)
let decode_member_skill_bytes (b : bytes) : member_skill =
  match Cbor.decode b with Ok c -> decode_member_skill c | Error e -> failwith e

let encode_group_skill_bytes (v : group_skill) : bytes = Cbor.encode (encode_group_skill v)
let decode_group_skill_bytes (b : bytes) : group_skill =
  match Cbor.decode b with Ok c -> decode_group_skill c | Error e -> failwith e

let encode_group_bytes (v : group) : bytes = Cbor.encode (encode_group v)
let decode_group_bytes (b : bytes) : group =
  match Cbor.decode b with Ok c -> decode_group c | Error e -> failwith e

let encode_group_member_bytes (v : group_member) : bytes = Cbor.encode (encode_group_member v)
let decode_group_member_bytes (b : bytes) : group_member =
  match Cbor.decode b with Ok c -> decode_group_member c | Error e -> failwith e

let encode_project_bytes (v : project) : bytes = Cbor.encode (encode_project v)
let decode_project_bytes (b : bytes) : project =
  match Cbor.decode b with Ok c -> decode_project c | Error e -> failwith e

let encode_project_task_bytes (v : project_task) : bytes = Cbor.encode (encode_project_task v)
let decode_project_task_bytes (b : bytes) : project_task =
  match Cbor.decode b with Ok c -> decode_project_task c | Error e -> failwith e

let encode_project_member_bytes (v : project_member) : bytes = Cbor.encode (encode_project_member v)
let decode_project_member_bytes (b : bytes) : project_member =
  match Cbor.decode b with Ok c -> decode_project_member c | Error e -> failwith e

let encode_project_owner_bytes (v : project_owner) : bytes = Cbor.encode (encode_project_owner v)
let decode_project_owner_bytes (b : bytes) : project_owner =
  match Cbor.decode b with Ok c -> decode_project_owner c | Error e -> failwith e

let encode_milestone_bytes (v : milestone) : bytes = Cbor.encode (encode_milestone v)
let decode_milestone_bytes (b : bytes) : milestone =
  match Cbor.decode b with Ok c -> decode_milestone c | Error e -> failwith e

let encode_event_bytes (v : event) : bytes = Cbor.encode (encode_event v)
let decode_event_bytes (b : bytes) : event =
  match Cbor.decode b with Ok c -> decode_event c | Error e -> failwith e

let encode_task_bytes (v : task) : bytes = Cbor.encode (encode_task v)
let decode_task_bytes (b : bytes) : task =
  match Cbor.decode b with Ok c -> decode_task c | Error e -> failwith e

let encode_comment_bytes (v : comment) : bytes = Cbor.encode (encode_comment v)
let decode_comment_bytes (b : bytes) : comment =
  match Cbor.decode b with Ok c -> decode_comment c | Error e -> failwith e

let encode_share_bytes (v : share) : bytes = Cbor.encode (encode_share v)
let decode_share_bytes (b : bytes) : share =
  match Cbor.decode b with Ok c -> decode_share c | Error e -> failwith e

let encode_house_summary_bytes (v : house_summary) : bytes = Cbor.encode (encode_house_summary v)
let decode_house_summary_bytes (b : bytes) : house_summary =
  match Cbor.decode b with Ok c -> decode_house_summary c | Error e -> failwith e

let encode_house_roles_bytes (v : house_roles) : bytes = Cbor.encode (encode_house_roles v)
let decode_house_roles_bytes (b : bytes) : house_roles =
  match Cbor.decode b with Ok c -> decode_house_roles c | Error e -> failwith e

let encode_identity_bytes (v : identity) : bytes = Cbor.encode (encode_identity v)
let decode_identity_bytes (b : bytes) : identity =
  match Cbor.decode b with Ok c -> decode_identity c | Error e -> failwith e

let encode_login_request_bytes (v : login_request) : bytes = Cbor.encode (encode_login_request v)
let decode_login_request_bytes (b : bytes) : login_request =
  match Cbor.decode b with Ok c -> decode_login_request c | Error e -> failwith e

let encode_complete_request_bytes (v : complete_request) : bytes = Cbor.encode (encode_complete_request v)
let decode_complete_request_bytes (b : bytes) : complete_request =
  match Cbor.decode b with Ok c -> decode_complete_request c | Error e -> failwith e

let encode_login_response_bytes (v : login_response) : bytes = Cbor.encode (encode_login_response v)
let decode_login_response_bytes (b : bytes) : login_response =
  match Cbor.decode b with Ok c -> decode_login_response c | Error e -> failwith e

let encode_dev_user_entry_bytes (v : dev_user_entry) : bytes = Cbor.encode (encode_dev_user_entry v)
let decode_dev_user_entry_bytes (b : bytes) : dev_user_entry =
  match Cbor.decode b with Ok c -> decode_dev_user_entry c | Error e -> failwith e

let encode_dev_users_response_bytes (v : dev_users_response) : bytes = Cbor.encode (encode_dev_users_response v)
let decode_dev_users_response_bytes (b : bytes) : dev_users_response =
  match Cbor.decode b with Ok c -> decode_dev_users_response c | Error e -> failwith e

let encode_dev_login_request_bytes (v : dev_login_request) : bytes = Cbor.encode (encode_dev_login_request v)
let decode_dev_login_request_bytes (b : bytes) : dev_login_request =
  match Cbor.decode b with Ok c -> decode_dev_login_request c | Error e -> failwith e

let encode_me_response_bytes (v : me_response) : bytes = Cbor.encode (encode_me_response v)
let decode_me_response_bytes (b : bytes) : me_response =
  match Cbor.decode b with Ok c -> decode_me_response c | Error e -> failwith e

let encode_empty_request_bytes (v : empty_request) : bytes = Cbor.encode (encode_empty_request v)
let decode_empty_request_bytes (b : bytes) : empty_request =
  match Cbor.decode b with Ok c -> decode_empty_request c | Error e -> failwith e

let encode_empty_response_bytes (v : empty_response) : bytes = Cbor.encode (encode_empty_response v)
let decode_empty_response_bytes (b : bytes) : empty_response =
  match Cbor.decode b with Ok c -> decode_empty_response c | Error e -> failwith e

let encode_bool_response_bytes (v : bool_response) : bytes = Cbor.encode (encode_bool_response v)
let decode_bool_response_bytes (b : bytes) : bool_response =
  match Cbor.decode b with Ok c -> decode_bool_response c | Error e -> failwith e

let encode_house_list_request_bytes (v : house_list_request) : bytes = Cbor.encode (encode_house_list_request v)
let decode_house_list_request_bytes (b : bytes) : house_list_request =
  match Cbor.decode b with Ok c -> decode_house_list_request c | Error e -> failwith e

let encode_house_scoped_list_request_bytes (v : house_scoped_list_request) : bytes = Cbor.encode (encode_house_scoped_list_request v)
let decode_house_scoped_list_request_bytes (b : bytes) : house_scoped_list_request =
  match Cbor.decode b with Ok c -> decode_house_scoped_list_request c | Error e -> failwith e

let encode_task_list_bytes (v : task_list) : bytes = Cbor.encode (encode_task_list v)
let decode_task_list_bytes (b : bytes) : task_list =
  match Cbor.decode b with Ok c -> decode_task_list c | Error e -> failwith e

let encode_project_list_bytes (v : project_list) : bytes = Cbor.encode (encode_project_list v)
let decode_project_list_bytes (b : bytes) : project_list =
  match Cbor.decode b with Ok c -> decode_project_list c | Error e -> failwith e

let encode_member_scoped_list_request_bytes (v : member_scoped_list_request) : bytes = Cbor.encode (encode_member_scoped_list_request v)
let decode_member_scoped_list_request_bytes (b : bytes) : member_scoped_list_request =
  match Cbor.decode b with Ok c -> decode_member_scoped_list_request c | Error e -> failwith e

let encode_project_scoped_list_request_bytes (v : project_scoped_list_request) : bytes = Cbor.encode (encode_project_scoped_list_request v)
let decode_project_scoped_list_request_bytes (b : bytes) : project_scoped_list_request =
  match Cbor.decode b with Ok c -> decode_project_scoped_list_request c | Error e -> failwith e

let encode_comment_list_request_bytes (v : comment_list_request) : bytes = Cbor.encode (encode_comment_list_request v)
let decode_comment_list_request_bytes (b : bytes) : comment_list_request =
  match Cbor.decode b with Ok c -> decode_comment_list_request c | Error e -> failwith e

let encode_notification_bytes (v : notification) : bytes = Cbor.encode (encode_notification v)
let decode_notification_bytes (b : bytes) : notification =
  match Cbor.decode b with Ok c -> decode_notification c | Error e -> failwith e

let encode_notification_list_request_bytes (v : notification_list_request) : bytes = Cbor.encode (encode_notification_list_request v)
let decode_notification_list_request_bytes (b : bytes) : notification_list_request =
  match Cbor.decode b with Ok c -> decode_notification_list_request c | Error e -> failwith e

let encode_notification_unread_count_bytes (v : notification_unread_count) : bytes = Cbor.encode (encode_notification_unread_count v)
let decode_notification_unread_count_bytes (b : bytes) : notification_unread_count =
  match Cbor.decode b with Ok c -> decode_notification_unread_count c | Error e -> failwith e

let encode_share_access_request_bytes (v : share_access_request) : bytes = Cbor.encode (encode_share_access_request v)
let decode_share_access_request_bytes (b : bytes) : share_access_request =
  match Cbor.decode b with Ok c -> decode_share_access_request c | Error e -> failwith e

let encode_resource_ref_bytes (v : resource_ref) : bytes = Cbor.encode (encode_resource_ref v)
let decode_resource_ref_bytes (b : bytes) : resource_ref =
  match Cbor.decode b with Ok c -> decode_resource_ref c | Error e -> failwith e

let encode_member_role_ref_bytes (v : member_role_ref) : bytes = Cbor.encode (encode_member_role_ref v)
let decode_member_role_ref_bytes (b : bytes) : member_role_ref =
  match Cbor.decode b with Ok c -> decode_member_role_ref c | Error e -> failwith e

let encode_member_skill_ref_bytes (v : member_skill_ref) : bytes = Cbor.encode (encode_member_skill_ref v)
let decode_member_skill_ref_bytes (b : bytes) : member_skill_ref =
  match Cbor.decode b with Ok c -> decode_member_skill_ref c | Error e -> failwith e

let encode_group_skill_ref_bytes (v : group_skill_ref) : bytes = Cbor.encode (encode_group_skill_ref v)
let decode_group_skill_ref_bytes (b : bytes) : group_skill_ref =
  match Cbor.decode b with Ok c -> decode_group_skill_ref c | Error e -> failwith e

let encode_group_member_ref_bytes (v : group_member_ref) : bytes = Cbor.encode (encode_group_member_ref v)
let decode_group_member_ref_bytes (b : bytes) : group_member_ref =
  match Cbor.decode b with Ok c -> decode_group_member_ref c | Error e -> failwith e

let encode_project_task_ref_bytes (v : project_task_ref) : bytes = Cbor.encode (encode_project_task_ref v)
let decode_project_task_ref_bytes (b : bytes) : project_task_ref =
  match Cbor.decode b with Ok c -> decode_project_task_ref c | Error e -> failwith e

let encode_project_task_order_request_bytes (v : project_task_order_request) : bytes = Cbor.encode (encode_project_task_order_request v)
let decode_project_task_order_request_bytes (b : bytes) : project_task_order_request =
  match Cbor.decode b with Ok c -> decode_project_task_order_request c | Error e -> failwith e

let encode_project_member_ref_bytes (v : project_member_ref) : bytes = Cbor.encode (encode_project_member_ref v)
let decode_project_member_ref_bytes (b : bytes) : project_member_ref =
  match Cbor.decode b with Ok c -> decode_project_member_ref c | Error e -> failwith e

let encode_project_owner_ref_bytes (v : project_owner_ref) : bytes = Cbor.encode (encode_project_owner_ref v)
let decode_project_owner_ref_bytes (b : bytes) : project_owner_ref =
  match Cbor.decode b with Ok c -> decode_project_owner_ref c | Error e -> failwith e

let encode_dependency_ref_bytes (v : dependency_ref) : bytes = Cbor.encode (encode_dependency_ref v)
let decode_dependency_ref_bytes (b : bytes) : dependency_ref =
  match Cbor.decode b with Ok c -> decode_dependency_ref c | Error e -> failwith e

let encode_dependency_target_bytes (v : dependency_target) : bytes = Cbor.encode (encode_dependency_target v)
let decode_dependency_target_bytes (b : bytes) : dependency_target =
  match Cbor.decode b with Ok c -> decode_dependency_target c | Error e -> failwith e

let encode_dependency_node_bytes (v : dependency_node) : bytes = Cbor.encode (encode_dependency_node v)
let decode_dependency_node_bytes (b : bytes) : dependency_node =
  match Cbor.decode b with Ok c -> decode_dependency_node c | Error e -> failwith e

let encode_dependency_graph_bytes (v : dependency_graph) : bytes = Cbor.encode (encode_dependency_graph v)
let decode_dependency_graph_bytes (b : bytes) : dependency_graph =
  match Cbor.decode b with Ok c -> decode_dependency_graph c | Error e -> failwith e

let encode_grant_bytes (v : grant) : bytes = Cbor.encode (encode_grant v)
let decode_grant_bytes (b : bytes) : grant =
  match Cbor.decode b with Ok c -> decode_grant c | Error e -> failwith e

let encode_task_grant_ref_bytes (v : task_grant_ref) : bytes = Cbor.encode (encode_task_grant_ref v)
let decode_task_grant_ref_bytes (b : bytes) : task_grant_ref =
  match Cbor.decode b with Ok c -> decode_task_grant_ref c | Error e -> failwith e

let encode_put_task_grant_request_bytes (v : put_task_grant_request) : bytes = Cbor.encode (encode_put_task_grant_request v)
let decode_put_task_grant_request_bytes (b : bytes) : put_task_grant_request =
  match Cbor.decode b with Ok c -> decode_put_task_grant_request c | Error e -> failwith e

let encode_set_task_visibility_request_bytes (v : set_task_visibility_request) : bytes = Cbor.encode (encode_set_task_visibility_request v)
let decode_set_task_visibility_request_bytes (b : bytes) : set_task_visibility_request =
  match Cbor.decode b with Ok c -> decode_set_task_visibility_request c | Error e -> failwith e

let encode_project_grant_ref_bytes (v : project_grant_ref) : bytes = Cbor.encode (encode_project_grant_ref v)
let decode_project_grant_ref_bytes (b : bytes) : project_grant_ref =
  match Cbor.decode b with Ok c -> decode_project_grant_ref c | Error e -> failwith e

let encode_put_project_grant_request_bytes (v : put_project_grant_request) : bytes = Cbor.encode (encode_put_project_grant_request v)
let decode_put_project_grant_request_bytes (b : bytes) : put_project_grant_request =
  match Cbor.decode b with Ok c -> decode_put_project_grant_request c | Error e -> failwith e

let encode_set_project_visibility_request_bytes (v : set_project_visibility_request) : bytes = Cbor.encode (encode_set_project_visibility_request v)
let decode_set_project_visibility_request_bytes (b : bytes) : set_project_visibility_request =
  match Cbor.decode b with Ok c -> decode_set_project_visibility_request c | Error e -> failwith e

let encode_effective_settings_bytes (v : effective_settings) : bytes = Cbor.encode (encode_effective_settings v)
let decode_effective_settings_bytes (b : bytes) : effective_settings =
  match Cbor.decode b with Ok c -> decode_effective_settings c | Error e -> failwith e

let encode_update_settings_request_bytes (v : update_settings_request) : bytes = Cbor.encode (encode_update_settings_request v)
let decode_update_settings_request_bytes (b : bytes) : update_settings_request =
  match Cbor.decode b with Ok c -> decode_update_settings_request c | Error e -> failwith e

let encode_bug_report_request_bytes (v : bug_report_request) : bytes = Cbor.encode (encode_bug_report_request v)
let decode_bug_report_request_bytes (b : bytes) : bug_report_request =
  match Cbor.decode b with Ok c -> decode_bug_report_request c | Error e -> failwith e

let encode_service_error_bytes (v : service_error) : bytes = Cbor.encode (encode_service_error v)
let decode_service_error_bytes (b : bytes) : service_error =
  match Cbor.decode b with Ok c -> decode_service_error c | Error e -> failwith e

let encode_audit_entry_bytes (v : audit_entry) : bytes = Cbor.encode (encode_audit_entry v)
let decode_audit_entry_bytes (b : bytes) : audit_entry =
  match Cbor.decode b with Ok c -> decode_audit_entry c | Error e -> failwith e

let encode_audit_query_bytes (v : audit_query) : bytes = Cbor.encode (encode_audit_query v)
let decode_audit_query_bytes (b : bytes) : audit_query =
  match Cbor.decode b with Ok c -> decode_audit_query c | Error e -> failwith e

let encode_audit_page_bytes (v : audit_page) : bytes = Cbor.encode (encode_audit_page v)
let decode_audit_page_bytes (b : bytes) : audit_page =
  match Cbor.decode b with Ok c -> decode_audit_page c | Error e -> failwith e

let encode_trash_item_bytes (v : trash_item) : bytes = Cbor.encode (encode_trash_item v)
let decode_trash_item_bytes (b : bytes) : trash_item =
  match Cbor.decode b with Ok c -> decode_trash_item c | Error e -> failwith e

let encode_trash_page_bytes (v : trash_page) : bytes = Cbor.encode (encode_trash_page v)
let decode_trash_page_bytes (b : bytes) : trash_page =
  match Cbor.decode b with Ok c -> decode_trash_page c | Error e -> failwith e

let encode_restore_request_bytes (v : restore_request) : bytes = Cbor.encode (encode_restore_request v)
let decode_restore_request_bytes (b : bytes) : restore_request =
  match Cbor.decode b with Ok c -> decode_restore_request c | Error e -> failwith e

let encode_purge_request_bytes (v : purge_request) : bytes = Cbor.encode (encode_purge_request v)
let decode_purge_request_bytes (b : bytes) : purge_request =
  match Cbor.decode b with Ok c -> decode_purge_request c | Error e -> failwith e
