(* Code generated by csilgen; DO NOT EDIT. *)

open Types

(* The transport seam is supplied by the consumer: [call] performs the RPC named
   by (service, op) over its carrier and returns the raw reply payload, or an
   error string. The generated client owns serialization (it encodes the typed
   request and decodes the typed reply via [Codec]); the carrier only moves bytes. *)
type client = {
  call : service:string -> op:string -> payload:bytes -> (bytes, string) result;
}

let make_client ~call = { call }

module Auth_service = struct
  let login (c : client) (req : login_request) : (login_response, string) result =
    match c.call ~service:"auth" ~op:"Login" ~payload:(Codec.encode_login_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_login_response_bytes payload)
    | Error _ as e -> e

  let complete (c : client) (req : complete_request) : (login_response, string) result =
    match c.call ~service:"auth" ~op:"Complete" ~payload:(Codec.encode_complete_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_login_response_bytes payload)
    | Error _ as e -> e

  let refresh (c : client) (req : empty_request) : (login_response, string) result =
    match c.call ~service:"auth" ~op:"Refresh" ~payload:(Codec.encode_empty_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_login_response_bytes payload)
    | Error _ as e -> e

  let logout (c : client) (req : empty_request) : (empty_response, string) result =
    match c.call ~service:"auth" ~op:"Logout" ~payload:(Codec.encode_empty_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let me (c : client) (req : empty_request) : (me_response, string) result =
    match c.call ~service:"auth" ~op:"Me" ~payload:(Codec.encode_empty_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_me_response_bytes payload)
    | Error _ as e -> e
end

module Dev_auth_service = struct
  let list_dev_users (c : client) (req : empty_request) : (dev_users_response, string) result =
    match c.call ~service:"devauth" ~op:"ListDevUsers" ~payload:(Codec.encode_empty_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_dev_users_response_bytes payload)
    | Error _ as e -> e

  let dev_login (c : client) (req : dev_login_request) : (login_response, string) result =
    match c.call ~service:"devauth" ~op:"DevLogin" ~payload:(Codec.encode_dev_login_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_login_response_bytes payload)
    | Error _ as e -> e
end

module House_service = struct
  let create_house (c : client) (req : house) : (house, string) result =
    match c.call ~service:"house" ~op:"CreateHouse" ~payload:(Codec.encode_house_bytes req) with
    | Ok payload -> Ok (Codec.decode_house_bytes payload)
    | Error _ as e -> e

  let get_house (c : client)
      ~(encode_request : house_id -> bytes)
      (req : house_id) : (house, string) result =
    match c.call ~service:"house" ~op:"GetHouse" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_house_bytes payload)
    | Error _ as e -> e

  let update_house (c : client) (req : house) : (house, string) result =
    match c.call ~service:"house" ~op:"UpdateHouse" ~payload:(Codec.encode_house_bytes req) with
    | Ok payload -> Ok (Codec.decode_house_bytes payload)
    | Error _ as e -> e

  let delete_house (c : client)
      ~(encode_request : house_id -> bytes)
      (req : house_id) : (empty_response, string) result =
    match c.call ~service:"house" ~op:"DeleteHouse" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_houses (c : client)
      ~(decode_response : bytes -> house list)
      (req : house_list_request) : (house list, string) result =
    match c.call ~service:"house" ~op:"ListHouses" ~payload:(Codec.encode_house_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Member_service = struct
  let create_member (c : client) (req : member) : (member, string) result =
    match c.call ~service:"member" ~op:"CreateMember" ~payload:(Codec.encode_member_bytes req) with
    | Ok payload -> Ok (Codec.decode_member_bytes payload)
    | Error _ as e -> e

  let get_member (c : client)
      ~(encode_request : member_id -> bytes)
      (req : member_id) : (member, string) result =
    match c.call ~service:"member" ~op:"GetMember" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_member_bytes payload)
    | Error _ as e -> e

  let get_member_by_identity (c : client) (req : member) : (member, string) result =
    match c.call ~service:"member" ~op:"GetMemberByIdentity" ~payload:(Codec.encode_member_bytes req) with
    | Ok payload -> Ok (Codec.decode_member_bytes payload)
    | Error _ as e -> e

  let update_member (c : client) (req : member) : (member, string) result =
    match c.call ~service:"member" ~op:"UpdateMember" ~payload:(Codec.encode_member_bytes req) with
    | Ok payload -> Ok (Codec.decode_member_bytes payload)
    | Error _ as e -> e

  let deactivate_member (c : client)
      ~(encode_request : member_id -> bytes)
      (req : member_id) : (empty_response, string) result =
    match c.call ~service:"member" ~op:"DeactivateMember" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let reactivate_member (c : client)
      ~(encode_request : member_id -> bytes)
      (req : member_id) : (empty_response, string) result =
    match c.call ~service:"member" ~op:"ReactivateMember" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_members (c : client)
      ~(decode_response : bytes -> member list)
      (req : house_scoped_list_request) : (member list, string) result =
    match c.call ~service:"member" ~op:"ListMembers" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Trusted_domain_service = struct
  let add_trusted_domain (c : client) (req : trusted_domain) : (trusted_domain, string) result =
    match c.call ~service:"trusteddomain" ~op:"AddTrustedDomain" ~payload:(Codec.encode_trusted_domain_bytes req) with
    | Ok payload -> Ok (Codec.decode_trusted_domain_bytes payload)
    | Error _ as e -> e

  let remove_trusted_domain (c : client)
      ~(encode_request : trusted_domain_id -> bytes)
      (req : trusted_domain_id) : (empty_response, string) result =
    match c.call ~service:"trusteddomain" ~op:"RemoveTrustedDomain" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_trusted_domains (c : client)
      ~(encode_request : house_id -> bytes)
      ~(decode_response : bytes -> trusted_domain list)
      (req : house_id) : (trusted_domain list, string) result =
    match c.call ~service:"trusteddomain" ~op:"ListTrustedDomains" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let is_domain_trusted (c : client) (req : trusted_domain) : (bool_response, string) result =
    match c.call ~service:"trusteddomain" ~op:"IsDomainTrusted" ~payload:(Codec.encode_trusted_domain_bytes req) with
    | Ok payload -> Ok (Codec.decode_bool_response_bytes payload)
    | Error _ as e -> e
end

module Role_service = struct
  let create_role (c : client) (req : role) : (role, string) result =
    match c.call ~service:"role" ~op:"CreateRole" ~payload:(Codec.encode_role_bytes req) with
    | Ok payload -> Ok (Codec.decode_role_bytes payload)
    | Error _ as e -> e

  let update_role (c : client) (req : role) : (role, string) result =
    match c.call ~service:"role" ~op:"UpdateRole" ~payload:(Codec.encode_role_bytes req) with
    | Ok payload -> Ok (Codec.decode_role_bytes payload)
    | Error _ as e -> e

  let delete_role (c : client)
      ~(encode_request : role_id -> bytes)
      (req : role_id) : (empty_response, string) result =
    match c.call ~service:"role" ~op:"DeleteRole" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_roles (c : client)
      ~(decode_response : bytes -> role list)
      (req : house_scoped_list_request) : (role list, string) result =
    match c.call ~service:"role" ~op:"ListRoles" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let grant_role (c : client) (req : member_role_ref) : (empty_response, string) result =
    match c.call ~service:"role" ~op:"GrantRole" ~payload:(Codec.encode_member_role_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let revoke_role (c : client) (req : member_role_ref) : (empty_response, string) result =
    match c.call ~service:"role" ~op:"RevokeRole" ~payload:(Codec.encode_member_role_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_member_roles (c : client)
      ~(decode_response : bytes -> role list)
      (req : member_scoped_list_request) : (role list, string) result =
    match c.call ~service:"role" ~op:"ListMemberRoles" ~payload:(Codec.encode_member_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Skill_service = struct
  let create_skill (c : client) (req : skill) : (skill, string) result =
    match c.call ~service:"skill" ~op:"CreateSkill" ~payload:(Codec.encode_skill_bytes req) with
    | Ok payload -> Ok (Codec.decode_skill_bytes payload)
    | Error _ as e -> e

  let update_skill (c : client) (req : skill) : (skill, string) result =
    match c.call ~service:"skill" ~op:"UpdateSkill" ~payload:(Codec.encode_skill_bytes req) with
    | Ok payload -> Ok (Codec.decode_skill_bytes payload)
    | Error _ as e -> e

  let delete_skill (c : client)
      ~(encode_request : skill_id -> bytes)
      (req : skill_id) : (empty_response, string) result =
    match c.call ~service:"skill" ~op:"DeleteSkill" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_skills (c : client)
      ~(decode_response : bytes -> skill list)
      (req : house_scoped_list_request) : (skill list, string) result =
    match c.call ~service:"skill" ~op:"ListSkills" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let add_member_skill (c : client) (req : member_skill_ref) : (empty_response, string) result =
    match c.call ~service:"skill" ~op:"AddMemberSkill" ~payload:(Codec.encode_member_skill_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_member_skill (c : client) (req : member_skill_ref) : (empty_response, string) result =
    match c.call ~service:"skill" ~op:"RemoveMemberSkill" ~payload:(Codec.encode_member_skill_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_member_skills (c : client)
      ~(decode_response : bytes -> skill list)
      (req : member_scoped_list_request) : (skill list, string) result =
    match c.call ~service:"skill" ~op:"ListMemberSkills" ~payload:(Codec.encode_member_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let add_group_skill (c : client) (req : group_skill_ref) : (empty_response, string) result =
    match c.call ~service:"skill" ~op:"AddGroupSkill" ~payload:(Codec.encode_group_skill_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_group_skill (c : client) (req : group_skill_ref) : (empty_response, string) result =
    match c.call ~service:"skill" ~op:"RemoveGroupSkill" ~payload:(Codec.encode_group_skill_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_group_skills (c : client)
      ~(encode_request : group_id -> bytes)
      ~(decode_response : bytes -> skill list)
      (req : group_id) : (skill list, string) result =
    match c.call ~service:"skill" ~op:"ListGroupSkills" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Group_service = struct
  let create_group (c : client) (req : group) : (group, string) result =
    match c.call ~service:"group" ~op:"CreateGroup" ~payload:(Codec.encode_group_bytes req) with
    | Ok payload -> Ok (Codec.decode_group_bytes payload)
    | Error _ as e -> e

  let update_group (c : client) (req : group) : (group, string) result =
    match c.call ~service:"group" ~op:"UpdateGroup" ~payload:(Codec.encode_group_bytes req) with
    | Ok payload -> Ok (Codec.decode_group_bytes payload)
    | Error _ as e -> e

  let delete_group (c : client)
      ~(encode_request : group_id -> bytes)
      (req : group_id) : (empty_response, string) result =
    match c.call ~service:"group" ~op:"DeleteGroup" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_groups (c : client)
      ~(decode_response : bytes -> group list)
      (req : house_scoped_list_request) : (group list, string) result =
    match c.call ~service:"group" ~op:"ListGroups" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let add_group_member (c : client) (req : group_member_ref) : (empty_response, string) result =
    match c.call ~service:"group" ~op:"AddGroupMember" ~payload:(Codec.encode_group_member_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_group_member (c : client) (req : group_member_ref) : (empty_response, string) result =
    match c.call ~service:"group" ~op:"RemoveGroupMember" ~payload:(Codec.encode_group_member_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_group_members (c : client)
      ~(decode_response : bytes -> member list)
      (req : member_scoped_list_request) : (member list, string) result =
    match c.call ~service:"group" ~op:"ListGroupMembers" ~payload:(Codec.encode_member_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Project_service = struct
  let create_project (c : client) (req : project) : (project, string) result =
    match c.call ~service:"project" ~op:"CreateProject" ~payload:(Codec.encode_project_bytes req) with
    | Ok payload -> Ok (Codec.decode_project_bytes payload)
    | Error _ as e -> e

  let get_project (c : client)
      ~(encode_request : project_id -> bytes)
      (req : project_id) : (project, string) result =
    match c.call ~service:"project" ~op:"GetProject" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_project_bytes payload)
    | Error _ as e -> e

  let update_project (c : client) (req : project) : (project, string) result =
    match c.call ~service:"project" ~op:"UpdateProject" ~payload:(Codec.encode_project_bytes req) with
    | Ok payload -> Ok (Codec.decode_project_bytes payload)
    | Error _ as e -> e

  let delete_project (c : client)
      ~(encode_request : project_id -> bytes)
      (req : project_id) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"DeleteProject" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_projects (c : client) (req : house_scoped_list_request) : (project_list, string) result =
    match c.call ~service:"project" ~op:"ListProjects" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_project_list_bytes payload)
    | Error _ as e -> e

  let list_project_tasks (c : client) (req : project_scoped_list_request) : (task_list, string) result =
    match c.call ~service:"project" ~op:"ListProjectTasks" ~payload:(Codec.encode_project_scoped_list_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_list_bytes payload)
    | Error _ as e -> e

  let add_project_task (c : client) (req : project_task_order_request) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"AddProjectTask" ~payload:(Codec.encode_project_task_order_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_project_task (c : client) (req : project_task_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"RemoveProjectTask" ~payload:(Codec.encode_project_task_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let set_project_task_position (c : client) (req : project_task_order_request) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"SetProjectTaskPosition" ~payload:(Codec.encode_project_task_order_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_project_members (c : client)
      ~(encode_request : project_id -> bytes)
      ~(decode_response : bytes -> member list)
      (req : project_id) : (member list, string) result =
    match c.call ~service:"project" ~op:"ListProjectMembers" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let add_project_member (c : client) (req : project_member_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"AddProjectMember" ~payload:(Codec.encode_project_member_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_project_member (c : client) (req : project_member_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"RemoveProjectMember" ~payload:(Codec.encode_project_member_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_project_owners (c : client)
      ~(encode_request : project_id -> bytes)
      ~(decode_response : bytes -> member list)
      (req : project_id) : (member list, string) result =
    match c.call ~service:"project" ~op:"ListProjectOwners" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let add_project_owner (c : client) (req : project_owner_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"AddProjectOwner" ~payload:(Codec.encode_project_owner_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_project_owner (c : client) (req : project_owner_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"RemoveProjectOwner" ~payload:(Codec.encode_project_owner_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_milestones (c : client)
      ~(encode_request : project_id -> bytes)
      ~(decode_response : bytes -> milestone list)
      (req : project_id) : (milestone list, string) result =
    match c.call ~service:"project" ~op:"ListMilestones" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let create_milestone (c : client) (req : milestone) : (milestone, string) result =
    match c.call ~service:"project" ~op:"CreateMilestone" ~payload:(Codec.encode_milestone_bytes req) with
    | Ok payload -> Ok (Codec.decode_milestone_bytes payload)
    | Error _ as e -> e

  let update_milestone (c : client) (req : milestone) : (milestone, string) result =
    match c.call ~service:"project" ~op:"UpdateMilestone" ~payload:(Codec.encode_milestone_bytes req) with
    | Ok payload -> Ok (Codec.decode_milestone_bytes payload)
    | Error _ as e -> e

  let delete_milestone (c : client)
      ~(encode_request : milestone_id -> bytes)
      (req : milestone_id) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"DeleteMilestone" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let set_project_visibility (c : client) (req : set_project_visibility_request) : (project, string) result =
    match c.call ~service:"project" ~op:"SetProjectVisibility" ~payload:(Codec.encode_set_project_visibility_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_project_bytes payload)
    | Error _ as e -> e

  let list_project_grants (c : client)
      ~(encode_request : project_id -> bytes)
      ~(decode_response : bytes -> grant list)
      (req : project_id) : (grant list, string) result =
    match c.call ~service:"project" ~op:"ListProjectGrants" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let put_project_grant (c : client) (req : put_project_grant_request) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"PutProjectGrant" ~payload:(Codec.encode_put_project_grant_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let delete_project_grant (c : client) (req : project_grant_ref) : (empty_response, string) result =
    match c.call ~service:"project" ~op:"DeleteProjectGrant" ~payload:(Codec.encode_project_grant_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e
end

module Event_service = struct
  let create_event (c : client) (req : event) : (event, string) result =
    match c.call ~service:"event" ~op:"CreateEvent" ~payload:(Codec.encode_event_bytes req) with
    | Ok payload -> Ok (Codec.decode_event_bytes payload)
    | Error _ as e -> e

  let get_event (c : client)
      ~(encode_request : event_id -> bytes)
      (req : event_id) : (event, string) result =
    match c.call ~service:"event" ~op:"GetEvent" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_event_bytes payload)
    | Error _ as e -> e

  let update_event (c : client) (req : event) : (event, string) result =
    match c.call ~service:"event" ~op:"UpdateEvent" ~payload:(Codec.encode_event_bytes req) with
    | Ok payload -> Ok (Codec.decode_event_bytes payload)
    | Error _ as e -> e

  let delete_event (c : client)
      ~(encode_request : event_id -> bytes)
      (req : event_id) : (empty_response, string) result =
    match c.call ~service:"event" ~op:"DeleteEvent" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let delete_event_and_future (c : client)
      ~(encode_request : event_id -> bytes)
      (req : event_id) : (empty_response, string) result =
    match c.call ~service:"event" ~op:"DeleteEventAndFuture" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_events (c : client)
      ~(decode_response : bytes -> event list)
      (req : house_scoped_list_request) : (event list, string) result =
    match c.call ~service:"event" ~op:"ListEvents" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Task_service = struct
  let create_task (c : client) (req : task) : (task, string) result =
    match c.call ~service:"task" ~op:"CreateTask" ~payload:(Codec.encode_task_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_bytes payload)
    | Error _ as e -> e

  let get_task (c : client)
      ~(encode_request : task_id -> bytes)
      (req : task_id) : (task, string) result =
    match c.call ~service:"task" ~op:"GetTask" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_task_bytes payload)
    | Error _ as e -> e

  let update_task (c : client) (req : task) : (task, string) result =
    match c.call ~service:"task" ~op:"UpdateTask" ~payload:(Codec.encode_task_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_bytes payload)
    | Error _ as e -> e

  let delete_task (c : client)
      ~(encode_request : task_id -> bytes)
      (req : task_id) : (empty_response, string) result =
    match c.call ~service:"task" ~op:"DeleteTask" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_tasks (c : client) (req : house_scoped_list_request) : (task_list, string) result =
    match c.call ~service:"task" ~op:"ListTasks" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_list_bytes payload)
    | Error _ as e -> e

  let set_task_visibility (c : client) (req : set_task_visibility_request) : (task, string) result =
    match c.call ~service:"task" ~op:"SetTaskVisibility" ~payload:(Codec.encode_set_task_visibility_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_bytes payload)
    | Error _ as e -> e

  let list_task_grants (c : client)
      ~(encode_request : task_id -> bytes)
      ~(decode_response : bytes -> grant list)
      (req : task_id) : (grant list, string) result =
    match c.call ~service:"task" ~op:"ListTaskGrants" ~payload:(encode_request req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let put_task_grant (c : client) (req : put_task_grant_request) : (empty_response, string) result =
    match c.call ~service:"task" ~op:"PutTaskGrant" ~payload:(Codec.encode_put_task_grant_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let delete_task_grant (c : client) (req : task_grant_ref) : (empty_response, string) result =
    match c.call ~service:"task" ~op:"DeleteTaskGrant" ~payload:(Codec.encode_task_grant_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e
end

module Dependency_service = struct
  let add_dependency (c : client) (req : dependency_ref) : (empty_response, string) result =
    match c.call ~service:"dependency" ~op:"AddDependency" ~payload:(Codec.encode_dependency_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let remove_dependency (c : client) (req : dependency_ref) : (empty_response, string) result =
    match c.call ~service:"dependency" ~op:"RemoveDependency" ~payload:(Codec.encode_dependency_ref_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let get_dependencies (c : client) (req : dependency_target) : (dependency_graph, string) result =
    match c.call ~service:"dependency" ~op:"GetDependencies" ~payload:(Codec.encode_dependency_target_bytes req) with
    | Ok payload -> Ok (Codec.decode_dependency_graph_bytes payload)
    | Error _ as e -> e
end

module Comment_service = struct
  let create_comment (c : client) (req : comment) : (comment, string) result =
    match c.call ~service:"comment" ~op:"CreateComment" ~payload:(Codec.encode_comment_bytes req) with
    | Ok payload -> Ok (Codec.decode_comment_bytes payload)
    | Error _ as e -> e

  let get_comment (c : client)
      ~(encode_request : comment_id -> bytes)
      (req : comment_id) : (comment, string) result =
    match c.call ~service:"comment" ~op:"GetComment" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_comment_bytes payload)
    | Error _ as e -> e

  let update_comment (c : client) (req : comment) : (comment, string) result =
    match c.call ~service:"comment" ~op:"UpdateComment" ~payload:(Codec.encode_comment_bytes req) with
    | Ok payload -> Ok (Codec.decode_comment_bytes payload)
    | Error _ as e -> e

  let delete_comment (c : client)
      ~(encode_request : comment_id -> bytes)
      (req : comment_id) : (empty_response, string) result =
    match c.call ~service:"comment" ~op:"DeleteComment" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_comments (c : client)
      ~(decode_response : bytes -> comment list)
      (req : comment_list_request) : (comment list, string) result =
    match c.call ~service:"comment" ~op:"ListComments" ~payload:(Codec.encode_comment_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Notification_service = struct
  let list_notifications (c : client)
      ~(decode_response : bytes -> notification list)
      (req : notification_list_request) : (notification list, string) result =
    match c.call ~service:"notification" ~op:"ListNotifications" ~payload:(Codec.encode_notification_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let unread_count (c : client)
      ~(encode_request : house_id -> bytes)
      (req : house_id) : (notification_unread_count, string) result =
    match c.call ~service:"notification" ~op:"UnreadCount" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_notification_unread_count_bytes payload)
    | Error _ as e -> e

  let mark_read (c : client)
      ~(encode_request : notification_id -> bytes)
      (req : notification_id) : (notification, string) result =
    match c.call ~service:"notification" ~op:"MarkRead" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_notification_bytes payload)
    | Error _ as e -> e

  let mark_all_read (c : client)
      ~(encode_request : house_id -> bytes)
      (req : house_id) : (empty_response, string) result =
    match c.call ~service:"notification" ~op:"MarkAllRead" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e
end

module Share_service = struct
  let create_share (c : client) (req : share) : (share, string) result =
    match c.call ~service:"share" ~op:"CreateShare" ~payload:(Codec.encode_share_bytes req) with
    | Ok payload -> Ok (Codec.decode_share_bytes payload)
    | Error _ as e -> e

  let delete_share (c : client)
      ~(encode_request : share_id -> bytes)
      (req : share_id) : (empty_response, string) result =
    match c.call ~service:"share" ~op:"DeleteShare" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let list_shares_by_resource (c : client)
      ~(decode_response : bytes -> share list)
      (req : resource_ref) : (share list, string) result =
    match c.call ~service:"share" ~op:"ListSharesByResource" ~payload:(Codec.encode_resource_ref_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e

  let check_access (c : client) (req : share_access_request) : (share, string) result =
    match c.call ~service:"share" ~op:"CheckAccess" ~payload:(Codec.encode_share_access_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_share_bytes payload)
    | Error _ as e -> e
end

module Member_audit_service = struct
  let list_audits_for_member (c : client)
      ~(decode_response : bytes -> member_audit list)
      (req : member_scoped_list_request) : (member_audit list, string) result =
    match c.call ~service:"memberaudit" ~op:"ListAuditsForMember" ~payload:(Codec.encode_member_scoped_list_request_bytes req) with
    | Ok payload -> Ok (decode_response payload)
    | Error _ as e -> e
end

module Settings_service = struct
  let get_settings (c : client)
      ~(encode_request : house_id -> bytes)
      (req : house_id) : (effective_settings, string) result =
    match c.call ~service:"settings" ~op:"GetSettings" ~payload:(encode_request req) with
    | Ok payload -> Ok (Codec.decode_effective_settings_bytes payload)
    | Error _ as e -> e

  let update_settings (c : client) (req : update_settings_request) : (effective_settings, string) result =
    match c.call ~service:"settings" ~op:"UpdateSettings" ~payload:(Codec.encode_update_settings_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_effective_settings_bytes payload)
    | Error _ as e -> e
end

module Bug_service = struct
  let report_bug (c : client) (req : bug_report_request) : (task, string) result =
    match c.call ~service:"bug" ~op:"ReportBug" ~payload:(Codec.encode_bug_report_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_task_bytes payload)
    | Error _ as e -> e
end

module Audit_service = struct
  let query_audit (c : client) (req : audit_query) : (audit_page, string) result =
    match c.call ~service:"audit" ~op:"QueryAudit" ~payload:(Codec.encode_audit_query_bytes req) with
    | Ok payload -> Ok (Codec.decode_audit_page_bytes payload)
    | Error _ as e -> e
end

module Trash_service = struct
  let list_trash (c : client) (req : house_scoped_list_request) : (trash_page, string) result =
    match c.call ~service:"trash" ~op:"ListTrash" ~payload:(Codec.encode_house_scoped_list_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_trash_page_bytes payload)
    | Error _ as e -> e

  let restore (c : client) (req : restore_request) : (empty_response, string) result =
    match c.call ~service:"trash" ~op:"Restore" ~payload:(Codec.encode_restore_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e

  let purge (c : client) (req : purge_request) : (empty_response, string) result =
    match c.call ~service:"trash" ~op:"Purge" ~payload:(Codec.encode_purge_request_bytes req) with
    | Ok payload -> Ok (Codec.decode_empty_response_bytes payload)
    | Error _ as e -> e
end
