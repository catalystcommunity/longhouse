[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "longhouse_client"
version = "0.13.0"
requires-python = ">=3.9"
description = "Generated CSIL client package"
dependencies = []

[tool.setuptools]
packages = ["longhouse_client"]
