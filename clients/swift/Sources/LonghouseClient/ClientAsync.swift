// Generated CSIL service clients.
// Code generated by csilgen; DO NOT EDIT.

/// The caller-supplied byte carrier: it performs the call named by (service, op)
/// with the already-encoded request bytes and returns the response bytes, or
/// throws. Asynchronous — the seam suspends on the I/O round-trip. The generated
/// client owns (de)serialization; the carrier only moves bytes.
public protocol AsyncCsilTransport {
    func call(service: String, op: String, request: [UInt8]) async throws -> [UInt8]
}

/// AuthAsyncClient is a typed client for the AuthService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct AuthAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func login(_ request: LoginRequest) async throws -> LoginResponse {
        let csilResp = try await transport.call(service: "auth", op: "Login", request: request.toCbor())
        return try LoginResponse.fromCbor(csilResp)
    }

    public func complete(_ request: CompleteRequest) async throws -> LoginResponse {
        let csilResp = try await transport.call(service: "auth", op: "Complete", request: request.toCbor())
        return try LoginResponse.fromCbor(csilResp)
    }

    public func refresh(_ request: EmptyRequest) async throws -> LoginResponse {
        let csilResp = try await transport.call(service: "auth", op: "Refresh", request: request.toCbor())
        return try LoginResponse.fromCbor(csilResp)
    }

    public func logout(_ request: EmptyRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "auth", op: "Logout", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func me(_ request: EmptyRequest) async throws -> MeResponse {
        let csilResp = try await transport.call(service: "auth", op: "Me", request: request.toCbor())
        return try MeResponse.fromCbor(csilResp)
    }

}

/// DevAuthAsyncClient is a typed client for the DevAuthService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct DevAuthAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listDevUsers(_ request: EmptyRequest) async throws -> DevUsersResponse {
        let csilResp = try await transport.call(service: "devauth", op: "ListDevUsers", request: request.toCbor())
        return try DevUsersResponse.fromCbor(csilResp)
    }

    public func devLogin(_ request: DevLoginRequest) async throws -> LoginResponse {
        let csilResp = try await transport.call(service: "devauth", op: "DevLogin", request: request.toCbor())
        return try LoginResponse.fromCbor(csilResp)
    }

}

/// HouseAsyncClient is a typed client for the HouseService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct HouseAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createHouse(_ request: House) async throws -> House {
        let csilResp = try await transport.call(service: "house", op: "CreateHouse", request: request.toCbor())
        return try House.fromCbor(csilResp)
    }

    public func getHouse(_ request: HouseId) async throws -> House {
        let csilResp = try await transport.call(service: "house", op: "GetHouse", request: encodeHouseGetHouseRequest(request))
        return try House.fromCbor(csilResp)
    }

    public func updateHouse(_ request: House) async throws -> House {
        let csilResp = try await transport.call(service: "house", op: "UpdateHouse", request: request.toCbor())
        return try House.fromCbor(csilResp)
    }

    public func deleteHouse(_ request: HouseId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "house", op: "DeleteHouse", request: encodeHouseDeleteHouseRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listHouses(_ request: HouseListRequest) async throws -> [House] {
        let csilResp = try await transport.call(service: "house", op: "ListHouses", request: request.toCbor())
        return try decodeHouseListHousesResponse(csilResp)
    }

}

/// MemberAsyncClient is a typed client for the MemberService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct MemberAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createMember(_ request: Member) async throws -> Member {
        let csilResp = try await transport.call(service: "member", op: "CreateMember", request: request.toCbor())
        return try Member.fromCbor(csilResp)
    }

    public func getMember(_ request: MemberId) async throws -> Member {
        let csilResp = try await transport.call(service: "member", op: "GetMember", request: encodeMemberGetMemberRequest(request))
        return try Member.fromCbor(csilResp)
    }

    public func getMemberByIdentity(_ request: Member) async throws -> Member {
        let csilResp = try await transport.call(service: "member", op: "GetMemberByIdentity", request: request.toCbor())
        return try Member.fromCbor(csilResp)
    }

    public func updateMember(_ request: Member) async throws -> Member {
        let csilResp = try await transport.call(service: "member", op: "UpdateMember", request: request.toCbor())
        return try Member.fromCbor(csilResp)
    }

    public func deactivateMember(_ request: MemberId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "member", op: "DeactivateMember", request: encodeMemberDeactivateMemberRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func reactivateMember(_ request: MemberId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "member", op: "ReactivateMember", request: encodeMemberReactivateMemberRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listMembers(_ request: HouseScopedListRequest) async throws -> [Member] {
        let csilResp = try await transport.call(service: "member", op: "ListMembers", request: request.toCbor())
        return try decodeMemberListMembersResponse(csilResp)
    }

}

/// TrustedDomainAsyncClient is a typed client for the TrustedDomainService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct TrustedDomainAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func addTrustedDomain(_ request: TrustedDomain) async throws -> TrustedDomain {
        let csilResp = try await transport.call(service: "trusteddomain", op: "AddTrustedDomain", request: request.toCbor())
        return try TrustedDomain.fromCbor(csilResp)
    }

    public func removeTrustedDomain(_ request: TrustedDomainId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "trusteddomain", op: "RemoveTrustedDomain", request: encodeTrustedDomainRemoveTrustedDomainRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listTrustedDomains(_ request: HouseId) async throws -> [TrustedDomain] {
        let csilResp = try await transport.call(service: "trusteddomain", op: "ListTrustedDomains", request: encodeTrustedDomainListTrustedDomainsRequest(request))
        return try decodeTrustedDomainListTrustedDomainsResponse(csilResp)
    }

    public func isDomainTrusted(_ request: TrustedDomain) async throws -> BoolResponse {
        let csilResp = try await transport.call(service: "trusteddomain", op: "IsDomainTrusted", request: request.toCbor())
        return try BoolResponse.fromCbor(csilResp)
    }

}

/// RoleAsyncClient is a typed client for the RoleService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct RoleAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createRole(_ request: Role) async throws -> Role {
        let csilResp = try await transport.call(service: "role", op: "CreateRole", request: request.toCbor())
        return try Role.fromCbor(csilResp)
    }

    public func updateRole(_ request: Role) async throws -> Role {
        let csilResp = try await transport.call(service: "role", op: "UpdateRole", request: request.toCbor())
        return try Role.fromCbor(csilResp)
    }

    public func deleteRole(_ request: RoleId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "role", op: "DeleteRole", request: encodeRoleDeleteRoleRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listRoles(_ request: HouseScopedListRequest) async throws -> [Role] {
        let csilResp = try await transport.call(service: "role", op: "ListRoles", request: request.toCbor())
        return try decodeRoleListRolesResponse(csilResp)
    }

    public func grantRole(_ request: MemberRoleRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "role", op: "GrantRole", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func revokeRole(_ request: MemberRoleRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "role", op: "RevokeRole", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listMemberRoles(_ request: MemberScopedListRequest) async throws -> [Role] {
        let csilResp = try await transport.call(service: "role", op: "ListMemberRoles", request: request.toCbor())
        return try decodeRoleListMemberRolesResponse(csilResp)
    }

}

/// SkillAsyncClient is a typed client for the SkillService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct SkillAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createSkill(_ request: Skill) async throws -> Skill {
        let csilResp = try await transport.call(service: "skill", op: "CreateSkill", request: request.toCbor())
        return try Skill.fromCbor(csilResp)
    }

    public func updateSkill(_ request: Skill) async throws -> Skill {
        let csilResp = try await transport.call(service: "skill", op: "UpdateSkill", request: request.toCbor())
        return try Skill.fromCbor(csilResp)
    }

    public func deleteSkill(_ request: SkillId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "skill", op: "DeleteSkill", request: encodeSkillDeleteSkillRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listSkills(_ request: HouseScopedListRequest) async throws -> [Skill] {
        let csilResp = try await transport.call(service: "skill", op: "ListSkills", request: request.toCbor())
        return try decodeSkillListSkillsResponse(csilResp)
    }

    public func addMemberSkill(_ request: MemberSkillRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "skill", op: "AddMemberSkill", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeMemberSkill(_ request: MemberSkillRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "skill", op: "RemoveMemberSkill", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listMemberSkills(_ request: MemberScopedListRequest) async throws -> [Skill] {
        let csilResp = try await transport.call(service: "skill", op: "ListMemberSkills", request: request.toCbor())
        return try decodeSkillListMemberSkillsResponse(csilResp)
    }

    public func addGroupSkill(_ request: GroupSkillRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "skill", op: "AddGroupSkill", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeGroupSkill(_ request: GroupSkillRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "skill", op: "RemoveGroupSkill", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listGroupSkills(_ request: GroupId) async throws -> [Skill] {
        let csilResp = try await transport.call(service: "skill", op: "ListGroupSkills", request: encodeSkillListGroupSkillsRequest(request))
        return try decodeSkillListGroupSkillsResponse(csilResp)
    }

}

/// GroupAsyncClient is a typed client for the GroupService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct GroupAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createGroup(_ request: Group) async throws -> Group {
        let csilResp = try await transport.call(service: "group", op: "CreateGroup", request: request.toCbor())
        return try Group.fromCbor(csilResp)
    }

    public func updateGroup(_ request: Group) async throws -> Group {
        let csilResp = try await transport.call(service: "group", op: "UpdateGroup", request: request.toCbor())
        return try Group.fromCbor(csilResp)
    }

    public func deleteGroup(_ request: GroupId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "group", op: "DeleteGroup", request: encodeGroupDeleteGroupRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listGroups(_ request: HouseScopedListRequest) async throws -> [Group] {
        let csilResp = try await transport.call(service: "group", op: "ListGroups", request: request.toCbor())
        return try decodeGroupListGroupsResponse(csilResp)
    }

    public func addGroupMember(_ request: GroupMemberRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "group", op: "AddGroupMember", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeGroupMember(_ request: GroupMemberRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "group", op: "RemoveGroupMember", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listGroupMembers(_ request: MemberScopedListRequest) async throws -> [Member] {
        let csilResp = try await transport.call(service: "group", op: "ListGroupMembers", request: request.toCbor())
        return try decodeGroupListGroupMembersResponse(csilResp)
    }

}

/// ProjectAsyncClient is a typed client for the ProjectService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct ProjectAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createProject(_ request: Project) async throws -> Project {
        let csilResp = try await transport.call(service: "project", op: "CreateProject", request: request.toCbor())
        return try Project.fromCbor(csilResp)
    }

    public func getProject(_ request: ProjectId) async throws -> Project {
        let csilResp = try await transport.call(service: "project", op: "GetProject", request: encodeProjectGetProjectRequest(request))
        return try Project.fromCbor(csilResp)
    }

    public func updateProject(_ request: Project) async throws -> Project {
        let csilResp = try await transport.call(service: "project", op: "UpdateProject", request: request.toCbor())
        return try Project.fromCbor(csilResp)
    }

    public func deleteProject(_ request: ProjectId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "DeleteProject", request: encodeProjectDeleteProjectRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listProjects(_ request: HouseScopedListRequest) async throws -> ProjectList {
        let csilResp = try await transport.call(service: "project", op: "ListProjects", request: request.toCbor())
        return try ProjectList.fromCbor(csilResp)
    }

    public func listProjectTasks(_ request: ProjectScopedListRequest) async throws -> TaskList {
        let csilResp = try await transport.call(service: "project", op: "ListProjectTasks", request: request.toCbor())
        return try TaskList.fromCbor(csilResp)
    }

    public func addProjectTask(_ request: ProjectTaskOrderRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "AddProjectTask", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeProjectTask(_ request: ProjectTaskRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "RemoveProjectTask", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func setProjectTaskPosition(_ request: ProjectTaskOrderRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "SetProjectTaskPosition", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listProjectMembers(_ request: ProjectId) async throws -> [Member] {
        let csilResp = try await transport.call(service: "project", op: "ListProjectMembers", request: encodeProjectListProjectMembersRequest(request))
        return try decodeProjectListProjectMembersResponse(csilResp)
    }

    public func addProjectMember(_ request: ProjectMemberRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "AddProjectMember", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeProjectMember(_ request: ProjectMemberRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "RemoveProjectMember", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listProjectOwners(_ request: ProjectId) async throws -> [Member] {
        let csilResp = try await transport.call(service: "project", op: "ListProjectOwners", request: encodeProjectListProjectOwnersRequest(request))
        return try decodeProjectListProjectOwnersResponse(csilResp)
    }

    public func addProjectOwner(_ request: ProjectOwnerRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "AddProjectOwner", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeProjectOwner(_ request: ProjectOwnerRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "RemoveProjectOwner", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listMilestones(_ request: ProjectId) async throws -> [Milestone] {
        let csilResp = try await transport.call(service: "project", op: "ListMilestones", request: encodeProjectListMilestonesRequest(request))
        return try decodeProjectListMilestonesResponse(csilResp)
    }

    public func createMilestone(_ request: Milestone) async throws -> Milestone {
        let csilResp = try await transport.call(service: "project", op: "CreateMilestone", request: request.toCbor())
        return try Milestone.fromCbor(csilResp)
    }

    public func updateMilestone(_ request: Milestone) async throws -> Milestone {
        let csilResp = try await transport.call(service: "project", op: "UpdateMilestone", request: request.toCbor())
        return try Milestone.fromCbor(csilResp)
    }

    public func deleteMilestone(_ request: MilestoneId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "DeleteMilestone", request: encodeProjectDeleteMilestoneRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func setProjectVisibility(_ request: SetProjectVisibilityRequest) async throws -> Project {
        let csilResp = try await transport.call(service: "project", op: "SetProjectVisibility", request: request.toCbor())
        return try Project.fromCbor(csilResp)
    }

    public func listProjectGrants(_ request: ProjectId) async throws -> [Grant] {
        let csilResp = try await transport.call(service: "project", op: "ListProjectGrants", request: encodeProjectListProjectGrantsRequest(request))
        return try decodeProjectListProjectGrantsResponse(csilResp)
    }

    public func putProjectGrant(_ request: PutProjectGrantRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "PutProjectGrant", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func deleteProjectGrant(_ request: ProjectGrantRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "project", op: "DeleteProjectGrant", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

}

/// EventAsyncClient is a typed client for the EventService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct EventAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createEvent(_ request: Event) async throws -> Event {
        let csilResp = try await transport.call(service: "event", op: "CreateEvent", request: request.toCbor())
        return try Event.fromCbor(csilResp)
    }

    public func getEvent(_ request: EventId) async throws -> Event {
        let csilResp = try await transport.call(service: "event", op: "GetEvent", request: encodeEventGetEventRequest(request))
        return try Event.fromCbor(csilResp)
    }

    public func updateEvent(_ request: Event) async throws -> Event {
        let csilResp = try await transport.call(service: "event", op: "UpdateEvent", request: request.toCbor())
        return try Event.fromCbor(csilResp)
    }

    public func deleteEvent(_ request: EventId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "event", op: "DeleteEvent", request: encodeEventDeleteEventRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func deleteEventAndFuture(_ request: EventId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "event", op: "DeleteEventAndFuture", request: encodeEventDeleteEventAndFutureRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listEvents(_ request: HouseScopedListRequest) async throws -> [Event] {
        let csilResp = try await transport.call(service: "event", op: "ListEvents", request: request.toCbor())
        return try decodeEventListEventsResponse(csilResp)
    }

}

/// TaskAsyncClient is a typed client for the TaskService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct TaskAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createTask(_ request: Task) async throws -> Task {
        let csilResp = try await transport.call(service: "task", op: "CreateTask", request: request.toCbor())
        return try Task.fromCbor(csilResp)
    }

    public func getTask(_ request: TaskId) async throws -> Task {
        let csilResp = try await transport.call(service: "task", op: "GetTask", request: encodeTaskGetTaskRequest(request))
        return try Task.fromCbor(csilResp)
    }

    public func updateTask(_ request: Task) async throws -> Task {
        let csilResp = try await transport.call(service: "task", op: "UpdateTask", request: request.toCbor())
        return try Task.fromCbor(csilResp)
    }

    public func deleteTask(_ request: TaskId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "task", op: "DeleteTask", request: encodeTaskDeleteTaskRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listTasks(_ request: HouseScopedListRequest) async throws -> TaskList {
        let csilResp = try await transport.call(service: "task", op: "ListTasks", request: request.toCbor())
        return try TaskList.fromCbor(csilResp)
    }

    public func setTaskVisibility(_ request: SetTaskVisibilityRequest) async throws -> Task {
        let csilResp = try await transport.call(service: "task", op: "SetTaskVisibility", request: request.toCbor())
        return try Task.fromCbor(csilResp)
    }

    public func listTaskGrants(_ request: TaskId) async throws -> [Grant] {
        let csilResp = try await transport.call(service: "task", op: "ListTaskGrants", request: encodeTaskListTaskGrantsRequest(request))
        return try decodeTaskListTaskGrantsResponse(csilResp)
    }

    public func putTaskGrant(_ request: PutTaskGrantRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "task", op: "PutTaskGrant", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func deleteTaskGrant(_ request: TaskGrantRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "task", op: "DeleteTaskGrant", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

}

/// DependencyAsyncClient is a typed client for the DependencyService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct DependencyAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func addDependency(_ request: DependencyRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "dependency", op: "AddDependency", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func removeDependency(_ request: DependencyRef) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "dependency", op: "RemoveDependency", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func getDependencies(_ request: DependencyTarget) async throws -> DependencyGraph {
        let csilResp = try await transport.call(service: "dependency", op: "GetDependencies", request: request.toCbor())
        return try DependencyGraph.fromCbor(csilResp)
    }

}

/// CommentAsyncClient is a typed client for the CommentService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct CommentAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createComment(_ request: Comment) async throws -> Comment {
        let csilResp = try await transport.call(service: "comment", op: "CreateComment", request: request.toCbor())
        return try Comment.fromCbor(csilResp)
    }

    public func getComment(_ request: CommentId) async throws -> Comment {
        let csilResp = try await transport.call(service: "comment", op: "GetComment", request: encodeCommentGetCommentRequest(request))
        return try Comment.fromCbor(csilResp)
    }

    public func updateComment(_ request: Comment) async throws -> Comment {
        let csilResp = try await transport.call(service: "comment", op: "UpdateComment", request: request.toCbor())
        return try Comment.fromCbor(csilResp)
    }

    public func deleteComment(_ request: CommentId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "comment", op: "DeleteComment", request: encodeCommentDeleteCommentRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listComments(_ request: CommentListRequest) async throws -> [Comment] {
        let csilResp = try await transport.call(service: "comment", op: "ListComments", request: request.toCbor())
        return try decodeCommentListCommentsResponse(csilResp)
    }

}

/// NotificationAsyncClient is a typed client for the NotificationService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct NotificationAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listNotifications(_ request: NotificationListRequest) async throws -> [Notification] {
        let csilResp = try await transport.call(service: "notification", op: "ListNotifications", request: request.toCbor())
        return try decodeNotificationListNotificationsResponse(csilResp)
    }

    public func unreadCount(_ request: HouseId) async throws -> NotificationUnreadCount {
        let csilResp = try await transport.call(service: "notification", op: "UnreadCount", request: encodeNotificationUnreadCountRequest(request))
        return try NotificationUnreadCount.fromCbor(csilResp)
    }

    public func markRead(_ request: NotificationId) async throws -> Notification {
        let csilResp = try await transport.call(service: "notification", op: "MarkRead", request: encodeNotificationMarkReadRequest(request))
        return try Notification.fromCbor(csilResp)
    }

    public func markAllRead(_ request: HouseId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "notification", op: "MarkAllRead", request: encodeNotificationMarkAllReadRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

}

/// ShareAsyncClient is a typed client for the ShareService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct ShareAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func createShare(_ request: Share) async throws -> Share {
        let csilResp = try await transport.call(service: "share", op: "CreateShare", request: request.toCbor())
        return try Share.fromCbor(csilResp)
    }

    public func deleteShare(_ request: ShareId) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "share", op: "DeleteShare", request: encodeShareDeleteShareRequest(request))
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func listSharesByResource(_ request: ResourceRef) async throws -> [Share] {
        let csilResp = try await transport.call(service: "share", op: "ListSharesByResource", request: request.toCbor())
        return try decodeShareListSharesByResourceResponse(csilResp)
    }

    public func checkAccess(_ request: ShareAccessRequest) async throws -> Share {
        let csilResp = try await transport.call(service: "share", op: "CheckAccess", request: request.toCbor())
        return try Share.fromCbor(csilResp)
    }

}

/// MemberAuditAsyncClient is a typed client for the MemberAuditService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct MemberAuditAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listAuditsForMember(_ request: MemberScopedListRequest) async throws -> [MemberAudit] {
        let csilResp = try await transport.call(service: "memberaudit", op: "ListAuditsForMember", request: request.toCbor())
        return try decodeMemberAuditListAuditsForMemberResponse(csilResp)
    }

}

/// SettingsAsyncClient is a typed client for the SettingsService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct SettingsAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func getSettings(_ request: HouseId) async throws -> EffectiveSettings {
        let csilResp = try await transport.call(service: "settings", op: "GetSettings", request: encodeSettingsGetSettingsRequest(request))
        return try EffectiveSettings.fromCbor(csilResp)
    }

    public func updateSettings(_ request: UpdateSettingsRequest) async throws -> EffectiveSettings {
        let csilResp = try await transport.call(service: "settings", op: "UpdateSettings", request: request.toCbor())
        return try EffectiveSettings.fromCbor(csilResp)
    }

}

/// BugAsyncClient is a typed client for the BugService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct BugAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func reportBug(_ request: BugReportRequest) async throws -> Task {
        let csilResp = try await transport.call(service: "bug", op: "ReportBug", request: request.toCbor())
        return try Task.fromCbor(csilResp)
    }

}

/// AuditAsyncClient is a typed client for the AuditService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct AuditAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func queryAudit(_ request: AuditQuery) async throws -> AuditPage {
        let csilResp = try await transport.call(service: "audit", op: "QueryAudit", request: request.toCbor())
        return try AuditPage.fromCbor(csilResp)
    }

}

/// TrashAsyncClient is a typed client for the TrashService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct TrashAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listTrash(_ request: HouseScopedListRequest) async throws -> TrashPage {
        let csilResp = try await transport.call(service: "trash", op: "ListTrash", request: request.toCbor())
        return try TrashPage.fromCbor(csilResp)
    }

    public func restore(_ request: RestoreRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "trash", op: "Restore", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

    public func purge(_ request: PurgeRequest) async throws -> EmptyResponse {
        let csilResp = try await transport.call(service: "trash", op: "Purge", request: request.toCbor())
        return try EmptyResponse.fromCbor(csilResp)
    }

}

